# API余额通 —— 项目交接文档

> 版本：0.1.0-beta ｜ 中文名：API余额通 ｜ 英文名：API Balance Manager ｜ 作者：Zimu233L
> 项目路径：`D:\DeepSeek\api-quota-monitor-tauri`
> 最后核对：**2026-09/16**（含当日十九轮改动：挂件/设置/同步导出/站点表单 + **充值面板改独立窗口** + 三轮文档轮；§11 版本记录按时间顺序追加，最下面是最新）

---

## 0. 接手须知（先读这里）

**当前状态**：功能完整可用 —— `tsc` 0 错、`pnpm test` **116 项全绿**（6 个文件）、`cargo test --lib` **14 项全绿**、
`cargo check` 无警告、`pnpm build` 通过、`pnpm build` 产物无 dev 诊断标记残留；
Windows 免安装 exe 与 Android APK 均可出包（§7）。
⚠️ **唯一"实现完成但没跑过运行时"的地方 = 安卓端的导入/导出**（2026-09/16 新加）：只过了编译
（安卓目标 `cargo check` + Gradle Kotlin 编译），**设备端一次都没验过**，待办与四条待验清单见 §8。
**git 仓库：2026-09-16 晚「清洁重建」**（原因见 §8「历史里的登录态压缩包」：旧历史的
`minmax.7z` 内含 MiniMax 登录态 cookie，**推送即公开**）——现为 `git init -b main` 的全新历史，
当前 **1 个提交**（`main` 分支），入仓 **174 文件 / 14.4MB**；旧 `.git` 已压缩备份到
`D:\DeepSeek\api-quota-monitor-tauri-git-backup-260916.zip`（⚠️ 备份里仍含那套 cookie，
**作废凭据后请自行删除**）；**仍未配置 remote**。真实账号登录态凭据早已移出项目目录（§4.3、§8）。

**开工前必看**
- **⚠️ 验证策略（2026-09/16 用户口径）**：**外观 / 排版类改动不需要量测或截图验证** —— 用户原话
  「每次外观改变不用测试，我来确认就好了」，改完直接交给用户看。**功能 / 逻辑类**仍按本项目方法论
  实测（`probe/` 夹具、像素取样、端到端点击），并复跑 `tsc` / `pnpm test` / `cargo test --lib`。
- 起 dev 用 **`pnpm dev:app`**（= `tauri dev --config src-tauri/tauri.dev.conf.json`），**不要用裸 `pnpm tauri dev`**：
  裸命令会与桌面打包版共用 identifier 与配置目录 → 单实例锁互踢、config.json 互相覆盖。
- ⚠️ **改挂件前端（CSS/TSX）后必须重启 dev**：挂件窗口是 `prevent_close()+hide()` 常驻复用、
  页面 JS 从不重载 → 只靠 HMR 会继续跑旧代码，量测/肉眼看到的都是**假象**（本轮已两次踩到）。
- ⚠️ **改前端的"导出/签名"后也必须重启 dev**（2026-09/16 实测踩到 = **白屏**）：删掉某个被别的模块
  import 的导出后，WebView 缓存里的**旧模块**会与**新模块**混用 → `Uncaught SyntaxError: ... does not
  provide an export named ...` → `main.tsx` 整条链不执行 → React 挂不上 → 全白。症状是"dev 起来了、
  日志里却一条前端活动都没有"。**处理见 §10「白屏排查与修复」**（清 WebView2 缓存 + 重启）。
- ⚠️ **改 Rust（含 Cargo.toml）会让 dev 实例自动重编译并重启**（实测：`config.rs` 写入 → 10s 后
  debug exe 重链接 → 应用新 PID 起来）。所以"改完 Rust 要去点界面"时**先等它重启完**，别对着旧进程做判断；
  验证完记得看 `Get-Process -Name 'api-quota-monitor-tauri'` 的 `CreationDate` 是否已更新。
- ⚠️ **安卓整包构建需要「开发者模式」或管理员**（Windows 符号链接）：拿不到提权就跑 **§7.6** 的两道编译门
  （Rust 安卓目标 + Gradle Kotlin 编译），**但要说清运行时没验过**。
- ⚠️ **停 dev 要停干净**：残留进程会占住 1420 端口（`Port 1420 is already in use`），
  此时再起 dev 会失败、而你可能对着**上一个会话的挂件**做判断（本轮踩过：量到的倒计时是上一会话的）。
  清理见 §10。
- 本机**只有 Windows PowerShell 5.1，没有 pwsh 7**。改 `.ps1` 必须保 UTF-8 BOM（§7.1）；
  临时 `.ps1` 也要 BOM（无 BOM 的中文注释会被按 GBK 解析、直接语法崩——本轮写自动化脚本时踩过）。
- 挂件相关的一切坑**都写在 §4.6 / §4.10**，动挂件之前先读完再动手。

**三条用血换来的方法论（别重犯）**
1. **「抖 / 糊 / 手感不对」这类主观问题，先建客观口径、再改代码。** 本项目为此建了 `probe/` 量测夹具（§4.10）：
   屏幕捕获 + 亚像素质心 / 笔画暗度。历史上多次"凭感觉改渲染路径"，事后实测**全部无效**（含一次已回滚的方案）。
2. **量测口径本身要先验证可信。** 例：「边缘跃迁宽度」会随字号自然变大（受文字白描边几何影响），
   既掩盖真实差异、也制造过假差异——据此误判过两次。可信口径 = **同窗口原生对照**，或**笔画核心暗度 / 深墨像素占比**。
3. **别在"某个操作尚未发生"的稳态下做 A/B。** 验证「某操作之后才出现的问题」必须在**该操作往返之后**测
   （S→其它→S 对比，比较首末两次），否则测的是另一个状态，结论无效——本项目管理因此白改并回滚过一次。
4. **（2026-09/16 更新）修"某个量干扰了结果"这类问题，优先把**干扰量挪出公式**，而不是在公式里加修正项。**
   充值与消耗混在同一个余额快照里 → 老模型把"已登记充值"当修正项加进公式，结果**充值被算两次**、
   判据下界被抬高，越修越绕；换成"**下降**累加为消费、**上升**只进另一个字段"后，干扰在结构上就不存在了（详见 §5）。
   同理：只要某个量**跨刷新还要成立**，就必须落盘——别放在瞬态判断里（这是老模型连踩三坑的共性）。


---

## 1. 项目概述

跨平台 **API 余额集中监控工具**：把各大模型 API 网站的剩余额度集中显示。

- **Windows**：主窗口（默认 437×600，**可调整大小、退出记忆位置/尺寸**）卡片网格展示各站点余额；
  **鲸鱼娘挂件**展示窗（独立无边框置顶透明小窗，见 §4.6）：单击/60s 刷新、拖动、边缘与悬停滚轮缩放、
  **空白处点击穿透**、DeepSeek 官方站峰谷徽标、大小/位置记忆；**「今日充值金额」面板**（2026-09/16 起为
  **独立窗口**，恒在挂件正上方并与其右对齐，故挂件缩到最小也不会裁到面板，见 §4.6）
- **Android**：响应式适配（窄屏布局优化），无挂件、无后台常驻
- 视觉：Apple 风玻璃拟态 + 壁纸背景（深浅色切换）

前身是 Python/PySide6 版（`D:\DeepSeek\api-quota-monitor`，已废弃），本 Tauri 版完全重做。

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 框架 | Tauri 2.11.5（Rust 1.98 MSVC + WebView2/系统 WebView） |
| 前端 | React 19 + TypeScript + Vite 7 + pnpm 11 |
| 安卓侧 | **两个手写 Kotlin 插件**（`@TauriPlugin`，`gen/android/app/src/main/java/com/apibalance/monitor/`）：`WebLoginPlugin.kt`（内嵌 WebView 登录抓余额）、`ConfigExportPlugin.kt`（导出配置：系统分享 / 保存到本地）；Rust 侧分别由 `web_login.rs` / `export.rs` 用 `register_android_plugin` + `run_mobile_plugin` 调用（见 §4.3 / §4.9 / §7.6） |
| Rust 侧 | reqwest 0.12（cookies + rustls）、tauri-plugin-opener / clipboard-manager / single-instance（仅桌面）、base64 0.22（自定义壁纸解码）、zip 0.2（方案 A 内嵌运行时解压）、windows-sys 0.52（退出时终止残留 node 子进程 + **导出配置：`CF_HDROP` 把文件放进剪贴板** + **`SetWindowPos` 原子设置窗口几何**，仅 Windows） |
| 拖拽 | @dnd-kit/core + sortable（长按卡片排序） |
| 二维码 | qrcode（生成）+ jsqr（本地解析）+ deflate 压缩（`sync.ts`） |
| 图标 | lucide-react |
| 测试 | 前端 Vitest：`test/` **6 个文件 116 项** = providers(44) / sync(16，含**文件导出**的标准 JSON / 加密 / 空口令 / 文件名，与「导出配置文件」明文·加密·空口令三分支) / crypto(11，含 **AQM1 旧密文兼容 fixture**与篡改/版本护栏) / **usage(今日已用=累计下降，12 项)** / **deepseekPeak(峰谷倒计时口径)** / **fmt(金额格式化回归)**；后端 `cargo test --lib` **14 项**（BOM 容错、观测字段兼容、**余额观测账本 4 项**：只有下降进消费 / 乱序样本丢弃 / 跨天与币种变更重建 / 定点累加不漂移，**导出模块**：文件名清洗防穿越 + `DROPFILES` 二进制布局 + 非 ASCII 路径，以及 **`widget_menu` 面板定位**：右对齐贴上方 / 空间不足翻转 / 副屏上边界 / 高度变化时底边与右缘不动）。**每轮改动后均复跑全绿** |
| 量测工具 | `probe/`（仅 dev）：屏幕捕获 + 探针（抖动/清晰度/**布局报告**/**通用元素量测**/**背景图取样**），见 §4.10 |

## 3. 目录结构

```
api-quota-monitor-tauri/
├── index.html / vite.config.ts / package.json / tsconfig.json
├── src/
│   ├── main.tsx            # 入口（平台标记 + 悬浮提示初始化 + 窗口分流 widget/main）
│   ├── App.tsx             # 主界面（Bento 概览 + 操作行 + 卡片网格 + 弹窗管理）
│   ├── app.css             # 主样式（毛玻璃/卡片/对话框 + Android 平台适配）
│   ├── styles/theme.css    # 主题变量（深浅色）+ Maple Mono 字体
│   ├── assets/fonts/       # MapleMono-NF-CN 两个 woff2（各 ~6.3MB，2026-08 由 ttf 无损压缩而来）
│   ├── components/         # SiteCard / SiteDialog / SiteActionsDialog / SyncDialog / PlanFilterDialog /
│   │                       # SettingsDialog / MenuSelect / ConfirmDialog / Toast / AuroraBackground /
│   │                       # PlanValue（多方案数值渲染）/ ProviderIcon（站点图标）
│   ├── widget/             # 鲸鱼娘挂件（WidgetApp.tsx + widget.css，label=widget 窗口渲染）
│   │                       #   含：峰谷徽标+倒计时、今日已用、右上角菜单（充值登记）、命中遮罩上传
│   │                       # WidgetMenuApp.tsx + widget-menu.css = 「今日充值金额」面板（label=widget-menu
│   │                       #   独立窗口，位于挂件正上方、右对齐；高度回报给 Rust，见 §4.6）
│   ├── lib/                # store / worker / net / sync / crypto / fmt / theme / tooltip /
│   │                       # plans（计费方案）/ deepseekPeak（峰谷时段与倒计时文案）/
│   │                       # usage（「今日已用」本地推算 + 充值登记判据，见 §4.6）
│   └── providers/          # 适配器层（见 §4.3）
├── src-tauri/
│   ├── tauri.conf.json     # 窗口 437×600（可调）+ assetProtocol（壁纸本地文件）
│   ├── capabilities/       # default.json（main + widget + widget-menu 三个窗口、拖动/缩放/置顶权限）
│   ├── src/                # lib.rs（入口+单实例+窗口记忆+调试钩子+退出清理）/ network.rs（含 node PID 追踪）/ config.rs / widget.rs（挂件窗口 + `set_bounds_atomic`）/ widget_menu.rs（充值面板独立窗口）/ web_login.rs（安卓 WebView 登录桥接）/ export.rs（配置导出：写文件 + CF_HDROP 放剪贴板）
│   └── gen/android/        # Android 工程（tauri 生成）；app/src/main/java/.../WebLoginPlugin.kt（内嵌 WebView 登录）
│                           #   + ConfigExportPlugin.kt（导出配置：系统分享 / 保存到本地，2026-09/16）
├── test/                   # providers.test / sync.test / crypto.test
├── probe/                  # 开发期量测夹具（仅 dev；vite 插件 apply:'serve'，生产构建零注入）
│   │                       # 用途与用法见 §4.10
│   ├── zoom-probe.ts       #   serve 注入的探针：品红底+锚点黑方块、合成滚轮、逐帧采样、
│   │                       #   布局报告、原生清晰度对照（主窗口对照图**只在显式触发时注入**）
│   ├── vite-plugin-aqm.ts  #   /__aqm/trigger（读 trigger.json 触发）+ /__aqm/trace（落盘）
│   ├── capture.ps1         #   DPI 感知屏幕捕获（60fps）+ 品红 bbox + 黑块两遍亚像素质心
│   ├── run-test.ps1        #   一键一轮：先开捕获 → 触发探针 → 等落盘（挂件缩放抖动量测）
│   ├── trigger.json        #   trigger 计划（run 号变化即执行一轮）
│   └── traces/             #   历史采样（CSV/JSON）+ README 说明有效样本与判定口径
├── public/                 # app-icon.png / background.png（挂件背景图）/ vite.svg
├── 打包.ps1                # 一键打包脚本（exe / +APK，产物自动带时间戳命名+清理旧包，见 §7；
│                           # ⚠️ 改后必须保 UTF-8 BOM，且本机只有 PowerShell 5.1 → 见 §7.1）
├── minmax/                 # 「模拟浏览器登录」兜底方案的 Node+Playwright 查询脚本
│   ├── query_balance.js    #   通用单次查询（登录→跳余额页→监听接口→stdout JSON）
│   ├── server.js / mm_balance.html / 启动.bat   # 独立版遗留（曾用于手动验证 minimax）
│   ├── web_state/          #   仅独立运行脚本时的回退位置（App 不写这里，见下）
│   └── node_modules/       #   playwright（依赖；打包版 exe 运行需此目录在 exe 旁）
└── dist/                   # 前端生产构建产物（`pnpm build` 输出）

> ⚠️ **登录态（真实账号会话 cookie）现存放于应用配置目录，不在项目内**（2026-09/15 迁移）：
> `%APPDATA%\com.apibalance.monitor[.dev]\web_state\browser_<域名>.json`，与 config.json 同级。
> 由 Rust 在 spawn node 时通过环境变量 `AQM_WEB_STATE_DIR` 注入（`network.rs`），
> 脚本 `query_balance.js` 优先读该变量、未注入时回退脚本同级 `web_state/`（只影响独立手动运行）。
> `.gitignore` 同时忽略 `minmax/web_state/` 作为兜底。
```

## 4. 核心架构

### 4.1 网络层（全走 Rust）
- 所有 HTTP 请求经 Rust `http_request` 命令（Android WebView CORS + 会话 cookie 保持）
- **cookie jar 按 proxy 缓存 client**（登录会话依赖；代理站点独立 client）
- 响应：`{ status, body }`（10MB 上限，30s 默认超时，前端可覆盖）
- 代理：站点配置 `proxy` 字段注入每个请求（`worker.ts` 包装，适配器不感知）

### 4.2 配置存储
- `config.json`（Windows: `%APPDATA%\com.apibalance.monitor\`，Android: 应用私有目录）**明文存 API Key**
- ⚠️ **config.json 必须是无 BOM 的 UTF-8**（2026-09/15 修复）：`serde_json` 把 BOM 当作 JSON 文本的一部分 → 报 `expected value at line 1 column 1`。而用**记事本/编辑器另存**、或 **PowerShell 5.1 的 `Set-Content -Encoding UTF8`** 写出的 JSON 都会带 BOM，一旦发生：旧版会判定「配置损坏」→ 备份后回退默认 → 任何一次保存又把默认值写回 → **用户站点与 API Key 静默丢失**。现 `load_config` 已加 BOM 容错（`strip_utf8_bom`，带 Rust 单测）。**排查手法**：`([IO.File]::ReadAllBytes($p)[0..2]) -join ','`，为 `239,187,191` 即带 BOM（用 `[IO.File]::WriteAllText($p,$txt,(New-Object Text.UTF8Encoding($false)))` 写回可去掉）。
- Rust 侧 `Mutex` 写锁串行化写命令（save_config_cmd / upsert_provider / remove_provider / **record_usage_baseline** / save_wallpaper / widget_save_size）；损坏时自动备份 `.bak` 回退默认
- 原子写入：先写 `.json.tmp` 再 `rename`
- 配置字段：`autoRefreshMinutes`（全局默认）/ `theme` / `providers` / `wallpaper`（aurora|plain|custom）/ `wallpaperFile`（自定义壁纸路径）/ `showWidgetSize` + `showWidgetPos`（挂件）/ `mainWindowSize` + `mainWindowPos`（主窗口）/ **`usageBaselines`（「今日已用」当日基准，按站点 uid，见 §5）**
- 自定义壁纸文件存 `app_config_dir/wallpapers/custom.png`（`save_wallpaper` 命令，20MB 上限），前端 `convertFileSrc` 读取（tauri.conf `assetProtocol` scope 放开该目录）
- 前端 `lib/store.ts` 包装 invoke

### 4.3 适配器层（8 个内置）

**注册顺序 = 「添加站点」类型列表顺序（用户指定，有业务含义，勿随意调整）**：DeepSeek → 自动识别 → 阿里云 → 火山引擎 → Code AI → 其余；类型下拉**只显示名称、不带 `（id）` 括号**；
`SiteCard` 的 `MARKS` 缩写表需同步维护。Sub2API / One-API / New-API 等框架站**已并入通用自动识别**（不做独立适配器）。
**站点类型的默认显示名称（2026-09/16 用户要求）**：选到 **DeepSeek 官方**时「显示名称」默认填入 `DeepSeek官方`（`SiteDialog` 的 `DEFAULT_DISPLAY_NAMES`）。规则 = **只在名字空着、或名字还是自动填的那个值时才改**（用户自己敲过的名字不会被覆盖）；切到别的类型时把自动填的值清掉，避免留下与类型不符的名字。因 DeepSeek 官方是列表第一项、也是新建时的默认类型，**打开「添加站点」即已是 `DeepSeek官方`**。实测：①打开添加站点 → 显示名称 = `DeepSeek官方`；②切到自动识别 → 显示名称清空（`''`）；③切回 DeepSeek 官方 → 重新填回 `DeepSeek官方`。

| # | id | 说明 |
|---|---|---|
| 1 | deepseek_official | DeepSeek 官方（`/user/balance`，多币种 extra 拼接） |
| 2 | generic_login_html | **自动识别(中转站用这个)**：账号密码登录 + 自动识别（Sub2API / One-API / New-API 通用，见下） |
| 3 | aliyun | 阿里云账户余额（BssOpenApi `QueryAccountBalance`，AccessKey + RPC V1 签名；见下） |
| 4 | volcengine | **火山引擎账户余额**（BssOpenApi `QueryBalanceAcct`，AccessKey + 火山签名；见下） |
| 5 | code_ai | **Code AI（OpenCode / CommandCode，下拉选择服务商）**（见下） |
| 6 | openai_compat | OpenAI 兼容计费接口（`/v1/dashboard/billing/credit_grants`） |
| 7 | openrouter | OpenRouter（`/api/v1/auth/key`） |
| 8 | browser_login | **模拟浏览器登录（兜底）**：Windows Edge 无头自动登录 / 安卓内嵌 WebView 手动登录（见下） |

**aliyun 适配器（2026-08 新增）**：
- 请求 `business.aliyuncs.com` 的 `QueryAccountBalance`（BssOpenApi 2017-12-14）：返回 `AvailableAmount`（可用余额）/`AvailableCashAmount`（现金）/`CreditAmount`（信用），主余额=可用余额，详情行显示现金与信用
- 认证：RAM 子账号 AccessKey + **RPC V1 签名（HMAC-SHA1，纯 JS 实现 SHA-1/HMAC/Base64，不依赖 Web Crypto）**；需在 RAM 控制台给子账号授权 `AliyunBSSReadOnlyAccess`（未授权返回 `NotAuthorized`，前端给出授权提示）
- ⚠️ 端点域名是 `business.aliyuncs.com`（`bssopenapi.aliyuncs.com` 不存在、解析不到）；AK/SK 明文存于配置文件，注意导出同步文件的保密

**volcengine 适配器（2026-08 新增）**：
- 请求 `billing.volcengineapi.com` 的 `QueryBalanceAcct`（billing 2022-01-01）：返回 `AvailableBalance`（可用）/`CashBalance`（现金）/`CreditLimit`（信用）/`FreezeAmount`（冻结）/`ArrearsBalance`（欠费），主余额=可用余额，详情行显示现金/信用/冻结，欠费>0 附提示
- 认证：API 访问密钥（控制台「右上角头像 → API 访问密钥」，或 IAM 子账号密钥）+ **火山签名（HMAC-SHA256，纯 JS 实现 SHA-256/HMAC，不依赖 Web Crypto）**；子账号需授权 `BillingCenterReadOnlyAccess`（只读余额）；子账号密钥建议只读授权，主账号密钥权限过大
- ⚠️ 签名要点：`Authorization: HMAC-SHA256 Credential=<AK>/<YYYYMMDD>/cn-beijing/billing/request, SignedHeaders=host;x-date, Signature=<hex>`——**Credential 带 CredentialScope、只签 host 与 x-date 两个头**（不含 X-Nonce/X-Content-Sha256，官方文档 GET 示例即此格式）；`X-Date: YYYYMMDDTHHMMSSZ`；`region=cn-beijing`、`service=billing`；签名前 QueryString 按 key 字典序 RFC3986 编码
- ⚠️ 曾踩坑：`Authorization` 写成 `Credential=<AK>,`（不带 scope）会返回 `InvalidAuthorization`；正确的 Credential 部分含 `/date/region/service/request`

**minimax API-Key 适配器已于 2026-08 移除**：官方站账号密码登录必须浏览器自动化（SPA + OAuth2 SSO + 可能滑块验证码，HTTP 无解）；minimax（及类似官方 SPA 站）统一走「模拟浏览器登录」兜底（见下）。

**code_ai 适配器（OpenCode / CommandCode 合并入口，2026-08）**：
- 一个站点类型，配置首字段 `service` **下拉选择服务商**（`ConfigField` 新增 `type:'select'` + `options`，SiteDialog 普通/高级字段区均支持下拉渲染）
- **OpenCode**（`service:'opencode'`）：
  - Go 订阅用量：API Key → 官方未文档化接口 `GET /zen/go/v1/usage`（`Authorization: Bearer`）→ `usage.rolling/weekly/monthly.percent`（5小时/周/月窗口），主余额 = `100 - rolling%`，币种 `%`（worker 币种白名单透传，显示「96.00 %」）；403 = key 有效但无 Go 订阅 → 中性占位
  - Zen 预付费余额：auth cookie + workspace id → `GET /workspace/<id>`（Cookie: auth=…）→ 正则提取 `Current balance $X.XX`；cookie 有效期 1 年
  - 两者都填 → `plans: { paygo: Zen, plan: Go }` 两种计费方式分卡显示（Zen 主显示）
- **CommandCode**（`service:'commandcode'`）：API Key（`commandcode.ai/settings/api` 创建）→ `GET /alpha/whoami`（取 org.id）→ `GET /alpha/billing/credits?orgId=`（月剩余 `monthlyCredits`/已购/免费）+ `GET /alpha/billing/subscriptions`（`planId`/`currentPeriodEnd`）；请求头带官方 CLI 指纹 `x-command-code-version: 0.52.1` + `x-cli-environment: production`；套餐映射表（individual-go=$10 … ultra=$300）→ extra 显示「套餐 Pro·$30/月 · 本期至 2026-09-13」；主余额 = 月剩余（0 时回退已购/免费）
- 旧 `opencode` 独立适配器已合并删除，`getProvider('opencode')` 保留别名兼容
- 参考研究：Go 用量 [cc-switch#6433](https://github.com/farion1231/cc-switch/issues/6433)；Zen 无官方 API（[anomalyco/opencode#10448](https://github.com/anomalyco/opencode/issues/10448)）；CommandCode 官方 CLI 同款接口（[CodexBar](https://github.com/steipete/CodexBar) 等社区实现背书）

**browser_login 适配器（「模拟浏览器登录」兜底，2026-08）**：
- 定位：其它方式（自动识别/探测接口）识别不了时用——自动用 Edge 无头打开登录页账号密码登录，跳余额页抓余额接口；适合带滑块/验证码/SPA、无公开接口的网页站点
- 链路：前端 `browser_login.ts` → `invoke('browser_login_query')`（Rust `network.rs`）→ **按需 spawn 一次性 Node 进程**执行 `minmax/query_balance.js`（Playwright `channel:'msedge'` 无头，登录后跳 `balanceUrl` 监听 URL/响应含 `balanceKeyword` 的接口，解析 `available_amount/cash_balance/voucher/credit/owed` 等字段）→ stdout JSON → 前端；**180s 超时 kill**（2026-08 从 90s 放宽：脚本最坏路径「直连超时→重登→跳页→等接口」可能逼近 2 分钟，90s 会误杀；实测全新登录 18s 完成）
- ⚠️ 2026-08 补全：`query_balance.js` 曾缺失（minmax 目录只有旧 `server.js` 独立版遗留），已按本文档描述重建并**真实账号实测通过**（全新登录 + 登录态复用两路径，MiniMax 站）；脚本支持「第一个参数传 JSON 或参数文件路径」，非 MiniMax 站点走通用选择器启发式登录
- ⚠️ 2026-08 MiniMax 新版登录页适配（`account.minimaxi.com/unified-login`，旧 `platform.minimaxi.com/login` 已弃用）：①默认是「手机号验证码登录」，需切到「账号密码登录」tab ②**必须勾选协议**（自定义 `<button>` 勾选框 + 「我已阅读并同意」文案行，不勾选点「立即登录」无任何反应）③**登录成功判定改用监听登录 API 响应 `code===0`**（无头下 SPA 前端不跳转，URL 轮询永远超时；响应后前端 JS 写会话 cookie，直接手动跳余额页）④错误探测正则不能用裸「验证码」（页面有「邮箱验证码登录」tab 文本会误判）
- 配置：登录页地址 / 余额页地址（必填）/ 账号 / 密码（**仅桌面端**，`desktopOnly` 字段安卓端自动隐藏——安卓为 WebView 手动登录）/ 余额接口关键字（高级，默认自动识别 `query_balance|balance|available…`）
- **登录态持久化**：成功后存 `browser_<域名>.json`（兼容复用旧 `minimax_web.json`），下次查询优先复用，避免重复登录。**⚠️ 2026-09/15 起存放位置改为应用配置目录** `%APPDATA%\com.apibalance.monitor[.dev]\web_state\`（原为项目目录 `minmax/web_state/`，属源码树内明文凭据），由 Rust 注入环境变量 `AQM_WEB_STATE_DIR` 指定；脚本未收到该变量时回退脚本同级 `web_state/`（独立手动运行脚本的行为不变）
- **依赖**：**方案 A 假单文件（2026-08）**——release 构建把 `minmax/` 全量（node.exe 93MB + playwright 17.7MB）压缩成 `runtime.zip`（38.8MB）经 `include_bytes!` **嵌入 exe**；首次「模拟浏览器登录」调用时自动解压到 `%APPDATA%\com.apibalance.monitor\runtime\`（可写，登录态 web_state 随之可写）——**分发物只有一个 exe（约 65MB），目标机免装 Node.js**；便携分发（exe 旁放 minmax 目录）仍兼容（优先级：exe 旁 minmax → 内嵌 runtime → dev 项目路径）；dev 模式不嵌入、用项目 minmax + PATH node；脚本定位逻辑：**exe 同级 `minmax/query_balance.js` 优先**，回退项目根路径 `src-tauri/../minmax/query_balance.js`（⚠️ 原代码 `../../minmax` 从 src-tauri 上跳两级指向 `D:\DeepSeek\minmax`，路径不存在——接口从一开始就是断的；独立跑 node 脚本绕过了 Rust 路径解析所以此前未暴露）
- **Android 端（方案 A，2026-08 完整可用）**：`browser_login_query` 安卓分支走 `web_login` 移动插件（`src/web_login.rs` 桥接 Kotlin `WebLoginPlugin.kt`）——**内嵌 WebView 手动登录**（用户可真人过验证码/滑块，登录态由系统 `CookieManager` 持久化）；**无感刷新**：WebView 不立即显示，有登录态时后台加载余额页抓取（DOM 提取 `encodeURIComponent` 通道 + 正则，SPA 渲染 2s×8 次重试）自动完成，仅被重定向回登录页时才弹出界面；**刷新兜底**：重试耗尽返回失败 + 45s 整体超时，杜绝「永久刷新中」（invoke 永不返回）；账号/密码字段安卓端隐藏（`desktopOnly`），只填登录页/余额页地址即可

> **Sub2API 框架适配器已于 2026-08 移除**：多数 Sub2API 部署没有统一余额接口，且其标准路径
> （`/api/v1/auth/login`，JSON `{email,password}`，登录响应即带 `data.user.balance`）与
> `generic_login_html` 的自动探测完全兼容——统一由自动识别接管（lvzhouai / poke2api 等已实测通过）。
> 旧配置用 `sub2api_framework` 的站点迁移为 `generic_login_html`（`base_url` → `login_url`）。
> **One-API / New-API 框架适配器（`oneapi_newapi`）亦已于 2026-08 移除**：`/api/user/self` Bearer 查询
> 由自动识别接管（登录响应提取 token → `/api/user/self` 探测已在余额 API 10 路径内，Token 缓存机制保障不重复登录）。

**generic_login_html 自动识别**：
- **登录探测 9 路径 × 5 组合**：`/api/v1/auth/login`（Sub2API 标准）**排首位**；组合顺序 用户配置 → json+email → json+account → form+account → form+email（json+email 提前命中 Sub2API 类站点，减少探测请求）
- 探测链：登录响应 JSON 余额 → 余额 API 探测（10 路径）→ 页面 HTML 启发式（关键词 ± 货币符号 ± 小数形态三策略打分）→ 首页兜底
- `_detected_login`/`_detected_balance` **缓存到 config**（首次探测后直接复用：普通站点 2-3 请求；Sub2API 类登录响应即余额则只需 **1 个请求**）
- `balance_choice` 记住用户校准（按 context 特征匹配同一处新数值）；候选数字附上下文供用户点选（`SiteDialog` 候选校准区）
- **多计费方式自动识别（2026-08）**：登录响应 JSON 按 key 语义归类 `plan`（套餐）/`duration`（包时）/`times`（按次）/`paygo`（按量），**`expires_in`/`expire_at`/token 过期字段排除在包时之外**（绿洲API 误判修复）；站点勾选 `_auto_billing_plans` 时每次刷新自动合并（自愈：检测结果变化自动纠正），手动保存即退出自动模式；适配器优先输出 `plan` 套餐，主余额默认选按量组
- ⚠️ **探测失败会抛出「未找到可用的登录接口」**（不再回退用根地址 POST——历史 bug 会把 404 响应当 JSON 解析，错误提示变成误导性的「登录响应是 JSON 但无余额字段」，2026-08 已修）
- 无数据三态区分：`ok:false` 且余额全空 → 灰色破折号「无数据」；真实错误 → 红色错误提示（2026-08 起卡片/详情/操作弹窗统一）
- **登录 Token 缓存机制（2026-08）**：登录响应里的鉴权 token 自动存进配置（`_cached_token` + `_cached_token_at`），默认缓存 **30 分钟**（高级设置 `token_ttl_minutes` 可调，0=每次重新登录）；余额接口（source=api）站刷新时 token 未过期 → **跳过登录直接复用**（防会话上限/限流）；token 401/403 自动清缓存重新登录（自愈）；**409 `AUTH_SESSION_LIMIT` 会话数上限**（如 api.maomaoai.pro）→ 自动尝试登出旧会话（常见 logout 路径）再重试，仍失败给明确提示「请到站点后台退出旧会话」

### 4.4 刷新调度
- `worker.ts fetchEntry`：**币种强制 CNY**（手动 `display_currency` 可覆盖；`%` 配额百分比白名单透传，2026-08）+ ProviderError 转 ok=false + **代理注入**（config.proxy → 每个请求）
- 适配器返回 `plans`（多方案数据）时自动勾选 `billingPlans`（自动模式自愈合并；用户手动保存退出自动模式）
- 单站刷新序号按 uid 隔离（避免并发互丢）；启动自动刷新（900ms 后全刷一次）+ **30s tick** 轮询：每站点按自身 `autoRefreshMinutes`（站点优先，0 或未设回落到全局默认）判断是否到期
- 结果落库双通道：前端 `setResults` + `widget_set_result` 同步 Rust 侧缓存（挂件打开直接复用）

### 4.5 主窗口几何记忆（2026-08 完成，替代"固定大小"）
- 主窗口 `width=437 height=600 minWidth=437`，**可调整大小**（无 resizable=false）
- 退出（CloseRequested）时保存 `mainWindowPos` / `mainWindowSize`（逻辑像素，`to_logical(scale_factor)` 换算，防高 DPI 错位）
- 启动 setup 后延迟 200ms 恢复（避开窗口初始化流程覆盖）；位置校验仍在某个显示器工作区内（外接屏拔掉防飞出）

### 4.6 鲸鱼娘挂件（2026-08 完成；2026-09~09/16 多轮改动：缩放实测标定 / 峰谷徽标+倒计时 / 空白穿透 / 「今日已用」观测账本（2026-09/16 改为"消费只由下降构成"）/ 充值面板独立窗口 / 排版与开关）
- 独立无边框置顶透明小窗（label=`widget`），默认 308×308（背景图 30%），最小 160×180，跳过任务栏
- **完全隐框**：`decorations(false)` + `resizable(false)` + `shadow(false)`（阴影/系统 resize 边框会呈现为边框与顶部白线观感）；边缘缩放由前端 JS 自实现（setSize 不受 resizable 限制）
- **复用 index.html 入口**：前端按 `getCurrentWindow().label` 分流（widget → `src/widget/WidgetApp.tsx`）。这一决策是解决历史白屏的关键——独立 html 入口在 dev 模式（vite 未配多页）会 404/加载异常
- **站点数据握手**：Rust 侧 `WidgetState` 记录目标站点，挂件页挂载后 invoke `widget_get_site` 主动拉取；窗口已存在时切换站点走 `widget:open` 事件（页面常驻，listen 不丢）
- 窗口 `visible(false)` 创建，`on_page_load Finished` 后再 show，避免白屏/闪烁
- 交互对齐 Python 版 show_window：**单击刷新**（mouseup 位移判定：位移 <4px=刷新，≥4px=`startDragging` 原生拖动；不用 click 事件——drag-region 会吞掉首次 click 导致"要双击才刷新"）、60s 自动刷新、JS 边缘缩放（10px 感应，**固定 1:1 正方形**，取期望宽高较大边 + 最小尺寸钳制；悬停边缘显示对应方向缩放箭头光标）、**悬停滚轮缩放（鼠标焦点版，2026-09 重构）**（见下方「挂件滚轮缩放实现说明」）、右上角 ↩ 返回（无悬浮提示文字）
- **主窗口联动**：展开挂件时 `main.hide()`（任务栏一并隐藏）；挂件关闭时 `main.show() + unminimize + set_focus` 恢复
- **窗口常驻策略**：CloseRequested 用 `prevent_close()` 拦截改为 `hide()`（窗口不销毁，再次展开直接 `show()` 复用）。历史教训：若用 `close()` 销毁，异步销毁期间窗口句柄仍存在，「已存在」分支只 emit 不 show，会复现「点按钮主窗口隐藏了但挂件不出现」的竞态
- **文字/内容随窗口缩放**：内容（背景图+三行文字+峰谷徽标）放在固定 **308×308 画布**（`.widget-canvas`）内，画布整体 `transform: scale(窗口边长/308)` 由 GPU 合成器缩放（窗口 308px 时 scale=1，视觉字号基准 16px=308 基准不变）；文字位置用画布高度百分比（站名 17.5% / 金额 29% / 状态 40.5%），水平居中于左 2% 宽 84%；返回按钮在画布外不随缩放（fixed 右上 10px）
- **DeepSeek 峰谷徽标 + 倒计时（2026-09/11 新增；2026-09/15 加倒计时并归位）**：仅 `deepseek_official` 站点显示，在**站名行上方**（`.widget-peak.row`，画布 `top: 9.9%` → 中心 y≈30.5、占 21~40px，与站名间隙实测 3.1px）。徽标（`lib/deepseekPeak.ts` 的 `DEEPSEEK_PEAK_INFO` 峰/谷标签与浅色主题配色，字号 13px / 19px 高——2026-09/11 按用户要求由 10px / 16px 放大）与**距下一时段切换的倒计时**同行 flex 居中显示，如「谷 剩余13时29分」。
  - 倒计时用 `fmtPeakCountdownRemaining`（2026-09/15 用户指定措辞「剩余5时23分」）：**与主卡片 `fmtPeakCountdown` 同口径**（同一套 `deepseekNextChange` 剩余秒数、同样的 **2 小时**分级阈值），只措辞不同——≥2 小时「剩余5时23分」（分钟补零两位）/「剩余5时」/「剩余2天1时」；<2 小时带秒「剩余1时59分59秒」/「剩余23分05秒」/「剩余45秒」。⚠️ 保留秒是为了不出现「剩余0分」这种"看起来停了"的文案。
  - 刷新节奏与主卡片同规则：剩余 <2 小时逐秒跳动，否则 **10 秒**一次（分钟级足够）。
  - 徽标尺寸用**固定画布 px**（不能用 vw/clamp——画布是被 transform 整体缩放的，视口单位会与画布缩放打架）。
  - ⚠️ **落点试错记录（勿重走）**：2026-09/15 曾按要求把这一行下移到「今日已用」下方（画布中心 y≈164）。实测该处（x≈110~200、y≈152~176）是角色头部与肩部之间**近乎全透明的横带**（平均 alpha 仅 1~30%，点击会穿透），再往下 y≥184 即角色白肩 → 观感"悬空"。用户看过效果后要求**放回顶部**，故恢复原落点。落点由 `probe/bg-sample.mjs`（背景图 alpha/亮度取样，见 §4.10）实测确定。
  - 实测验收（探针布局报告）：峰谷行 y=20.1 h=19；chip y=20.1 h=19；倒计时 y=23.1 h=13；站名 y=49.3；金额 y=77.5 h=36；今日已用 y=118.2 h=13；文案「剩余13时29分」与「北京时间 19:30 → 次日 09:00 = 13 时 29 分」逐位吻合。
  - **行距（2026-09/15）**：有特殊内容行（DeepSeek 有峰谷行）时 峰谷行→站名 **10.2px**、站名→金额 **6.6px**、金额→今日已用 **4.7px**（`.widget-name` 19.5% / `.widget-balance` 31% / `.widget-status` 40.5%）。
    - **无特殊内容行时（非 DeepSeek 站点）三行整体上移 10.2px 画布（≈3.3%）**（2026-09/15 用户要求：「显示没有特殊显示内容的站点数据（如峰谷等）时，三行上移」）：`WidgetApp.tsx` 给画布加 `data-has-peak`（1=有 / 0=无），CSS 用 `.widget-canvas[data-has-peak='0']` 把三行改到 站名 **16.2%** / 金额 **27.7%** / 状态 **37.2%**。上移量取 10.2px = 「有峰谷行时的 峰谷行→站名 间隙」，因此两种站点的顶部留白观感一致；**行间相对关系完全不变**。
      - 实测（非 DeepSeek 站点 Max66）：站名 y 49.3→**39.1**、金额 77.5→**67.3**、状态 118.2→**108.1**（三行各上移 10.2px，间隙仍为 6.6px / 4.7px）；三行落点仍在白色气泡内（画布 y=12~125 亮度 255）。
      - ⚠️ 未实测：**有峰谷行那一侧**本轮没量到（挂件始终显示 Max66，主界面未推切换到 DeepSeek）。该侧数值沿用上一轮实测值（49.3 / 77.5 / 118.2，与本次上移前的值一致），且新规则的选择器只匹配 `data-has-peak='0'`，不影响该侧。
    - ⚠️ **曾尝试按「气泡内区等间距」重排并已被用户要求撤回**（2026-09/15，见 §11 第六轮）：当时把三行改成 17.5% / 31.5% / 44.1%（相邻间隙各 14.3px），用户看后要求撤回，已回到上面的数值。**教训：改纵向分布前先确认用户想要的效果**；气泡内区仅 103px 而内容总高 70.6px，「等间距」与「不越界」不能兼得（当时实测结论，可作讨论依据）。
- **「今日已用」行的币种符号间隙（2026-09/15 用户要求，仅此行）**：`¥` 与数字之间加一点间隙。实现 = `fmt.ts` 新增 `fmtMoneyParts`（拆成「前缀符号 + 数字主体」）与 `fmtMoneySpaced(v, currency, thinSpace)`；挂件该行传入 **U+2009 THIN SPACE**（≈0.2em），其余金额（余额大字、卡片、方案卡等）**仍走 `fmtMoney` 紧贴格式不变**。
  - ⚠️ 间隙由**字符**承载而非 CSS margin：宽度可量测、可单独调到更窄/更宽，且不影响「今日已用」标签与金额之间的字距；对没有前缀符号的币种（`%` 配额、未知币种）自动退化为与 `fmtMoney` 完全一致。
  - 实测：探针布局报告回传文字 `今日已用 ¥ 0.00`，非 ASCII 码位为 `U+2009`（在 `¥` 与 `0.00` 之间）；行宽 258.7 = 文字列宽，未溢出。`test/fmt.test.ts` 18 项覆盖（含 **`fmtMoney` 既有行为回归**——拆分重构不得改变卡片等既有调用点的输出）。
- ⚠️ **改挂件前端后必须重启 dev**（历史教训见 §4.6「踩坑教训」末条）：挂件窗口是 `prevent_close()+hide()` **常驻复用**，页面 JS 从不重载 → HMR 后旧代码仍在跑，量测会拿到**假象**。本轮改 CSS 后即按此重启复测。
- **大小/位置记忆（三层保障）**：① 缩放结束 mouseup 时前端 invoke `widget_save_size` 立即持久化（应用退出也不丢）；② 关闭/隐藏时 `CloseRequested` 兜底保存（Rust 侧 `to_logical` 换算防高 DPI 放大）；③ 打开时读取 `showWidgetSize` 恢复，位置同样记忆 `showWidgetPos`。前端 `saveConfig` 保留这些字段
- 诊断日志：`[widget]` 前缀（page-load / page-state / render-state），dev 用 `AUTO_WIDGET_UID=first` 环境变量自动开挂件（release 无此钩子）
- 背景图 `public/background.png`（从 Python 版复制，1026×1026 RGBA；**约 48% 像素完全透明**，鲸鱼娘是异形角色）
- **空白处点击穿透（2026-09/11 新增）**：窗口是矩形但背景图近一半透明，那些空白会**吃掉鼠标事件、挡住后面的窗口**。实现 = **动态 `set_ignore_cursor_events`**：Rust 侧约 60Hz 轮询全局光标 → 查前端上传的命中遮罩 → 光标落在空白处就把整个窗口设为穿透、回到实体处再关掉。
  - **不用 `SetWindowRgn`**（硬边区域会把图片的抗锯齿边缘切出锯齿，且每次缩放都要重建区域）；本方案只改命中性，**渲染完全不受影响**。
  - 遮罩由**前端**从 `background.png` 的 alpha 通道降采样成 128×128 二值网格后一次性上传（命令 `widget_set_hitmask`）——Rust 侧读不到前端打包资源。取块内**平均** alpha + 低阈值（≥16）≈ 对实体区域做膨胀：宁可多捕获一点，也不能把本体判成空白（否则点不到、拖不动）。实测实体格 8833/16384 = **53.9% 实体 / 46.1% 穿透**。
  - ⚠️ **右上角固定 UI 必须整体特殊保护**（2026-09/16 修正）：`.widget-back`（返回）与 `.widget-menu`（菜单）都定位在窗口右上角、**压在透明区上**，若只按遮罩判定就会点不到——返回键表现为"关不掉挂件"，菜单键表现为"点了没反应、面板打不开"（两者都实测踩到）。现 Rust 侧把**两键所在矩形（宽 40 × 高 72 CSS px，各留 4px 余量）**始终算作实体（`in_fixed_overlay`）。`widget_hit_state` 诊断字段相应由 `backButton` 更名为 **`fixedOverlay`**。
    - ⚠️ **该矩形由 230×176 缩小到 40×72（2026-09/16）**：原尺寸是为了把「展开的面板」也罩进来（面板当时画在本窗口内、`top:42 right:42` 宽 178）。面板改为**独立窗口**后不再需要，右上角其余空白恢复"点一下就穿透"。**缩小时必须重算两键边界**：返回键 `top:10 right:10 26×26`、菜单键 `top:42 right:10 26×26` → 覆盖 `x ≥ W-36`、`y ≤ 68`，取 40×72 留 4px 余量（实测：真实鼠标点两键均命中，见下方验收）。
  - 遮罩缺失/越界/加锁失败一律按**实体**处理（保守：不穿透），上传失败也不影响可用性；窗口隐藏时自动复位穿透态（避免下次显示时残留导致整窗点不动）。
  - **验收**（把光标移到指定点后读 `widget_hit_state` 诊断命令）：空白处 → 穿透=true ✓；鲸鱼娘本体 → 穿透=false ✓；右上角固定 UI 区 → 穿透=false（`fixedOverlay=true`，保护生效）✓；该区域下方空白 → 穿透=true ✓；窗口外 → false ✓。
  - **代价**：拖动窗口必须抓在鲸鱼娘本体上（空白处不再可拖）。关掉该功能只需删掉 `WidgetApp.tsx` 里调用 `uploadHitMask()` 的那个 effect（无遮罩即不穿透）。

#### 充值面板独立窗口（2026-09/16，用户口径「把他独立，移动到鲸鱼娘窗口上方，和鲸鱼娘窗口右对齐」）
- **背景（用户反馈）**：面板原先内嵌在挂件窗口里（`.widget-menu-panel` 绝对定位于画布右上角外侧）。挂件窗口最小可缩到 160×180，而面板固定 178 宽 + 约 130 高 → **挂件越小越会被裁掉**（"设置界面显示不全"）。
- **做法**：新增 Rust 模块 `widget_menu.rs` + 前端 `WidgetMenuApp.tsx`（窗口 label=`widget-menu`）。窗口 = 无边框 + 透明 + 置顶 + 不占任务栏 + 系统不可缩放；**位置 = 挂件正上方、与挂件右对齐**（`menu_origin`：`x = 挂件右缘 − 面板宽(224)`、`y = 挂件顶边 − 6 − 面板高`）。
  - ⚠️ **上方空间不足**（挂件贴显示器顶边）时**翻到挂件下方**（同样右对齐）——宁可换到下面，也不能把面板推到屏幕外让用户点不到。上边界取挂件所在显示器的顶边（副屏可能为负或很大）。
  - ⚠️ **高度由内容决定**：前端量测根节点高度后回报 `widget_menu_resize`，Rust 按新高度重算位置 → **底边与右边缘不动，面板向上生长**。`menu_origin` 的 4 项 Rust 单测钉住「右对齐 / 贴上方 / 越界翻转 / 高度变化时底边不动」四条不变量。
  - ⚠️ **前端根节点不能写 `inset:0` / `height:100%`**：那样量到的是窗口高度而非内容高度 → 「窗口变高 → 内容变高 → 再变高」自激循环。
- **跟随挂件**：挂件拖动是**系统级拖动**、缩放是逐帧改窗口几何，两者都**没有"挂件移动了"的事件可监听** → 借挂件那条已有的 ~60Hz 命中检测轮询线程顺带调用 `sync_menu_pos`（只在目标位置与当前位置差 ≥0.5px 时才调系统 API，静止时零系统调用）。**实测**：拖动挂件时面板全程跟随，右缘与 6px 间隙保持不变。
- **开关与联动**：☰ → `widget_menu_toggle`（已显示则隐藏）；面板的「取消」/「确定」/Esc → `widget_menu_close`；两者都向挂件发 `widget:menu-state`（挂件据此同步 ☰ 的 `aria-expanded`，并在收起时重读 `usageBaselines` → 状态行从「设置今日充值金额」恢复为「今日已用 ¥X」）。**挂件收起时一并收起面板**，否则桌面上会留下一块无主面板。
  - 面板窗口同样 `prevent_close() + hide()` 常驻复用（避免 `close()` 异步销毁期句柄仍在的竞态）；再次打开时 Rust 重算位置并发 `widget:menu-show`，让面板重读站点数据（挂件可能已切站点）。
  - ⚠️ **窗口复用 + 前端热更新**：面板页面也**从不重载**，改 `WidgetMenuApp.tsx`/`widget-menu.css` 后同样要重启 dev（与挂件同一条坑，见 §0）。
- **实测（2026-09/16：Rust 日志 + 探针量测 + 真实鼠标点击 + 截图四方对照）**：挂件 `(80.0,488.8) 250.4×250.4` 时 → 面板 `[menu] opened at (106.4,350.8) size 224x132`：右缘 106.4+224 = **330.4 = 挂件右缘** ✓、底边 350.8+132 = **482.8 = 挂件顶边 488.8 − 6** ✓。内容高度回报触发两次 resize（118 → 151），两次都保持**右缘 330.4 / 底边 482.8 不动** ✓。面板窗口内量测：`.widget-menu-panel` 204×130.9、输入框与两按钮全部 `inViewport=true`、无换行溢出（scrollW == clientW）✓。**真实鼠标**点击 ☰：打开（`[menu] reused at (106.4,331.6)`）→ 再点关闭（`[menu] hidden`）→ 证明**命中保护在缩小到 40×72 后仍然有效** ✓（这条必须用真实输入验：JS `el.click()` 会绕过系统级命中判定）。

#### 挂件滚轮缩放实现说明（2026-09 鼠标焦点版 → 2026-09/11 实测标定版）
- **交互**：滚轮向下放大 / 向上缩小 ≈10px/格（`deltaY×0.1`，单事件限幅 ±50 防平滑滚动突变，deltaMode 行模式 ×16 兜底）；**缩放焦点 = 鼠标所在位置**（鼠标指向的内容点在屏幕上不动，窗口随焦点平移）；1:1 正方形（对齐边缘缩放规则）；范围 180~800；拖动/边缘缩放进行中不响应（`resizeRef`/`pressRef` 判定 + 500ms 自愈兜底清除卡死状态）；停止滚动 300ms 防抖 `widget_save_size` 持久化
- **架构三层**：
  1. **锚点数学在 Rust**（`widget.rs widget_set_bounds(mouse_x, mouse_y, target_w, target_h, first)`）：手势首帧（first=true）从系统真实 `outer_position/outer_size` 缓存锚点 (P0, S0)，之后每步 `k = 请求边长/S0`、`new_x = P0 + mouse_x*(1-k)` → 放大再缩回原位精确归零，数学上无累积漂移。
     - ⚠️ **2026-09/11 修正：`k` 必须用「前端请求值」而不是「落地取整值」**——前端内容层 scale 由请求值算出，Rust 若用落地值算 k，两者差最多 0.4 CSS px → 锚点系统性偏移 `m·Δ/S0`（≈0.16 物理像素）。物理像素取整交给 `SetWindowPos`，与位置数学无关。
     - ⚠️ `cur_w`/`anchor.size` 取整到 **CSS 整数像素**（与 `window.innerWidth` 同口径），否则与内容层所用 `k0 = innerWidth/308` 差最多 0.5 CSS px。
     - 位置数学仍全部在 Rust（不读前端位置值）：早期「向右下漂移」源于前端自维护位置。**注：2026-09/11 复测 `window.screenX/screenY` 在本项目环境下读数是准确的**（204/380 与配置中的 `showWidgetPos` 一致），旧文档"差 1000px"的说法未能在当前环境复现——该结论保留但存疑。
  2. **内容层 = 固定 308×308 画布 + CSS `transform: scale(窗口/308)`**（`widget.css .widget-canvas`，`transform-origin: 0 0`）：浏览器不重排内容（vw 字号 / cover 图每帧重排滞后 = 「内容抖」的根源），整张画布由 GPU 合成器缩放；画布原点(0,0)对齐窗口左上角，配合 Rust 锚点数学鼠标点不动；返回按钮在画布外不随缩放。`will-change: transform` 保证常驻合成层。
  3. **窗口平滑动画 = rAF 逐帧泵**（`WidgetApp.tsx wheelFrame`，2026-09/11 由 Promise 链改为 rAF 帧驱动）：每帧只走一步 → 窗口尺寸变化规整（旧版步长 3/6/10px 参差、快速滚动时单帧窗口位移可达 9 物理像素）；**每帧先把内容层 scale 提交到本步目标，再发窗口几何（同一任务内）**，让两者落进同一个合成帧；步长基准用**前端自持的浮点游标 `wheelIdealSize`**（不回读落地取整值——回读会把 1.25px 步长向上顶到 2px，抖动同比放大）。
- **⚠️ 残余抖动的真实根因（2026-09/11 屏幕捕获实测标定，取代旧文档的"亚像素错位"猜测）**：
  - **现象**：锚点标记（画在鼠标所指画布坐标上的黑方块）在屏幕上的位置，在**每次窗口尺寸变化的那一帧**会朝窗口移动方向偏一下再回位（稳定帧完全不偏）。
  - **量化规律**：**偏移量 ≈ 0.4 × 单帧窗口位移**，即"这一帧窗口走了多远，锚点就被拖回多远"。实测：单帧位移 1px→低于量测噪声底；3px→摆约 2 物理像素；9px→摆 5 物理像素。**与单帧位移成正比，与步数无关。**
  - **机制**：窗口几何由系统在收到 `SetWindowPos` 后**立即**应用（DWM 合成），而内容层 `transform` 要等渲染进程下一次提交才上屏。这一帧里窗口已移到新位置、内容还是上一步的缩放 → 鼠标下的锚点被拖回一步。**这是平台级两条合成路径的帧错位**：实测把内容层写入与窗口几何放进同一任务**无改善**（n=3 对照 0.99 vs 1.04），把内容层改为窗口落地后再写也无改善——**但反过来让窗口几何晚一帧发布就能对上（关键解法，见下）**。
  - **解法（两步，2026-09/11 实测得出）**：
    1. **让窗口几何比内容层晚一帧发布**（`GEOM_DELAY_FRAMES=1`）——**这是关键**。既然上屏的内容总是落后窗口几何一帧，那就反过来让几何晚一帧发布，使两者在同一个呈现帧里取自**同一个步** → 错位从根上消失。此后不必再靠"压小单帧位移"换平滑。
    2. **单帧位移上限作为「速度 vs 抖动」旋钮**：`MAX_STEP=6`、`STEP_FRAMES=4`（单步 = |diff|/4 并钳在 [0.5, 6]）。②③④是配套：步长基准用**前端浮点游标**（回读落地取整值会把 1.25px 步长顶到 2px，抖动同比放大）、每帧内容层与几何同一 rAF 任务内推进、`resizeHandler` 在泵运行期间完全不插手。
  - **实测结果**（屏幕捕获 n 次、亚像素质心、单位=物理像素；静止帧噪声底 0.02~0.07 为口径可信度指标）：
    | 场景 | ① 旧策略（step 10/6/3） | ② 仅压小步长（step≤2，无延后） | ③ 几何延后 + step≤6（**最终**） |
    |---|---|---|---|
    | 标准（dY=100×6 → +60 逻辑px）抖动均值 | 0.98 | 0.64 | **0.286**（n=4） |
    | 快速（dY=500×2 → +100 逻辑px）抖动均值 | **3.94** | 0.87 | **0.285**（n=3） |
    | 快速 抖动峰值 | 5.0 | 1.0 | **0.53** |
    | 快速 动画时长 | ~300ms | 932ms | **399ms** |
    最终版把抖动压到 **0.29 物理像素**（≈0.23 逻辑像素，噪声底 0.03~0.07 的 4~10 倍，已接近量测极限），
    且**动画比①②都快或相当**——既快又不抖，不再需要二选一。
  - **副作用（已知未消除）**：延后期间内容层比窗口略大（最多一个步长 ≈6px）→ 内容在窗口右/下边缘被裁掉一条。
    用户看到的**可见框仍由窗口驱动、平滑增长**，被裁的是图像边角；实测该幅度下不易察觉。
    若要彻底消除：需让 Rust 把「锚点数学所用尺寸」与「窗口实际尺寸」分开（窗口留 margin、内容不越界），
    并把返回按钮改为相对内容定位（当前定位在窗口右上角，会被 margin 推偏）。
  - **⚠️ 连带 bug（2026-09/11 发现并修复）**：`resizeHandler` 里「距上次滚轮 >400ms 就清空 `wheelPendingSize`」这个阈值是给**旧快泵**（100ms 内跑完）定的；单帧位移钳小后动画要跑 600~900ms，于是**动画在最后一次滚轮 400ms 后被腰斩**——实测快速场景目标只走到 29%（+29 逻辑 px / 应有 +100），且**表现为"滚动不够灵敏"而非报错**，极易被误判成手感问题。修法：`resizeHandler` 开头 `if (wheelPumping) return`（泵运行中既不动内容层也不清手势状态）。**教训：凡是"动画/异步过程还没结束"的兜底阈值，都必须以"过程是否仍在进行"为准绳，而不是"距上次输入多久"。**
- **⚠️ 踩坑教训（按发现顺序）**：
  - **漂移**：前端自维护窗口位置 → 锚点基准错 → 向右下漂移。根治 = 位置数学全交给 Rust，从系统真实状态出发
  - **双监听**：HMR / StrictMode 多次挂载 useEffect，旧 listener 残留 + 新叠加 → 同一滚轮被处理两次、锚点反复重置 → 忽大忽小颤。根治 = wheel/resize 监听器与手势状态提升**模块级单例**（`widgetRefs` 注入 + `ensureWheelListener` 先卸旧再挂新）；**跨 HMR 幂等必须把 handler 引用存 `window.__dshWidgetWheel`**（模块内函数引用在 HMR 后是新函数，removeEventListener 找不到旧函数，旧监听仍残留）
  - **IPC 风暴**：rAF 循环里用滞后的 `window.innerWidth` 判断「是否追平」→ 永远追不平 → 无限重发同 target。根治 = 用前端游标判断追平
  - **乐观/落地回退震荡**：乐观累计推进 + 落地回退互相拉扯 → 位置来回摆。根治 = 游标单调推进不回落，落地值只作确认
  - **诊断日志阻塞**：Rust 命令内 `eprintln!` 每次 IPC 同步写终端 → 拖慢命令执行、放大抖动。诊断完必须移除
  - **窗口复用不重载页面**：挂件 hide/show 复用窗口，页面 JS 从不重载 → HMR 后旧代码仍在跑（测试假象）。改动前端后必须**重启 dev** 或确认日志出现新诊断标志
  - **两步窗口操作**：`set_position` + `set_size` 分开调用中间有「新位置+旧尺寸」渲染帧；单次 `SetWindowPos`（windows-sys `Win32_UI_WindowsAndMessaging`，hwnd 转 isize 取 `.0`）原子设置位置+尺寸，WebView2 只收到一次尺寸变化
- **📐 量测夹具**：抖动与清晰度的客观量测工具（`probe/`，仅 dev）**已独立成节，见 §4.10**——
  本节上面所有"实测"数字都出自它；改挂件渲染/交互前，先用它量一遍再动手。

### 4.7 计费方案与筛选（2026-08 完成，替代"仅 UI 半成品"）
- `lib/plans.ts`：四方案（按量余额 paygo / 包时 duration / 按次 times / Plan 套餐 plan）；站点可多选 `billingPlans` + 主显示方案 `primaryPlan`（缺数据兜底按量余额）
- **方案筛选已接线**：`App.tsx visibleEntries` 按搜索词 + 方案 AND 过滤（站点任一支持方案命中即显示）
- **多方案卡片**：`SiteCard` 主卡展示主方案数据，底部收纳条展开次级卡片（每方案一张），次级卡可「⭐ 设为主卡」即时切换持久化 `primaryPlan`
- `SiteDialog` 计费方式多选（勾选主方案自动回退最高优先级）；`PlanFilterDialog` 全选/清空 + 各方案站点计数
- Bento 概览第三扩展：可用总额（分币种合计）+ 剩余包时 + 剩余次数（`plans` 分方案数据汇总）

### 4.8 主题与壁纸（设置页，2026-08 完成；2026-09/15 改版排版）
- `SettingsDialog` 结构（**2026-09/15 按用户要求改版**；**2026-09/16 行间加立体分割线**、**同日把启动开关改成下拉框**）：反馈 Q 群（置顶）→ **「主题」三按钮横向 + 「背景壁纸」下拉同一行**（主题在左、壁纸在右，`.settings-row` flex）→ **「启动时自动打开桌宠模式」下拉框三选一**（`.switch-row`：左标签 + 右下拉，选项 = **关闭自动 / 上次站点 / 置顶站点**）→ 底部 **「高级设置」折叠区**（**「全局默认自动刷新间隔」已收纳进去**，默认收起）→ 关闭/保存
  - **行间「立体」分割线**（`.settings-divider`，2026-09/16 用户要求「设置中每一行加一条」）：共 **3 条** —— 反馈行之后、背景壁纸说明之后、滑动开关之后（高级设置之前）。实现 = **`border-top` 实线 + `box-shadow: 0 1px 0` 淡线**，布局只占 1px、视觉 2~3px。
    ⚠️ **两个主题的做法不同，因为两个主题的弹窗底色不同**：浅色弹窗底色是**纯白**（`--dialog-bg: #ffffff`）→ 下方那条**不能用"更亮的白线"**（实测截图里那条白线完全不可见：暗线行 229、相邻行 255），改成"更淡的暗色"；深色弹窗底色是 `#1c1d24` → 下方那条用"淡亮线"才是经典浮雕。故两色都由主题变量给（`--divider-line` / `--divider-highlight`）。
    实测像素（截图取样，非肉眼）：浅色 **219 → 245 → 252 → 255**（刻线 + 下缘淡影）；深色 底色 **28,29,36** → 暗线 **15,16,20** → 亮线 **41,42,49**（比底色亮，看得见）。
    ⚠️ `.advanced-section` 原先自带的 `border-top` 已删除，否则会和分割线叠成三条。
  - **「设置标题」字体统一**（2026-09/16 用户要求「标题如：主题、背景壁纸，字体统一为高级设置那种」）：字号/颜色本来就一致（`clamp(9.5px,2.91vw,12px)` + `--text-secondary`），**差的只有字重** → 在 `.settings-dialog` 作用域下把 `.field-label` / `.switch-label` 统一成 `font-weight: 600`（与 `.advanced-toggle` 相同）。
    ⚠️ **必须用 `.settings-dialog` 作用域，别直接改 `.field-label`** —— 它在「添加站点」等弹窗也在用。设置页根节点因此加了 `settings-dialog` 类（顺带成为以后设置页专用样式的挂钩）。
    ⚠️ 该设置页专属作用域类也要记得：`.settings-dialog .field-label` 连「高级设置」折叠区里的标签一起覆盖（展开后同样 600）。
  - **壁纸行下方的提示语**：**2026-09/16 用户要求删掉了「选择主界面背景样式，立即生效」**（非自定义壁纸时**不再有任何提示行**）；**仅**「自定义壁纸」时保留一句 `当前使用自定义壁纸，可点「选择图片」替换`（它说明了怎么换图，且与上方「🖼 选择图片」按钮配套）。
  - 主题三按钮：`.theme-group` + `.theme-opt`（选中态加 `.active`，用 `--accent` 描边），标签「浅色 / 深色 / 跟随系统」；壁纸下拉改用**短标签**（🌌 极光 / ⬜ 纯色 / 🖼 自定义）以便与按钮组同行不挤
  - `watchSystemTheme` 订阅系统深浅色；壁纸由 `data-wallpaper` + CSS 变量 `--custom-wallpaper-url` 控制；旧 `anime` 配置自动映射为 `aurora`
  - **「启动时自动打开桌宠模式」下拉框**（2026-09/16 用户口径：文案改为「启动时自动打开桌宠模式」，右侧控件由滑动开关换成**下拉框三选一**）：
    - 三态：**关闭自动**（不打开）/ **上次站点**（打开 `config.lastWidgetUid` 记的那个，= 原开关打开时的行为）/ **置顶站点**（打开**站点列表最上面那张卡**，即用户拖拽排序后的第一张）。
    - 存储：`config.autoOpenWidgetMode`（`"off"` / `"last"` / `"pinned"`）；⚠️ 老字段 `autoOpenWidget`（布尔）保留作兼容读取与同步回写（保存时 `mode != "off" ⇔ true`），判定一律走 `lib/autoWidget.ts`。
    - 判定与站点选择 = 纯函数（`autoWidgetMode()` / `pickAutoOpenUid()`，**12 项单测**）：老的布尔配置自动归一到 `"last"`/`"off"`；`last` 的记录被删/停用 → 回退第一个启用站点；`pinned` 的首张卡被停用 → 跳过（同一回退链：宁可开一个能用的，也不要"启动没反应"）。仅桌面端、启动 1200ms 后触发（effect 依赖仍是 `[]` + ref，见下方踩坑）。
    - ⚠️ **下拉面板右对齐的坑（2026-09/16 实测并修掉）**：`.menu-select-pop` 是 `right:0` 定位，对齐的是**根元素**右缘而**不是按钮**右缘。给根元素加 `min-width`（第一版加了 106px）后，根元素比按钮宽（按钮按内容定宽 90.4）→ 展开的面板右缘凸出按钮、左缘与按钮齐平，**看起来像"左对齐"**（用户就是这么报的）。修法：**根元素不要定宽**（三个选项都是 4 个汉字，按钮宽度天然稳定）。实测修后：**按钮右缘 392.8 == 面板右缘 392.8，差 0.0px** ✓。
    - **文案沿革**（2026-09/16 用户第四次改）：最初叫「滑动效果」（名实不符）→「自动打开鲸鱼娘窗口」→「启动时自动打开鲸鱼娘窗口(上次选择)」（把语义写进标签）→ 现在叫「**启动时自动打开桌宠模式**」（**站点语义改由右侧下拉框承担**，标签只说明"自动打开桌宠"）。实测标签宽 **120px、单行不换行**（h=15.2），与右侧下拉框不重叠（`.switch-row` 是 space-between）。
    - 实测两态滑块位置（探针元素量测）：关闭态拇指相对轨道左 **2.8px**、开启态 **22.4px**，右缘余量 1.6px（两态都在轨道内）。轨道 40×22、拇指 16×16。
    - ⚠️ 命名历史：该开关最初按用户口述叫「滑动效果」（名实不符，已在 §8 记录提醒）；用户随后更正为「自动打开鲸鱼娘窗口」，代码注释/文档已全部同步。
  - ⚠️ **该 effect 的依赖必须是 `[]` + 从 `cfgRef` 读配置**（实测踩坑）：写成依赖 `[cfg]` 时，挂件还没打开 `cfg` 就被后续动作改掉（写入当日基准/探测缓存 → `setCfg`），React 在 cleanup 里 `clearTimeout` 取消定时器 → **挂件永远打不开**，且表现为「开关没反应」而非报错。诊断标记 `#__aqm_autoopen`（仅 dev，probe `measure` 可读）定位到此问题
- **自定义壁纸替换修复（2026-08）**：`save_wallpaper` 固定存 `wallpapers/custom.png`，替换时路径不变 → WebView2 缓存旧图「替换不生效」；修复 = CSS 变量 URL 追加时间戳 `?t=${Date.now()}` 强制刷新
- **「选择图片」替换按钮**：当前壁纸为 custom 时，在主题/壁纸行**下方**显示「🖼 选择图片」按钮（`.wallpaper-row`），点击打开文件选择器重新选图
- 自制 `MenuSelect` 下拉（替代原生 select：Windows 原生面板深色模式看不清）

### 4.9 同步（**只走文件**：导入选 .json；导出 = Windows 放进剪贴板 / 安卓分享或保存；+ 口令加密）
- **2026-09/16 改版（用户口径：删掉文本方案，只保留文件导入导出）**：
  - 面板只剩「配置文件」一段（原「方案 A · 剪贴板」的**粘贴文本框 + 从剪贴板导入按钮已删除**），按钮一行：**`[⤓ 导入配置文件]` 靠左、`[⤒ 导出配置文件]` 靠右**（`.sync-actions.split` = `space-between`；默认 `.sync-actions` 是右对齐，给二维码段留着），两个都用**次级样式**（`btn secondary`，**导出不再是蓝色** —— 2026-09/16 用户要求）。图标 14×14/描边 2.2（与 `SiteActionsDialog` 同约定）：**导入 = `Download`、导出 = `Upload`，主界面「导出/导入」按钮也同步换成 `Upload`**。上方是「加密导出」滑动开关（**默认开启**）。
  - **导入**：`<input type="file" accept=".json,.txt">` 选文件 → `file.text()` → 走原有 `applyImportText`（加密文件先弹口令行，再 `确认导入`）。
  - **导出**：`buildExportFileText` 生成文件内容 → Rust 命令 `export_config_file(text, fileName, mode?)`（**`mode` 仅安卓用**：`share`/`save`；Windows 忽略它）→ **Windows** 写到 `%APPDATA%\com.apibalance.monitor[.dev]\export\` 后**把该文件本身放进剪贴板**（`CF_HDROP`，`src-tauri/src/export.rs`）→ 用户到资源管理器 / 微信 / QQ 里 `Ctrl+V` 粘出来的就是 `.json` 文件；**安卓**见下方平台分流。**不做「另存为」对话框**（Windows 口径），因此**未引入** `tauri-plugin-dialog`/`fs`。
  - 文件内容（`buildExportFileText`，与二维码/剪贴板用的 `buildExportText` 分开）：**未加密 = 标准 JSON**（`schemaVersion=1`，真 `.json`、可读可手改，导入端本来就能解析）；**加密 = 该 JSON 的 `AQM2:` 密文**。二者都只走已有的解析路径，导入端**零新增格式**。
  - 文件名 `API余额通-配置_yyMMdd.HHmm.json`（`exportFileName`，与打包产物同风格）；前端只给"建议名"，Rust 侧 `sanitize_file_name` 再清洗一遍（**防目录穿越**：`/ \ : * ? " < > |` 与控制字符→`_`、去前导 `.`、强制 `.json` 结尾）。
  - ⚠️ **剪贴板引用的是磁盘上那份文件**，所以 `export/` 里的文件必须留着（用户粘贴时系统才读得到），不要当垃圾清理。
  - ⚠️ **平台分流（2026-09/16 用户口径「AB 结合」后已两端齐备）**：
    - **Windows**：`CF_HDROP` 把文件放进剪贴板（上面那一套）。
    - **安卓**：点导出后**二选一** —— **系统分享**（`ACTION_SEND` + FileProvider，发到微信/QQ/网盘/邮件）或**保存到本地**（`ACTION_CREATE_DOCUMENT`，自己选位置）。实现 = Kotlin 插件 `gen/android/app/src/main/java/com/apibalance/monitor/ConfigExportPlugin.kt`（`@TauriPlugin`，两个 `@Command`：`share` / `save`，保存结果用 `@ActivityCallback` 回调），Rust 侧由 `export::plugin()` 注册、经 `ConfigExport<Wry>` 句柄用 `run_mobile_plugin` 调用，**照 `web_login.rs`/`WebLoginPlugin.kt` 的既有桥接模式**。
    - **零权限**：分享走 FileProvider 临时授权、保存走 SAF（`ACTION_CREATE_DOCUMENT`）→ **不申请任何存储权限**，也不受分区存储限制。
    - ⚠️ **不需要改 AndroidManifest.xml**：Tauri 安卓模板**自带** `androidx.core.content.FileProvider`（authority `${applicationId}.fileprovider`）与 `res/xml/file_paths.xml`（已声明 `cache-path`/`external-path`）。**因此分享的文件只能放 `cacheDir`（或外部目录）** —— 别改成写 app 私有 dataDir，那种路径 FileProvider 不认（会抛 `IllegalArgumentException: Failed to find configured root`）。
    - ⚠️ 命令必须是 `async`：`run_mobile_plugin` 会**阻塞当前线程**直到 Kotlin 回调 resolve，而 Kotlin 的 UI 操作要跑在主线程 → 同步命令占住主线程会**死锁**（`browser_login_query` 同样是 async，原因相同）。
    - ⚠️ **运行时未验证**：Kotlin 侧行为（分享面板是否列出目标 App、保存后文件是否真的落地）**只能在模拟器/真机上实测**，静态编译查不出来。本轮已完成：安卓目标 `cargo check`（Rust 侧）+ Gradle 构建（Kotlin 编译）；**设备端实测待做**。
- **加密格式 `AQM2:`（2026-09/16）**：`AQM2:<base64( ver(1)\|iter(4,BE)\|salt(16)\|iv(12)\|ciphertext )>`
  - **KDF 参数写进密文自身**，迭代 **10 万 → 60 万**（导出物含 API Key 甚至阿里云/火山 AccessKey Secret，属长期留存载体，提高离线爆破成本）。
  - 旧格式 `AQM1:<base64( salt | iv | ciphertext )>`（固定 10 万次、无头部）**仍可解密**，只是不再产出 → 旧版导出的剪贴板文本/二维码图片照旧能导入。
  - **为什么必须升版**：旧格式把迭代次数硬编码在代码里，**一旦调整就永久解不开历史导出物**（不是"将来再说"，是没有补救机会）。
  - 回归护栏是一条**由升级前实现产出的真实旧密文**（`test/crypto.test.ts` 里的 `LEGACY_AQM1` 常量，固定 salt/iv/口令，注释写明生成方式），另加篡改 1 bit 必失败、未知版本号明确报错、截断报"已损坏"三项。
  - 代价：PBKDF2 60 万次在低端安卓机上约 1s 量级（导出/导入各一次），可接受。
  - ⚠️ **开关打开而口令为空 → 抛错，绝不静默回退成明文**（口径在纯函数 `buildExportFileText` / `buildExportText`，均有单测）。实测界面：默认态直接点导出得到「导出失败：口令不能为空」。
- **旧格式（导入端仍支持，但不再产出）**：标准 JSON（schemaVersion=1）+ 口令加密（`AQM1:` 前缀，PBKDF2 10w 次 + AES-GCM）；**2026-08/31 优化**：导出改用**紧凑 JSON + deflate 压缩的 `Z1:` 文本**（`buildClipboardPayload`，与二维码同格式），实测 4 站 3143→551 字符（-82%，聊天软件可发送），加密导出同样先压缩；导入 `parseAnyImportPayload` 自动识别 Z1 解压，新旧格式（标准/紧凑/压缩/加密）全部兼容 —— **2026-09/16 起这几条只剩"旧导出物仍能导入"的意义**（剪贴板文本导出本身已删除）。
- ⚠️ **方案 B（二维码）暂时禁用（2026-09/16 用户口径）**：`SyncDialog.tsx` 顶部常量 `ENABLE_QR_SECTION = false` 把整段二维码入口从界面收起，**实现、状态与 handler 全部保留**，改回 `true` 即恢复（块内缩进刻意不动，恢复/再禁用都是两行以内 diff）。实测收起后弹窗只剩方案 A：`.sync-section` 1 个、`.qr-box`/`.qr-img` 为 0、弹窗高 611.5 → **444**、仍在视口内（底 653.2 < 862）。⚠️ 禁用期 `qrcode`/`jsqr` 仍在打包产物里（未做摇树），且 `test/sync.test.ts` 的压缩/二进制路径测试继续跑（保护 re-enable 时不回归）。
- **方案 B 二维码**：紧凑 JSON（schemaVersion=2，短字段名 + 剔除 `_` 缓存/`balance_choice`）→ deflate 压缩 → **二进制 byte 模式直编**（魔数 `Z1`，jsQR 取 binaryData）→ 低纠错 L 级；导入自动识别标准/紧凑/压缩三种格式
- 加密二维码：与方案 A **共用 `buildExportText`**（同一格式同一压缩 —— 两路产物互为同构，手写的 base64 拼装已删除）；导入从图片识别（本地降采样到 1024 再 jsQR）
- **容量**（实测，仅对**仍禁用的二维码**有意义）：每站紧凑文本 ≈203 字符 / deflate ≈103 字节；二维码 v40-L 上限 2953 字节 ≈ 约 28 站。⚠️ **2026-09/16 起这条不再是迁移瓶颈**：导出物已是**文件**（Windows 复制文件 / 安卓分享或保存），**不受字符数限制**；旧的「超 10 站建议剪贴板文本、30+ 站建议文件导入导出」已全部作废（"文件导入导出"当年那个待办也已实现并改动多轮，见 §8 与 §11 第十五~十七轮）
- 合并规则：同 uid 覆盖（导入优先），新 uid 追加

### 4.10 开发期量测夹具（`probe/`，2026-09/11 新建，仅 dev）

**为什么存在**：挂件的「抖 / 糊 / 手感不对」是纯主观描述，肉眼反复试错在本项目已经失败过多次
（有一轮改进方案甚至被错误地"验证"为有效、又被错误地"证否"、最后回滚）。这套夹具的作用是
**把主观观感变成可比较的数字**，并且——同等重要——**让量测口径本身可被质疑和验证**（见下方"踩过的坑"）。

**组成**

| 文件 | 作用 |
|---|---|
| `zoom-probe.ts` | 由 vite serve 注入页面的探针（挂件与主窗口都会加载，各自分流） |
| `vite-plugin-aqm.ts` | 中间件：`GET /__aqm/trigger`（读 `trigger.json`）+ `POST /__aqm/trace`（落盘到 `probe/traces/`） |
| `capture.ps1` | DPI 感知屏幕捕获（GDI `CopyFromScreen`，约 60fps），逐帧算指标 |
| `run-test.ps1` | 一键一轮：先开捕获 → 写 `trigger.json` 触发探针 → 等落盘 |
| `trigger.json` | 触发计划（**run 号变化即执行一轮**；支持 `layout` / `measure` / `setSize` / `setWillChange` / `realLayout` / `control` 等开关） |
| `bg-sample.mjs` | **零依赖**背景图取样（zlib 解 PNG + 五种滤波器）：输出画布不透明图 / 区间亮度，用于给元素选落点 |
| `traces/` | 采样结果与 `README.md`（有效样本、判定口径、已删除的废样本原因） |

**探针能力**
- **抖动量测**：把挂件内容临时换成**品红底 + 锚点黑方块**（黑方块画在滚轮锚点对应的画布坐标上，
  理想情况下整段手势它在屏幕上**纹丝不动**），合成真实 `WheelEvent` 驱动缩放，逐帧采样
  `innerWidth` / 画布 transform / resize 计数；配合 `capture.ps1` 从屏幕像素回读锚点真实位置。
- **布局报告**：`{"layout":true}` → 回传画布内各元素几何（换算回 308 画布坐标）+ 背景图对应位置亮度采样
  + Rust 侧命中判定（`widget_hit_state`）。**没有图像输入能力时，用像素数据代替眼睛核对 UI 落点**。
- **元素量测（2026-09/15 新增）**：`{"measure":[".theme-opt",".wmp-input",…]}` →
  回传这些选择器的 **viewport 坐标、尺寸、`scrollWidth/clientWidth`（判断是否被挤压折行/截断）、
  是否在视口内、文字/当前值**。用于核对**非挂件也能用**的界面排版与控件状态——设置页改版、
  挂件菜单面板、进度条位置都是靠它验收的，不靠肉眼。
  - ⚠️ **两个窗口都会响应**（2026-09/16 补：此前只接在主窗口循环，挂件侧不响应，
    导致量测永远拿到主窗口那份，排查挂件内按钮时白等了好几轮）。取用时按 `viewport.w` 区分：
    挂件窗 < 400，主窗口约 438。
  - 取值细节：`input` 的当前值走 `.value`（`textContent` 读不到）；但 `<button>` 也有空 `value`
    会盖住按钮文字 → 探针已按标签区分（`INPUT/TEXTAREA/SELECT` 用 value，其余用 textContent）。
  - **回传字段全表**（2026-09/16 补齐后）：`text`（文字/当前值）、`x/y/w/h`（viewport 矩形）、
    `scrollW/clientW`（是否被挤压折行/截断）、`fontSize`、**`cls`**、**`fontWeight`**、**`color`**、
    `visible`、`inViewport`。
    - **`cls` / `fontWeight` / `color` 是"无几何差异"类改动的唯一硬指标**（2026-09/16 加）：
      颜色/字重/类名变了但矩形一模一样的改动，光看坐标永远看不出来。实战用途：核对
      "导出按钮不再是蓝色（`btn primary`→`secondary`）"、"主界面图标确实换成了 `lucide-upload`"、
      "设置标题字重统一成 600"。
    - ⚠️ `cls` 取的是 `getAttribute('class')` 而**不是** `el.className`：SVG 元素的 `className` 是
      `SVGAnimatedString` **对象**，直接 `toString()` 只会得到 `[object SVGAnimatedString]`，
      拿不到 `lucide-upload` 这类类名（本轮踩过）。
  - ⚠️ **量测样本的落盘文件名取决于回传体里有没有 `plan.run`**（见下方「踩过的坑」第 7 条）。
  - 配套：`App.tsx` 里**仅 dev** 的钩子 —— URL 带 `?settings=1` 打开设置页、`?sync=1` 打开导出/导入弹窗
    （`import.meta.env.DEV` 静态求值为 false，生产构建整段被摇树移除；已实测 `dist` 产物中
    不含 `__aqm_autoopen` / `settings=1` / `sync=1` 任何标记）。需要量测设置页时，
    临时给 `tauri.dev.conf.json` 的 main 窗口加 `"url": "index.html?settings=1"` 即可。
- **通用点击（2026-09/16 新增）**：`{"click":[".switch", …]}` → 探针按顺序处理每个选择器：
  **等元素出现（最多 2s）→ 点第一个匹配元素 → 等 260ms（CSS 过渡 0.18s）→ 处理下一个**，
  再执行同轮的 `measure`。回传 `clicked[].before`（点击前的 `aria-checked`/`class`）供前后对照。
  用途：验证控件的**两态**几何与"点一下是否真的切换了"，而不是只看静态布局；"等出现"这条让
  串联点击可行（**点 A 打开弹窗 → 再点弹窗里的 B**，本轮测加密开关就是这么做的）。
  **主窗口与挂件窗口都支持**（挂件侧 2026-09/16 补：此前只有主窗口接了 click，量挂件菜单面板时
  点不开——而面板"必须先点 ☰ 才存在"，只能退回下方那套合成鼠标事件的坐标换算；
  补齐后 `{"click":[".widget-menu"]}` 直接量到面板内控件）。
- **背景图取样（`bg-sample.mjs`）**：`node probe/bg-sample.mjs map 8` 输出整张画布的**不透明图**
  （每格平均 alpha，看角色轮廓在哪），`node probe/bg-sample.mjs 6 265 60 130 4` 输出指定区间的
  **不透明占比/亮度范围**。给挂件元素选落点时用，可避免把文字放进透明带或异色背景上
  （实测气泡白色内区约 y 30~133）。
- **真实交互验证（无探针内建支持时的做法）**：需要"点按钮/敲键盘"时用 PowerShell + `user32`
  （`SetCursorPos` + `mouse_event`；脚本必须带 BOM，见 §10）。物理坐标换算：
  `窗口矩形左/上 + CSS 坐标 × (窗口物理宽 / 308)`（挂件窗 308 CSS 宽）。⚠️ 先确认窗口矩形
  （`GetWindowRect`），不要用配置里的 `showWidgetPos` 推算——窗口可能已被调整过（本轮因此点空过）。
- **清晰度对照**：`{"control":true}` 在**同一窗口**注入一段原生尺寸的同款文字/图片作为对照
  （未经画布 transform 缩放），与画布内缩放内容同帧比对 —— 这是排除"是不是缩放导致的糊"的唯一干净办法。

**判定口径（重要）**
- 抖动：比较「**尺寸变化帧**」与「**静止帧**」的锚点偏差；后者即**量测噪声底**，两者接近即说明已到噪声底。
- 清晰度：用**同窗口原生对照**，或**笔画核心暗度 / 深墨像素占比**。
- **先看噪声底再信数字**：静止底明显偏高（如 0.13 或 8）说明该轮捕获受外部干扰（人为操作挂件、前台窗口争夺），该样本作废。

**踩过的坑（这些坑让量测一度给出完全错误的结论）**
1. **窗口漂移**：锚点缩放必然让窗口向左上漂移，早期夹具每轮不归位 → 几轮后挂件跑出捕获区域，
   量到的是**被裁剪**的方块（`mx0=0`）。→ 现在每轮先 `setSize` + 归位位置，分析脚本还强制校验裁剪。
2. **口径会骗人**：「边缘跃迁宽度」会随**字号**自然变大（文字带 8 向白描边，混合区随字号一起变宽），
   原生对照同样从 1.04px 涨到 1.63px → 它既掩盖真实差异、也制造过假差异，**据此误判过两次**。
3. **A/B 的状态必须对**：验证「某操作之后才出现的问题」必须在**该操作往返之后**测（S→其它→S，比首末两次）；
   在"尚未做该操作"的稳态下切开关，只会测出"无差别"，从而把一个方案错误地判为无效。
4. **探针绝不能默认注入主窗口**：为做清晰度对照，探针会往主窗口注入一张鲸鱼娘图——
   早期写成"页面加载即注入且移除不掉"，结果那张图一直挂在主界面上（用户直接质问过）。
   现在**只有显式 `{"control":true}` 才注入**，`false` 即移除。
5. **`.ps1` 必须 UTF-8 with BOM**：本机只有 PowerShell 5.1（无 pwsh 7），无 BOM 的中文注释会被按 GBK 解析、语法直接崩。
6. **`probe/` 已纳入类型检查**（`tsconfig.json` 的 `include` 为 `["src","probe"]`）——
   早期只有 `src`，探针代码里的低级错误（未定义变量）不会被 `tsc` 发现，踩过一次。
7. **量测样本的文件名取决于回传体里有没有 `plan.run`**（2026-09/16 踩，已修）：
   `vite-plugin-aqm.ts` 用 `data.plan.run ?? 0` 命名落盘文件，而**主窗口的量测回传体原本不含 `plan`**
   → 所有主窗口量测样本都被写成 `run-000-*.json`，**按 run 号去 `Get-ChildItem -Filter 'run-<N>-*'` 永远找不到**。
   本轮因此误判成"主窗口探针没响应 / 被 Chromium 节流"，连着用错口径排查了三轮（甚至去查窗口 Z 序和最小化状态）。
   现已修：两类窗口的量测回传体都带上 `plan:{run}`，文件名恢复带 run 号。
   → **附带事实**：窗口处于**最小化**状态时探针照常轮询与回传（本轮实测，Chromium 的节流没到"不响应"的程度）。
   **教训：量测"没数据"时先确认自己找对的文件名，再去怀疑被测量对象。**
8. **一次只能有一个"真相来源"**：本轮为了确认主窗口显示什么，先用 `GetWindowRect` + `CopyFromScreen` 截图，
   结果截到的是**盖在上面的浏览器窗口**（截图截的是屏幕像素，不是"那个窗口的画面"）。
   要判断某个窗口自身的内容，得先把窗口提到最前（`SetWindowPos(HWND_TOP, SWP_NOACTIVATE)`，
   不动几何以免污染 App 的尺寸/位置记忆），或干脆改用 DOM 侧的量测（本轮最终就是这样拿到数据的）。
   用完后把窗口恢复原状（本轮：截前后都是最小化）。

## 5. 功能清单（已完成）
- ✅ 8 适配器 + 自动识别增强（Sub2API / One-API 站点统一由通用自动识别接管；2026-08 新增 Code AI 与模拟浏览器登录）
- ✅ **Code AI 适配器**（2026-08）：OpenCode（Go 用量 API Key + Zen 余额 cookie）+ CommandCode（API Key）一个入口，下拉选服务商（见 §4.3）
- ✅ **「模拟浏览器登录」兜底适配器**（2026-08）：Windows Edge 无头自动登录抓余额 + **Android 内嵌 WebView 手动登录（无感刷新，登录态 CookieManager 持久化）**（见 §4.3）
- ✅ **登录 Token 缓存机制**（2026-08）：token 复用跳过重复登录 + 失效自愈 + 409 会话上限自动清理（见 §4.3）
- ✅ 卡片网格（拖拽排序：长按 250ms 拖动，自动保存）
- ✅ 多方案体系：计费方式多选 + 主卡/次级卡 + 设为主卡 + 方案筛选接线（见 §4.7）
- ✅ 概览 Bento 卡：可用总额（分币种）+ 剩余包时 + 剩余次数 + 导出/导入
- ✅ 操作行两行（grid 2 列）：导出/导入+添加站点 ｜ 方案筛选+全部刷新（激活态高亮 + 计数）
- ✅ 同步：**只走文件**（2026-09/16）—— `[导入配置文件]`（选 `.json`/`.txt`）+ `[导出配置文件]`（写 `.json` 后**把文件本身放进剪贴板**，可到文件夹/微信/QQ 直接 `Ctrl+V`）+ 「加密导出」滑动开关（**默认开启**，`AQM2:` 迭代 60 万，`AQM1:` 旧密文仍可解）+ 按 uid 合并；⚠️ **文本粘贴方案已删除**；（**2026-08/31 起紧凑+deflate 压缩 `Z1:` 文本**的旧记录见 §4.9）；⚠️ **二维码导出/导入（方案 B）已于 2026-09/16 暂时禁用**（`ENABLE_QR_SECTION = false`，实现保留、改回即恢复）
- ✅ **主窗口关闭即退出**（2026-08/31）：挂件窗口常驻不再阻止退出（`CloseRequested` → 保存几何 + `exit(0)`）；退出时自动终止残留 node 子进程
- ✅ **新增站点置顶**（2026-08/31）：`upsert_provider` 新站点 `insert(0)`，卡片出现在列表最上面；编辑仍原位替换
- ✅ **OpenCode Go 卡片增强**（2026-08）：三窗口（5h/周/月）`\n` 多行详情 + 青蓝 Go 徽标 + 概览可用总额排除 `%` 配额 + `%` 与数字紧贴（`88.00%`）
- ✅ **设置页 Q 群反馈**（2026-08/31）：置顶「反馈可加入项目Q群哦：1109402164」+ 一键复制按钮
- ✅ 设置页（2026-08 完成，**2026-09/15 改版**、**2026-09/16 行间加立体分割线 + 启动开关改为下拉框三选一**）：主题改为**三按钮横向**、背景壁纸移入**同一行右侧**、全局默认自动刷新间隔**收纳进「高级设置」折叠区**、**「启动时自动打开桌宠模式」下拉框（关闭自动 / 上次站点 / 置顶站点）**、**每行之间一条立体分割线**；主题深浅色/跟随系统 + 壁纸（极光/纯色/自定义图片）（见 §4.8）
- ✅ 窗口几何记忆（2026-08 完成）：主窗口位置/大小退出记忆 + 挂件大小/位置记忆（见 §4.5/§4.6）
- ✅ DeepSeek 官方峰谷时段徽标（`lib/deepseekPeak.ts`：工作日 9-12/14-18 峰，周末全天谷）+ 卡片倒计时 → 下一时段；**挂件同步显示该徽标并加倒计时（站名行上方，2026-09/15；措辞如「谷 剩余13时29分」）**
- ✅ 站点对话框：动态表单 + 必填校验（字段级红框 inline 提示）+ 测试连接 + 候选余额校准（`balance_choice` 记忆）+ 高级设置折叠（代理/登录细节）
- ✅ emoji 状态全套（✅正常 ⚠️刷新中 ○未刷新 ❌失败）
- ✅ 零延迟悬浮提示（JS 动态 fixed tooltip，Android 禁用）；**提示物收敛**：仅「导出/导入」「方案筛选」「展示窗口」三个按钮保留悬浮文字（其余按钮去掉 tooltip，2026-08）
- ✅ 液态玻璃按钮（2026-08，配方取自 `D:\yzrt-liquidglass`）：`filter: contrast(3)` 压边 + `backdrop-filter` + inset 翻边高光 + `::after` 45° 扫光（hover 时光线滑过）；应用于 ⚙️设置按钮 / 卡片操作按钮（完整液态）、主按钮（accent 色保留 + 玻璃扫光）、次级按钮（完整液态）；深浅主题通用
- ✅ 自制 MenuSelect / ConfirmDialog / Toast（不用 window.confirm，Android 可靠 + 深色可控）
- ✅ Windows 单实例（防双开）；Android 条件编译跳过
- ✅ 代理功能（配置注入，2026-08 接通）
- ✅ 死代码清理（bodyB64 全量 base64 移除等性能优化）
- ✅ **空白处点击穿透**（2026-09/11）：挂件近一半透明区域不再吃鼠标，可点到底下的窗口；返回按钮特殊保护（见 §4.6）
- ✅ **挂件状态行显示「今日已用」（2026-09/15 上线；2026-09/16 晚换模型）**：挂件第三行显示**今日已用金额**（如 `今日已用 ¥0.41`），不请求任何额外接口。
  - **口径 = 消费只由「下降」构成**（2026-09/16 换模型，对齐开源实现 `MeteorNOX/DeepSeek-Balance-Whale-Widget`）：每次成功刷新都上报一次余额观测（`record_usage_baseline`），Rust 侧把**本次读数与上次读数比较**：
    | 本次相对上次 | 落到哪个字段 | 含义 |
    |---|---|---|
    | **下降** | `debit += 差额` | **今日已用**（唯一来源） |
    | **上升** | `credit += 差额` | 充值 / 赠金 / 纠错，**只记录、不进消费** |
    | 不变 | — | 不记账 |
  - 于是**充值在结构上不可能污染这个数字**：它恒等于"当日所有下降步之和"，单调不减、恒 ≥ 0，**不再需要用户登记任何充值金额**，也不会再出现"提示把数字顶掉"。实测（改配置构造）：「人为把 `last` 调低 10 → 刷新」→ `debit` **原样不变**、`credit` = 10.0 ✓；真实消耗时精确累加（`48.18 → 47.77` = `debit 0.41`）✓；挂件 `render-state` 同步显示 `今日已用 ¥ 0.41` ✓。
  - ⚠️ **旧模型（已删除，别改回去）**：`今日已用 = max(基准, 水位 peak, 当日最高余额 maxSeen) + 已登记充值 − 当前余额` + 「余额创新高 → 状态行改显 `设置今日充值金额`」的登记流程。它把充值当**修正项**加进公式，而登记时水位又前移到"已含这笔充值的余额" → **充值被算两次**，显示值恒多出整整一个充值额（实测：登记 ¥70 后界面恒显示 `真实消耗 + 70`，而真实消耗只有 ¥1.10）；判据下界同时被抬到 `peak + 充值` → 之后再充小额**永不提示**。2026-09/16 用户决定按参考实现换模型，`peak` / `maxSeen` / `recharged` / `set_recharge_amount` / `apply_recharge` / 那套提示与 `.widget-status.recharge` **全部删除**（对应单测一并删除）。
  - **记录结构**（`config.json` 的 `usageBaselines`，按站点 uid）：`{opening, currency, day, last, lastAt, debit, credit}`；老配置的 `value`（当日首次余额）经 serde alias 读进 `opening`，`debit`/`credit` 缺省 0 —— 因此**换模型当天数字从 ¥0.00 重新累计**（旧记录里的"水位/已登记充值"没有历史下降步可继承）。
  - **三条工程保障**（都在 `config.rs::observe` 这个纯函数里，各有单测钉住）：
    ①**丢弃重复/迟到样本**（`at <= lastAt`）——挂件 60s 与主界面各自上报、会乱序到达，照收会凭空造出一段"下降"或"上升"；
    ②**跨天 / 币种变化一律重建区间**——币种切换的数值跳变不是消费（参考实现踩过：币种随机切换曾把每次跳变记成一笔消费、单日虚记数千元）；
    ③**定点累加**（8 位小数整数单位相加）——避免误差随刷新次数累积（单测：300 次 0.01 的下降必须精确等于 3.00）。
  - ⚠️ **口径边界（如实的固有代价，别当账单数据）**：①**起点之前的消费不在本区间内** —— 区间 = 当天第一次成功查询的那一刻起；②**充值"与"消费落在同一次刷新间隔内**时净结果是一次上升 → 那部分消费观测不到（余额接口只给快照、没有流水，参考实现同样承认这条）；③跨天后要等当天首次成功刷新才有数字（此前回退为状态文案）；④非金额币种（`%` 配额、按次/包时）不显示；⑤站点删除时记录一并清除（`remove_provider`）。
  - 上报点只有两处：`App.tsx` 的 `commitResult`（所有成功结果的必经处）与挂件自己的 `refresh`；两边共用同一份记录，口径一致。
  - ⚠️ **回退逻辑**：无当日区间 / 跨天 / 币种不一致 / `%` 站点 / 当前无值 → 回退为原有状态文案（`刷新中…`、红色错误原因、`数据正常`）。**状态类名必须是单一值**（`used`），不能与 `statusKey` 并列——否则 `.widget-status.err` 的深红（CSS 规则在后）会盖掉今日已用。
    - ⚠️ **命中保护**：两颗右上角按钮（返回 / 菜单）都落在背景图透明区，不特判会被**点击穿透**（实测：菜单键点了没反应、面板打不开）。故 Rust 侧把两键所在矩形**（40×72 CSS px）**统一算实体（`in_fixed_overlay`，`widget_hit_state` 的 `backButton` 字段更名 `fixedOverlay`）。⚠️ 该矩形 2026-09/16 由 230×176 **缩小**到 40×72——面板已移出挂件窗口（独立窗口），不再需要为它预留不可穿透区。
  - 实测验收（探针布局报告 `{"layout":true}` + Rust `render-state` 日志）：挂件状态行 `今日已用 ¥ 0.41` 与记录里的 `opening 48.18 − last 47.77` 逐位吻合；`.widget-status` 宽 258.7（与站名行同宽）**未溢出**。`test/usage.test.ts`（12 项）覆盖上述边界。
- ✅ **DeepSeek 峰谷徽标**（挂件同步显示，站名行上方；2026-09/11）
- ✅ **鲸鱼娘挂件**（2026-08 完成，见 §4.6）：透明置顶小窗 + 真实余额 + 单击/60s 自动刷新 + 拖动/边缘缩放/**悬停滚轮缩放（鼠标焦点版 2026-09；2026-09/11 改为 rAF 逐帧泵 + 步长钳制，抖动经屏幕捕获实测标定）** + 大小/位置记忆

## 6. 开发命令

```bash
pnpm install            # 安装依赖（pnpm-workspace.yaml allowBuilds: esbuild）
pnpm dev:app            # 开发（vite 1420 + cargo watch；带 tauri.dev.conf.json 隔离 identifier，见 §11）
pnpm exec tsc --noEmit  # 类型检查（当前 0 错；include 已含 probe/，见 §4.10）
pnpm test               # 前端 vitest 116 项（全部通过；6 个文件，明细见 §2）
cd src-tauri; cargo test --lib   # 后端单测 14 项（BOM 容错 / 观测字段兼容 / 余额观测账本四项 / 导出模块三项 / 挂件面板定位四项）
pnpm build              # 前端生产构建（tsc + vite build → dist）
pnpm tauri build        # Windows release 打包（见 §7 注意事项）
pnpm tauri icon <png>   # 生成全套图标（源图需正方形）

# 安卓侧的两道「编译门」（本机无法提权时用它替代整包构建，详见 §7.6）
# Rust：cargo check --lib --target aarch64-linux-android（需先接 NDK clang，见 §7.6）
# Kotlin：cd src-tauri/gen/android; .\gradlew.bat :app:compileArmDebugKotlin

# 一键打包（脚本在仓库根目录；本机 pwsh 7 未安装 → 用 powershell 或直接调用）
powershell -File .\打包.ps1              # 桌面 exe（产物自动带时间戳命名）
powershell -File .\打包.ps1 -Android     # 桌面 exe + 安卓 APK（自动 UAC + 签名）
```

> ⚠️ 改完挂件前端后：**重启 dev** 再验收（窗口复用不重载页面）；
> 提交前跑一遍 `pnpm exec tsc --noEmit` + `pnpm test` + `cd src-tauri; cargo test --lib`。

**⚠️ 起 dev 一律用 `pnpm dev:app`**。裸 `pnpm tauri dev` 会与桌面打包版共用 identifier
（`com.apibalance.monitor`）与配置目录，导致单实例锁互踢、config.json 互相覆盖。
另：dev 进程残留会占住 1420 端口（表现为 `Error: Port 1420 is already in use`），
此时重启前先确认并结束旧的 `node`/`api-quota-monitor-tauri`/`cargo` 进程（见 §10「进程清理」）。

## 7. 构建打包（重要注意事项）

### 7.1 通用警告 ⚠️
- **APK 打包前先手动 `pnpm build`**：tauri android build 的 beforeBuildCommand 偶发用过期 dist（历史上多次出现"改动没进 APK"，手动刷新 dist 解决）
- **Android 构建必须管理员**（UAC，Windows symlink 需要）：临时 bat 在 `%TEMP%\build_android_debug.bat` / `build_android_release.bat`，日志 `D:\android-*-build.log`
- **不要直接 gradle 调用 tauri android-studio-script**（缺 WebSocket 服务会失败）；勿改 RustPlugin.kt 减 ABI（CLI 硬编码 4 ABI 任务会破坏构建）
- ⚠️ **改 `.ps1` 脚本必须保 UTF-8 with BOM**：`edit` / `write` 工具写回会丢 BOM → PowerShell 按 GBK 解析中文 →
  乱码、后半段源码被当字符串输出、构建中断。**本机只有 Windows PowerShell 5.1（没有 pwsh 7）**，
  正确做法是用 5.1 的 .NET API 写回 BOM：
  ```powershell
  $p = "D:\DeepSeek\api-quota-monitor-tauri\打包.ps1"
  $t = [IO.File]::ReadAllText($p, [Text.Encoding]::UTF8)
  [IO.File]::WriteAllText($p, $t, (New-Object Text.UTF8Encoding($true)))
  # 核对前 3 字节应为 239,187,191
  ([IO.File]::ReadAllBytes($p)[0..2]) -join ','
  ```
  另注意 5.1 下 `[IO.File]` 等 .NET 静态调用用**进程 CWD** 而非 PS 的 `cd` → 必须传绝对路径。

### 7.2 Windows exe（打包）
```bash
pnpm tauri build                 # 完整产物（含 NSIS 安装包）
pnpm tauri build --no-bundle     # 快速产物：仅 release exe（本机出包方式）
# 产物：src-tauri\target\release\api-quota-monitor-tauri.exe（免安装单文件，约 25MB）
# MSI 会失败：版本 0.1.0-beta 的 -beta 不满足 MSI 数字版号（需要时改纯数字版本）
```

### 7.3 Android APK（一次性打包流程）
```bash
# 1. pnpm build 刷新 dist（防 beforeBuildCommand 偶发用过期产物）
# 2. 管理员运行 %TEMP%\build_android_arm64.bat（admin：Windows symlink 需要），日志 D:\android-arm64-build.log
#    命令：pnpm tauri android build --apk --target aarch64
#    ⚠️ 停开发进程后再构建（cargo target 锁冲突）；构建完重启 dev 不影响项目
# 3. 产物：src-tauri\gen\android\app\build\outputs\apk\universal\release\app-universal-release-unsigned.apk
#    （--target aarch64 时 APK 内仅 lib/arm64-v8a 一个 ABI → 约 31MB 最小体积；arm64 主流手机可装）
# 4. 签名（调试密钥，Android 可装）：
#    apksigner.bat sign --ks %USERPROFILE%\.android\debug.keystore \
#      --ks-pass pass:android --key-pass pass:android \
#      --out 桌面\API余额通.apk app-universal-release-unsigned.apk
#    apksigner.verify 验证；附带的 *.idsig 是签名验证文件可删
# 5. 分发复制桌面；注意：正式发布需 release keystore（当前 debug 签名）
```

### 7.4 模拟器（Android 验证）
```bash
# AVD: tauri_avd（Pixel 7 / 1080×2400 @420dpi，已改回标准分辨率）
# GPU host 模式（比 swiftshader 流畅）：-gpu host
# adb: emulator-5554；调试 APK 与 release 签名不同，重装需先卸载
```
- ⚠️ **`tauri_avd` 是 x86_64 镜像**（2026-09/16 实测 `config.ini`：`abi.type = x86_64`、
  `image.sysdir.1 = system-images\android-35\google_apis\x86_64\`；设备 `ro.product.cpu.abi = x86_64`，
  但 `abilist` 同时列出了 `arm64-v8a`）→ **只出 `--target aarch64` 的最小包在这个模拟器上有装不上的风险**。
  要在本机模拟器上验证，出包时**不要**带 `--target`（= 全 ABI 的 universal 包）：
  `pnpm tauri android build --apk` → `gen/android/app/build/outputs/apk/universal/release/app-universal-release-unsigned.apk`
  （实测 4 个 ABI 全带，106.8MB；本机 4 个安卓 Rust 目标都已安装：aarch64 / armv7 / i686 / x86_64）。
  真机分发仍建议 `--target aarch64`（约 31MB 最小体积）。

### 7.5 一键打包脚本（2026-08 起使用，推荐）
- `打包.ps1`：`pnpm tauri build --no-bundle` → 复制桌面 **`API余额通_<时间戳>.exe`**（时间戳 = 打包完成时刻 `yyMMdd.HHmm`，如 `260830.0809`）
- `打包.ps1 -Android`：额外触发安卓 UAC 管理员构建（`--apk --target aarch64`）→ 等待完成 → `apksigner` 签名 → 桌面 **`API余额通-安卓_<时间戳>.apk`**（全程自动）
- **产物清理（2026-08/31）**：桌面只保留最新一个 APK（旧 APK 及 `.idsig` 签名验证文件自动删除，`.idsig` 无分发价值）；桌面 exe 同样清理旧版（⚠️ 旧 exe 删除可能被系统权限/Defender 锁定，需重启后手动清理）
- 依赖本机环境：Node / JDK 17 / Android SDK（见 §10）；安卓部分需在 UAC 弹窗点「是」
- ⚠️ **「模拟浏览器登录」已单 exe 化（方案 A，2026-08）**：`打包.ps1` 构建前自动生成 `src-tauri/runtime.zip`（minmax 全量压缩 38.8MB，剔除 `web_state/` 登录态隐私）嵌入 exe（~65MB）；首次查询自动解压到 `%APPDATA%\com.apibalance.monitor\runtime\`；目标机仅需系统 Edge（Win10/11 标配）+ WebView2 + VC++ 运行库。便携分发（exe + minmax 目录）仍兼容。zip 依赖 `zip = "2"`（Cargo.toml 已加）；`runtime.zip` 需在构建前存在（打包.ps1 自动生成；`cargo build --release` 直接跑会因缺 `runtime.zip` 失败——先用打包.ps1 或手动生成）
- ⚠️ 改 `打包.ps1` 的 BOM 注意事项见 **§7.1**（本机无 pwsh 7）
- ⚠️ **冒烟测试新 exe 时别指望"重定向 `APPDATA`"能隔离**（2026-09/16 实测踩到）：Tauri 的
  `app_config_dir()` 走 Windows **known-folder API**，**不读 `APPDATA` 环境变量** → 即使 `$env:APPDATA`
  指向临时目录，新 exe 照样会读写**真实的** `%APPDATA%\com.apibalance.monitor\config.json`。
  真要隔离只有两条路：①**先备份**真实配置再跑，跑完还原；②给测试用的构建换 identifier（像 dev 那样）。
  实测影响面：站点/Key/主题/壁纸都不受影响，被改的只是 `usageBaselines`（新版本首次启动本来就会做的迁移）。

### 7.6 安卓侧「编译门」（不需要管理员；整包构建跑不了时的替代）

`pnpm tauri android build` 在 Windows 上**必须能建符号链接**（把 `target/…/lib*.so` 链进
`jniLibs/`）——这需要**开发者模式或管理员权限**，否则会停在：

```
failed to build Android app: Failed to create a symbolic link from "...\target\aarch64-linux-android\release\libapi_quota_monitor_tauri_lib.so" to "...\jniLibs\arm64-v8a\..."
Creation symbolic link is not allowed for this system. For Windows 10 or newer: You should use developer mode.
```

拿不到管理员时，用下面两道门**照样能查出编译错误**（本轮实测有效，比整包快得多）：

- ⚠️ **桌面专用的常量/函数必须 `#[cfg(desktop)]` 门控**（2026-09/16 打整包时实测）：未门控会在
  安卓 release 构建里刷一屏 `never used` 警告（本轮新模块 `widget_menu.rs` 一次贡献 6 条，已修）。
  桌面 `cargo check` **看不见**这些警告（那边它们都被用到），只有安卓目标才会暴露。
  查法：`cargo check --lib --target x86_64-linux-android`（下面①的环境，换成 x86_64 的 CC/AR）。
  ⚠️ 截至本轮**仍剩 6 条既有的**（`widget.rs` 的 `DEFAULT_W/H`、`MIN_W/H`；`network.rs` 的
  `record_node_pid`/`clear_node_pid`；release 构建里还会多 `ensure_runtime`/`RUNTIME_ZIP`）—— 属待清理项，
  不影响产物行为。

```powershell
# ① Rust 侧（安卓目标）：先把 NDK 的 clang 接进环境，否则卡在 ring 的 C 编译
$ndk = "$env:LOCALAPPDATA\Android\Sdk\ndk\30.0.15729638"     # 版本号按实际目录改
$tc  = "$ndk\toolchains\llvm\prebuilt\windows-x86_64\bin"
$env:PATH = "$tc;$env:PATH"
$env:CC_aarch64_linux_android = "$tc\aarch64-linux-android24-clang.cmd"
$env:AR_aarch64_linux_android = "$tc\llvm-ar.exe"
cd src-tauri; cargo check --lib --target aarch64-linux-android
# ⚠️ 这些环境变量**别留着**跑桌面构建：CC 指向安卓 clang 会把 ring 的 Windows 构建弄崩（本轮踩过）。
#    每次 pwsh 调用是新进程，所以「单独一条命令跑安卓 check」就天然隔离了。

# ② Kotlin 侧：把已编译好的 .so 拷进 jniLibs（代替符号链接），再直接叫 Gradle 编译 Kotlin
$so = 'src-tauri\target\aarch64-linux-android\release\libapi_quota_monitor_tauri_lib.so'
Copy-Item $so 'src-tauri\gen\android\app\src\main\jniLibs\arm64-v8a\' -Force
cd src-tauri\gen\android
$env:JAVA_HOME   = 'C:\Users\<你>\AppData\Local\Programs\Eclipse Adoptium\jdk-17.0.20+8'
$env:ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"; $env:ANDROID_SDK_ROOT = $env:ANDROID_HOME
$env:NDK_HOME    = "$env:LOCALAPPDATA\Android\Sdk\ndk\30.0.15729638"
.\gradlew.bat :app:compileArmDebugKotlin --console=plain
# ⚠️ 任务名必须带 flavor：`:app:compileDebugKotlin` 会报 ambiguous
#    （候选 compileArmDebugKotlin / compileUniversalDebugKotlin），本项目的 APK 走 arm。
# ⚠️ 检查完**删掉**那份拷贝（`.so` 虽被 gitignore，但留着陈旧副本会被后续打包误用）。
#    只跑 Gradle 的 Kotlin 编译任务**不会**触发 tauri CLI 的符号链接步骤 → 绕过权限问题。
```

⚠️ 这两道门只能证明**编译通过**，证明不了运行时行为（分享面板里有没有微信、保存的文件是否真落地等）
——那些只能在模拟器/真机上验（§7.4）。

## 8. 已知问题与待办

| 项 | 状态 | 说明 |
|---|---|---|
| **鲸鱼娘挂件滚轮缩放** | ✅ 根因已定位 + 既快又不抖（2026-09/11） | 屏幕捕获实测锁定根因 = **窗口几何被 DWM 即时应用、内容层下一帧才上屏**（偏移 ≈ 0.4 × 单帧窗口位移）；解法 = **窗口几何晚一帧发布**（两者取自同一步 → 错位消失）+ 单帧位移上限 `MAX_STEP=6` 作速度旋钮（§4.6）。实测（物理像素）：快速滚动 抖动均值 3.94→**0.285**、峰值 5.0→**0.53**，且动画 399ms **比旧策略还快**；标准 0.98→**0.286**。已知副作用：延后期间内容比窗口大 ≤6px，右/下边缘被裁一条（可见框仍平滑，不易察觉）。**待用户主观验收**；量测夹具 `probe/` 可随时复测回归 |
| **方案筛选** | ✅ 已完成（2026-08） | 弹窗 + visibleEntries 过滤 + 多方案卡片已接线（§4.7） |
| **设置页** | ✅ 已完成（2026-08） | 主题/壁纸/全局自动刷新（§4.8） |
| **窗口几何记忆** | ✅ 已完成（2026-08） | 主窗口 + 挂件位置/大小记忆（§4.5/§4.6） |
| **测试 2 项失败** | ✅ 已修复（2026-08） | 根因：`parseAnyImportPayload` 是 **async 函数**，但 `test/sync.test.ts` 第 78/86 行调用时**没有 await**（`it` 未标 async），拿到的是 Promise，`.ok` 为 undefined。已加 `await` 并将两处 `it` 回调改为 `async`，`pnpm test` 32 项全部通过。生产代码本无问题 |
| **git 仓库未建** | ✅ 已建（2026-09/15） | 已 `git init` 并建立基线提交（仓库根 `.git`，分支 `master`，仓库级 user.name/email = Zimu233L）。**未配置任何 remote**（需用户确认托管位置后再加）。提交信息风格沿用 `type(scope): 中文描述`，正文写"问题/改法/验证" |
| **仓库体积与忽略规则** | ✅ 已补（2026-09/15） | 根 `.gitignore` 已补：`/src-tauri/target/`(实测 47.0GB)、`/src-tauri/gen/android/{app/,}build/`(10.8GB)、`/src-tauri/runtime.zip`(37MB)、`minmax/node.exe`(88MB)、`minmax/web_state/`、`/probe/traces/`、`/probe/shots/`；新增 `.gitattributes`（二进制不转换；换行统一 LF，仅 `.bat/.cmd` 保 CRLF）。**实测入仓 202 个文件 / 21MB**（若无规则将是约 57GB）。补充：原文档把 `src-tauri/gen`(10.8GB) 整体当成"待忽略"，实测**其中 10.76GB 全在 `app/build/`**，`app/src/`（含**手写的 `WebLoginPlugin.kt`**，安卓 WebView 登录桥接）只有 0.2MB 且必须入仓——故规则只忽略 `build` 产物目录，不整目录忽略 `gen/`，否则会丢掉安卓端源码。⚠️ `src-tauri/target` 的 47GB 是构建缓存，**勿误删**（删了要重新全量编译） |
| **README.md 未更新** | ✅ 已更新（2026-08） | 已改写为项目说明（概述/技术栈/开发命令/文档链接），非模板内容 |
| **死代码** | ✅ 已清理（2026-08） | 删除 `worker.ts refreshAll`（App 自实现逐站点刷新，无调用）、`store.ts moveProvider` + Rust `move_provider` 命令（拖拽排序早已改为整体 saveConfig）、`qr-decode.mjs` 调试残留（import pngjs 但无依赖，不可运行）；测试 42 项 + tsc + cargo check 全过 |
| **Maple Mono 字体** | ✅ 已优化（2026-08） | **woff2 无损压缩**（fonttools+brotli）：39.5MB ttf → 12.7MB woff2（各 ~6.3MB），CSS 改引用 woff2、删除源 TTF，dist 40.3MB→13.1MB，exe/APK 体积同步下降，Android OTS 警告消除，视觉零损失 |
| **手机真机验证** | ⚠️ 待办 | 桌面 APK 已装模拟器验证，真机待用户试用 |
| **正式发布** | ⚠️ 未做 | 需 release keystore（当前是 debug 签名）、图标已更新、Play AAB 未做 |
| **设备间迁移体验** | ✅ 两端齐备（2026-09/16，**只走文件**） | 面板只剩「配置文件」一段：`[导入配置文件]`（选 `.json`/`.txt`）+ `[导出配置文件]`。**Windows**：写文件后**把文件本身放进剪贴板**（`CF_HDROP`），到资源管理器/微信/QQ 直接 `Ctrl+V`。**安卓**（用户口径「AB 结合」）：点导出后二选一 —— **系统分享**（`ACTION_SEND`+FileProvider）/ **保存到本地**（`ACTION_CREATE_DOCUMENT`），零存储权限，实现见 `ConfigExportPlugin.kt`。**文本粘贴方案已删除**。文件内容：未加密 = 标准 JSON、加密 = 该 JSON 的 `AQM2:` 密文（默认加密）。**不做「另存为」对话框**，故仍未引入 `tauri-plugin-dialog`/`fs`。⚠️ **安卓导入/导出的运行时均未在设备上实测**（导入靠 wry 的 `onShowFileChooser`，实现存在；本轮新代码只过了编译：安卓目标 `cargo check` + Gradle `:app:compileArmDebugKotlin` 均通过），待模拟器/真机验证。⚠️ 安卓端 `crypto.subtle` **依据规范可用**（不再是未知）：Tauri 页面 origin = `tauri.localhost`（`https://` 或 `http://`），而 W3C Secure Contexts 把 `localhost` 及其子域列为可信来源 → `isSecureContext === true` → Web Crypto 可用；但**未在设备上实测过**（若真机发现不可用，退路是把 PBKDF2/AES 挪到 Rust 侧） |
| **Android 端验证** | ⚠️ 部分完成 | 设置/壁纸/Code AI 等前端改动已在模拟器实测通过；**模拟浏览器登录（WebView 方案 A）安卓端完整实测通过**（无感刷新、自动抓取、不弹窗）；真机未验（模拟器 AVD tauri_avd，adb root 可写 config） |
| **清理残留** | ✅ 已完成（2026-08） | `icon_sprbaoxtamr/`（旧图标素材）、`public/vite.svg`（模板 favicon，无引用）已删除 |
| **MaoMaoAI 会话上限经验** | ⚠️ 记录 | api.maomaoai.pro 登录接口 `POST /api/user/login` 返回 **409 `AUTH_SESSION_LIMIT`**（会话数上限，前端 JS 错误码表：官方解法 = 网页 Login sessions「Sign out other sessions」或**重置密码**登出全部会话）；另有 `AUTH_SESSION_ISSUANCE_LIMIT`（登录频率限制，滚动窗口）；`/api/user/auth/refresh` 接口存在（401=需鉴权）；常见 logout 路径全 404（无自动化登出）。另：大量探测请求会触发 **Cloudflare IP 层封禁**（TCP 443 拒绝，几分钟~几十分钟自动解除，可用代理绕过）。恢复建议：该站刷新间隔调大（≥30 分钟），App 侧已有 token 缓存 + 409 自动清理兜底 |
| **「模拟浏览器登录」实测** | ✅ 已补全并实测通过（2026-08） | `minmax/query_balance.js` 已补全（此前缺失，Rust `browser_login_query` 引用它导致适配器是断的）；桌面 MiniMax 站真实账号实测：全新登录/复用登录态/稳定性 ✓；**安卓端方案 A（内嵌 WebView）完整实测**：无感刷新自动抓取（DOM 提取）、登录态 CookieManager 持久化、不弹窗、刷新兜底/超时 ✓。待办：桌面打包版 exe 实机验证 + 其它站点适配 |
| **登录态凭据裸放项目目录** | ✅ 已迁移（2026-09/15） | 三个真实账号会话文件（`browser_account.minimaxi.com.json` / `browser_platform.minimaxi.com.json` / `minimax_web.json`）已移出项目目录 → `%APPDATA%\com.apibalance.monitor\web_state\` 与 `...monitor.dev\web_state\`（SHA256 校验一致、双份备份、ACL 收紧为「仅当前用户/SYSTEM/Administrators」），项目内副本已删除。代码改法见 §4.3；`.gitignore` 另加 `minmax/web_state/` 兜底。⚠️ **未做**：项目外无痕化（回收站/卷影副本仍可能有旧副本）；**另建议用户到 MiniMax 后台登出旧会话**（这几个 cookie 曾在项目目录明文存放约两周） |
| **PowerShell 7 未安装** | ✅ 文档已修正（2026-09/11） | 原文档多处写"**必须用 pwsh 脚本写回保 BOM**"，但本机只有 **Windows PowerShell 5.1**（`pwsh` 不存在）。BOM 要求本身正确（5.1 会把无 BOM 的 UTF-8 中文按 GBK 解析、直接语法崩），已把 §6/§7.1/§10 的指引改成 5.1 下可直接照做的写法（.NET API 写回 + 绝对路径） |
| **`window.screenX` 高 DPI 读数** | ⚠️ 旧结论存疑（2026-09/11） | 旧文档称"WebView2 高 DPI 下 `screenX` 与 Rust 真实值差可达 1000px，是漂移根源"；2026-09/11 复测**未能复现**（读数与配置中的 `showWidgetPos` 一致）。当前实现仍不依赖它（位置数学全在 Rust，这是对的），但该结论不应再被当作事实引用 |
| **config.json 带 BOM 会丢配置** | ✅ 已修（2026-09/15） | 实测：用带 BOM 的 config.json 启动 → `serde_json` 报 `expected value at line 1 column 1` → 旧逻辑判定「配置损坏」→ 备份回退默认 → 差点丢掉 6 个站点配置（本次由「改配置做跨天实测」意外触发，已从备份完整恢复并实测修复生效）。现 `load_config` 走 `strip_utf8_bom` 容错 + 3 项 Rust 单测。⚠️ **提醒**：config.json 里是明文 API Key，恢复时若拿 `.bak` 覆盖注意别把 `config.json.bak-before-sub2api-merge` 这类**历史快照**当成当前配置 |
| **「今日已用」口径** | ✅ 已换模型（2026-09/16 晚，用户口径「我的项目也改为：消费只由『下降』构成」） | **消费 = 当日累计下降**：每次观测与上次读数比较，下降累加进 `debit`（= 今日已用）、上升只累加进 `credit`。于是**充值在结构上不可能污染数字**（单调不减、恒 ≥ 0），**不再需要用户登记任何金额**，也不会再出现"提示把数字顶掉"。旧模型（`max(基准,水位,最高余额)+已登记充值−当前余额` + `设置今日充值金额` 登记流程）已整体删除：它把充值当修正项，而登记时水位又前移到"已含这笔充值的余额"→ 充值被算两次、显示值恒多出整整一个充值额，且判据下界被抬到 `peak+充值`（之后再充小额永不提示）。⚠️ 换模型当天数字**从 ¥0.00 重新累计**（旧记录没有历史下降步可继承）。详见 §5 |
| **挂件纵向分布** | ⚠️ 用户已要求撤回一版 | 2026-09/15 曾把三行按气泡内区等间距重排（站名 17.5%/金额 31.5%/状态 44.1%），**用户看过效果后要求全部撤回** → 现为站名 19.5%/金额 31%/状态 40.5%。结论保留：气泡内区约 103px 而内容总高 70.6px，"等间距"与"不越界"不能兼得。另：非 DeepSeek 站点（无峰谷行）三行整体上移 10.2px（`data-has-peak='0'`，见 §4.6） |
| **PEAK 倒计时文案** | ✅ 已定稿（2026-09/16） | 带秒（<2h）**不带「剩余」**（如 `1时30分46秒`），不带秒（≥2h）保留前缀（如 `剩余5时23分`）；阈值与主卡片 `fmtPeakCountdown` 一致（7200s 分界） |
| **充值面板独立窗口** | ✅ 已完成（2026-09/16，用户口径「把他独立，移动到鲸鱼娘窗口上方，和鲸鱼娘窗口右对齐」） | 面板原先内嵌在挂件窗口内、**挂件缩小后被裁**（用户反馈"设置界面显示不全"）→ 改为**独立窗口**（`src-tauri/src/widget_menu.rs` + `src/widget/WidgetMenuApp.tsx` + `widget-menu.css`，label=`widget-menu`）：恒在挂件**正上方**、与其**右对齐**，高度由内容决定（**底边/右边缘固定、向上生长**），拖动/滚轮缩放挂件时由挂件那条 60Hz 轮询同步跟随（**实测拖动全程跟随**）。实测四方对照：面板右缘 == 挂件右缘（330.4）、底边 == 挂件顶边 − 6px（482.8）、两次内容高度回报（118→151）都保持底边不动、面板内元素全部在视口内；**真实鼠标**点 ☰ 开/关均命中（命中保护区由 230×176 **缩到 40×72**）。详见 §4.6「充值面板独立窗口」 |
| **历史里的登录态压缩包** | ✅ 已清除（2026-09-16 推送前审计发现并处理） | **发现**：跟踪文件 `minmax.7z`（3.11MB，基线提交 `374b524` 引入）里含 `minmax/web_state/minimax_web.json`（Playwright storageState：**16 条 cookie**，域名 `.minimaxi.com`/`platform.minimaxi.com`/`account.minimaxi.com`，含 `_sid`/`_token`/`_oauth_state`，**最长有效期 2027-10-03**）；blob `de59bc6a…` 存在于**全部 32 个提交的树**里 → **推送即公开**。**处置**（用户选 B）：删 `minmax.7z` 与 `src-tauri/icons.bak/`（35 文件 3.33MB 备份图标）→ **清洁重建仓库**（`git init -b main`，旧历史弃用，旧 `.git` 压缩备份到 `D:\DeepSeek\api-quota-monitor-tauri-git-backup-260916.zip`）→ 复验对象库里已无 `de59bc6a`。**遗留（需用户做）**：去 MiniMax 后台**登出其它会话或改密码**，让这套 cookie 彻底失效（凭据 2027 年才过期）；确认无需旧提交后删除那个备份 zip。**根因**：`.gitignore` 挡不住"打进压缩包的副本"，归档入仓前须先列包内容 |
| **「今日已用」开发期白屏** | ✅ 已定位并解决（2026-09/16） | **现象**：dev 起来了、Vite 也在 1420，但主窗口全白、dev 日志里**一条前端活动都没有**（无 `icon-fetch`、无 `[ui] result`）。**根因**：改前端时**删掉了某个被别的模块 import 的导出**（`usage.ts` 不再导出 `setRechargeAmount`），而 dev 还开着 → WebView 缓存里留着**旧版 WidgetMenuApp**（仍 `import { setRechargeAmount }`）+ 新版 `usage.ts` → 模块图混用 → `Uncaught SyntaxError: ... does not provide an export named ...` → `main.tsx` 整条链没执行 → React 挂不上 → 白屏。**定位手法**（可复用）：① 探针 `{"measure":[".app-shell"]}` 有响应但 `count=0` → 判定"脚本在跑、React 没挂载"；② 在 `index.html` 临时加 `window.addEventListener('error')` 把报错 POST 到 vite 的 `/__aqm/trace`（`probe/traces/run-999-*`），一次就拿到真实错误。**解决**：停 dev → 清 WebView2 的 `Default\Cache` + `Default\Code Cache`（§10）→ 重启 dev。**预防**：**改前端导出/接口签名后一律重启 dev**（与"改挂件前端必须重启"同一条坑） |
| **安卓导入/导出的设备端实测** | ⚠️ 待办（2026-09/16） | 本轮新代码只过了**编译**（安卓目标 `cargo check` + Gradle `:app:compileArmDebugKotlin`），**运行时一次都没跑过**。要在 `tauri_avd` 上验：①导入选文件是否走系统选择器、`file.text()` 是否拿得到内容；②导出的「系统分享」面板是否列出微信/QQ、发出去的文件能否被另一台设备导入；③「保存到本地」是否真把 `AQM2:` 密文写进所选位置；④加密路径要用到 `crypto.subtle`（依据规范可用，见上表「设备间迁移体验」）。⚠️ 前提：整包 APK 构建需要**开发者模式/管理员**（§7.6） |

## 9. 安全与约定

- **凭据**：config.json 明文存 API Key**及阿里云/火山引擎 AccessKey Secret（云账户钥匙，权限更高，注意保密）**；同步导出含 Key（界面有警示 + 可选口令加密/二维码明文警示）——**2026-08/31 剪贴板导出已压缩（`Z1:` 文本），但明文压缩 ≠ 加密，含 Key 的压缩文本同样注意保密**
- ⚠️ **归档/分发压缩包必须先剔除登录态**（2026-09-16 审计踩到）：`minmax.7z`（旧的手工分发包）里带着
  `minmax/web_state/minimax_web.json`（16 条 MiniMax cookie，`_sid`/`_token` 等，最长有效期到 **2027-10**）
  → 从基线提交起就在 git 历史里。**教训**：`.gitignore` 只挡"工作区文件"，**挡不住"打进压缩包的副本"**；
  凡是要提交的归档（`.7z`/`.zip`/`.tar`）都要先列内容确认（`7z l <包>`）再入仓。
  打包脚本生成 `runtime.zip` 时**本来就剔除 `web_state/`**，人工打包时要照做。
  **处置**：删包 + 清洁重建仓库（§8）；**凭据本身仍须作废**（去 MiniMax 后台登出其它会话或改密码）
- **导出口令强度（2026-09/16 起更要紧）**：导出的密文可能被**长期留存**（存成文件、丢网盘、发聊天软件），离线爆破没有次数限制 → `AQM2:` 迭代提到 **60 万次 PBKDF2**，但**弱口令（如 8 位纯数字）依然不安全**。界面文案已提示"口令丢失无法恢复"；**未做**口令强度校验（本项目偏好不拦用户输入）。⚠️ 加密封装保护不了元信息：文件名/时间戳、文件大小（可粗略推出站点数）仍是明文
- **明文导出的静默降级已被显式禁止**：加密开关打开但口令为空时必须报错（`buildExportText` 抛错 + 单测），**不允许**"悄悄按明文复制"这种用户以为已加密的情况；且该开关**默认开启**（2026-09/16 用户口径），即默认路径是加密、明文需用户主动关掉
- **浏览器登录态（会话 cookie）**：**2026-09/15 起已移出项目目录**，存 `%APPDATA%\com.apibalance.monitor[.dev]\web_state\`（应用配置目录，与 config.json 同级），权限收紧为仅当前用户/SYSTEM/管理员；Rust 通过环境变量 `AQM_WEB_STATE_DIR` 注入给 node 脚本。`.gitignore` 另以 `minmax/web_state/` 兜底防误入仓（见 §4.3、§8）
- 适配器注册顺序有用户指定的业务含义（DeepSeek → 自动识别 → 阿里云 → 火山引擎 → Code AI → OpenAI 兼容 → OpenRouter → 模拟浏览器登录，2026-08 多次调整后的最终顺序）
- 云厂商（阿里云/火山引擎）「接口地址（可选）」配置项收在对话框**高级设置**折叠区（`advanced: true`），表单默认只露必填项
- 非主流站点 placeholder 用 `example.com`，主流保留真实地址
- 界面小步迭代、改前确认（用户偏好）
- **git 提交约定**（2026-09/15 建库后启用的实际风格）：`type(scope): 中文描述`，正文写「问题 / 改法 / 验证」三段；
  `type` 用 feat / fix / docs / chore / revert。提交信息**用文件传入**（`git commit -F <msgfile>`）——
  带中文与引号的 `-m` 串在 PowerShell 里会被拆成 pathspec（本轮踩过，见 §10）。

## 10. 环境依赖（本机）

- Rust 1.98 MSVC（`%USERPROFILE%\.cargo`，rsproxy 镜像）✅ 实测 rustc 1.98.0
- Node 24（实测 v24.19.0）+ pnpm 11（实测 11.7.0 / 11.22.0）
- JDK 17（Adoptium，实测路径 `%LOCALAPPDATA%\Programs\Eclipse Adoptium\jdk-17.0.20+8`；Gradle 构建需 `JAVA_HOME`）+ Android SDK（`%LOCALAPPDATA%\Android\Sdk`）+ **NDK 30**（实测 `ndk\30.0.15729638`）
  - ⚠️ 安卓侧**两道编译门**的环境变量（NDK clang / Gradle 任务名 / 符号链接权限问题）全写在 **§7.6**
- 模拟器 AVD `tauri_avd`
- ⚠️ **安卓整包构建需要「开发者模式」或管理员**（Windows 建符号链接），本机历史上靠 `打包.ps1 -Android` 走 UAC；
  拿不到提权时用 **§7.6** 的两道编译门替代（能查编译错，查不了运行时）。
- ⚠️ **PowerShell：只有 Windows PowerShell 5.1（`PSHOME = C:\Windows\System32\WindowsPowerShell\v1.0`），没有 pwsh 7**
  （`Get-Command pwsh` 无结果）。本文档中一切 `.ps1` 相关操作按 5.1 写：**必须保 UTF-8 BOM**（§7.1），
  且 .NET 静态 API（`[IO.File]` 等）用的是**进程 CWD**、不是 PS 的 `cd` → 传绝对路径（踩过）。
- ⚠️ **显示环境**：1920×1200 物理 @ **125% 缩放**（PowerShell 默认 DPI 不感知，`Screen.Bounds` 会报 1536×960，
  量测脚本需先 `SetProcessDpiAwarenessContext(-4)` 才是物理像素）。
- ⚠️ **进程清理**：dev 进程残留会占住 1420 端口（报 `Port 1420 is already in use`）。
  重启前先确认/结束：`node`（vite 与 tauri CLI，命令行含 `api-quota-monitor-tauri`）、`api-quota-monitor-tauri`、`cargo`。
  ⚠️ **别误杀 DSH 自身进程**（监听 3080 的 node）——按命令行匹配项目路径来筛。
  一键清理（安全：只按项目路径匹配，不动 DSH）：
  ```powershell
  Get-CimInstance Win32_Process -Filter "Name='node.exe' OR Name='cargo.exe' OR Name='api-quota-monitor-tauri.exe' OR Name='rustc.exe'" |
    Where-Object { $_.CommandLine -match 'api-quota-monitor-tauri' } |
    ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
  ```
  ⚠️ 停完**务必确认 1420 已释放**再起 dev；否则新 dev 会启动失败，而你可能对着**上一个会话的挂件**
  继续做判断（2026-09/16 实测踩过：量到的倒计时属于上一会话，结论全错）。
- ⚠️ **白屏排查与修复**（2026-09/16 实测，症状：dev 起来了、日志里却一条前端活动都没有）：
  1. **判定"是不是 React 没挂载"**：用探针打 `{"run":N,"layout":true,"measure":[".app-shell",".site-card"]}` ——
     若**有 trace 回传但 `count=0`**，说明页面脚本在跑、只是 React 挂不上（不是页面没加载）。
  2. **拿到真实报错**：临时在 `index.html` 的 `<head>` 里加一段 `window.addEventListener('error', …)`，
     把 `message/stack` POST 到 vite 的 `/__aqm/trace` → 落盘成 `probe/traces/run-999-*.json`（用完删掉）。
     典型报错：`Uncaught SyntaxError: The requested module '/src/lib/xxx.ts' does not provide an export named 'yyy'`
     —— 即**旧模块 + 新模块混用**（改了前端的导出/签名却没重启 dev）。
  3. **修复**：停干净 dev（上面那条命令）→ 删 WebView2 的 HTTP 缓存与代码缓存 → 重启 dev：
     ```powershell
     $root = "$env:LOCALAPPDATA\com.apibalance.monitor.dev\EBWebView"
     Remove-Item "$root\Default\Cache","$root\Default\Code Cache","$root\Default\GPUCache" -Recurse -Force
     ```
     （只删缓存子目录，**不动** `Default` 下的 localStorage 等；登录态本来就在 App 配置目录、不在这里。）
- ⚠️ **写临时 `.ps1` 自动化脚本**（点按钮/敲键盘类验证）：PowerShell 5.1 读取无 BOM 的 UTF-8 中文脚本会
  按 GBK 解析 → 语法直接崩（本轮又踩一次：用 `write` 工具生成的**中文** `.ps1` 直接崩在中文行上）。
  **最省事的做法：临时脚本内容只用 ASCII**（英文提示语照样够用）；确需中文则必须带 BOM：
  用 `[IO.File]::WriteAllText($p, $code, (New-Object Text.UTF8Encoding($true)))` 写
  （`$true` = 带 BOM）。另：`git commit -m "含中文与引号的长串"` 在 PS 里会被拆成 pathspec，
  **改用 `-F <消息文件>`**。
- ⚠️ **量测挂件内的控件**（按钮/输入框位置）：用 `probe/zoom-probe.ts` 的 `{"measure":[…]}` 读几何，
  比推算屏幕坐标可靠得多；需要真实点击时，物理坐标 = 窗口矩形左/上 + CSS 坐标 × (窗口物理宽 / 308)。
  `input` 的当前值走 `.value`（`textContent` 读不到），但 `<button>` 也有空 `value` 会盖住文字——探针已按标签区分。

## 11. 文档版本记录

**近期改动速查（详细记录见下方表格，按时间顺序追加，最后一条最新）**

> ⚠️ 读表须知：本表只收录**有功能/行为变化**的轮次。**纯文档轮**（第十二轮、第十八轮等）只出现在下方「详细记录」表里，
> 因此这里的轮号**可能跳号**（如"第十一轮 → 第十三轮"）——那是文档轮，不是漏记。

| 轮次 | 一句话 | 关键结论 |
|---|---|---|
| 2026-09（挂件缩放重构） | 锚点数学移到 Rust + 内容层改固定画布 transform 缩放 | 消除漂移；但遗留"残余抖动"未结 |
| 2026-09/11 第一轮 | 建 `probe/` 客观量测夹具，实测定位抖动的**真实根因** | 根因 = 窗口几何被 DWM 即时应用 vs 内容层下一帧才上屏，偏移 ≈ 0.4×单帧位移；推翻了"亚像素错位"旧结论 |
| 2026-09/11 第二轮 | 让**窗口几何晚一帧发布**，抖动与时长同时下降 | 快速滚动 3.94→0.285 均值、5.0→0.53 峰值，且动画比旧策略更快 |
| 2026-09/11 第三轮 | 挂件加 DeepSeek 峰谷徽标 | 徽标落点用背景图亮度图确定（§4.6） |
| 2026-09/11 第四轮 | 挂件**空白处点击穿透** | 背景图 47.8% 全透明 → 动态 `ignore_cursor_events` + 128² 遮罩；返回按钮必须特判保护 |
| 2026-09/15 | **接管评估 + 建库 + 凭据迁移**（本轮） | 四项检查全绿复验；`git init` 建基线（忽略规则补全后 197 文件 / 20.6MB，而非 57GB）；登录态凭据移出项目目录改存应用配置目录 |
| 2026-09/15 第二轮 | **挂件状态行改为「今日已用」** | 纯本地推算（当日首次余额为基准，存 `config.json.usageBaselines`）；探针布局报告实测与「基准−当前」逐位吻合、未溢出；测试 56 → 70 |
| 2026-09/15 第三轮 | **挂件峰谷行加倒计时；先下移再按用户要求归位顶部** | 新增 `fmtPeakCountdownRemaining`（「剩余5时23分」，与主卡片同口径同阈值）；下移落点实测是**全透明横带**（点击穿透、观感悬空）→ 用户看后要求放回顶部；新增 `probe/bg-sample.mjs` 背景图 alpha/亮度取样 |
| 2026-09/15 第四轮 | **挂件排版微调（两处）+ 币种符号间隙** | ①峰谷行上移 0.3% → 与站名间隙 3.1→4.2px；②**站名与金额整体下移 2%（≈6.2px）**→「金额↔今日已用」10.9→**4.7px**；③「今日已用」行 `¥` 与数字之间加 **U+2009 细空格**（新增 `fmtMoneyParts`/`fmtMoneySpaced`，其余金额格式不动）。全部用探针布局报告核对数值；新增 `test/fmt.test.ts`（含 fmtMoney 回归），测试 83 → **101** |
| 2026-09/15 第五轮 | **设置页改版 + 「滑动效果」开关（启动自动打开挂件）** | ①主题改**三按钮横向**、壁纸下拉移到**同一行右侧**；②全局默认自动刷新间隔**收纳进「高级设置」折叠区**；③新增 `config.autoOpenWidget` + 「滑动效果」勾选框，启动 1.2s 后自动打开挂件窗（并记住 `config.lastWidgetUid`，启动还原上次那个）。探针新增**通用元素量测**（`{"measure":[…]}`，主窗口也能用）+ **`probe/bg-sample.mjs`**（背景图不透明图/亮度取样）；`App.tsx` 加仅 dev 的 `?settings=1` 钩子（已实测生产构建无残留）。⚠️ 实测踩坑：自动打开 effect 依赖 `[cfg]` 会被 cleanup 取消定时器 → 挂件永远打不开（表现为"开关没反应"）；改 `[]` + `cfgRef` 后实测打开成功 |
| 2026-09/15 第六轮 | **挂件纵向分布「等间距重排」——已按用户要求全部撤回** | 曾为「普通站点时站名偏到纵向中下部」把三行改为站名 17.5% / 金额 31.5% / 状态 44.1%（相邻间隙实测各 14.3px，气泡内区实测 y≈30~133）。用户看过效果后要求**撤回该句对应的全部改动** → 已回退 `src/widget/widget.css` 与 `probe/zoom-probe.ts`（对前一提交零差异），恢复为站名 19.5% / 金额 31% / 状态 40.5%。**结论保留**：气泡内区仅 103px 而内容总高 70.6px，「等间距」与「不越界」不能兼得；`probe/bg-sample.mjs` 仍可用于日后重新讨论 |
| 2026-09/15 第七轮 | **开关控件改版：文案更正 + 左右滑动开关** | 用户要求：文案改为「自动打开鲸鱼娘窗口」（原「滑动效果」名实不符）、控件改成**左右滑动开关**、**不要勾选框**、**不要说明文本**。实现 `.switch` / `.switch-thumb`（`role="switch"`、`aria-checked`、拇指 `left` 过渡，键盘焦点环）；实测两态滑块 2.8px ↔ 22.4px（右缘余量 1.6px）、`.check-label` 已消失、开关下方无说明文本、标签「自动打开鲸鱼娘窗口」x=44.8 在左、轨道 x=352.8 在右；开启态启动实测自动打开挂件成功 |
| 2026-09/15 第八轮 | **无特殊内容行的站点：三行整体上移 10.2px** | 用户要求「显示没有特殊显示内容的站点数据（如峰谷等）时，三行上移」。实现 = 画布加 `data-has-peak` 标记 + `.widget-canvas[data-has-peak='0']` 条件位移（站名 16.2% / 金额 27.7% / 状态 37.2%）。实测 Max66（无峰谷行）：三行 y 49.3→**39.1**、77.5→**67.3**、118.2→**108.1**（各上移 10.2px），行间间隙仍 6.6/4.7px，落点仍在白色气泡内。上移量取「有峰谷行时的 峰谷行→站名 间隙 10.2px」→ 两类站点顶部留白一致。⚠️ 有峰谷行那一侧本轮未量到（挂件停在 Max66），数值沿用上轮实测 |
| 2026-09/16 第九轮 | **充值干扰修复：右上角菜单 + 「设置今日充值金额」** | 用户反馈「今日已用数据受到了刚刚充值余额的数据干扰」。方案（用户指定）= 状态行改显 `设置今日充值金额`，右上角新增与返回键同款的**菜单按钮**（正下方）弹出面板手动填写充值额。⚠️ 踩坑：菜单键/面板在透明区被**点击穿透**（漏了命中特判）→ 把右上角 230×176 整块纳入保护（`in_fixed_overlay`，`widget_hit_state` 字段 `backButton`→`fixedOverlay`）。⚠️ 顺带修：探针 `{"measure":[…]}` 此前只接在主窗口循环、挂件侧不响应（量测永远拿到主窗口那份）。测试 101 → **110** |
| 2026-09/16 第十轮 | **充值判据按用户口径重构：余额上升即充值；拆开 peak / maxSeen** | 用户指出「今日消耗单调递增，数值变小时不就代表充值吗」—— 我原先只在「差值为负」时提示，会漏掉**充值额小于已消耗**的情形。重构中连踩三个坑并逐一修掉：①把「上升」当瞬态（本次>上次）→ 提示会闪一下就没（挂件 60s 刷一次，下次读数回落信号即消失）；②用「当前余额 < 水位」当判据 → 每次正常消耗都误报；③把 `peak` 同时当「已入账水位」与「当日最高余额」→ 刷新时抬高水位**把充值信号自己消费掉**，提示永不出现（实测 `peak` 被抬到 18.32 后 `current > peak` 恒不成立、界面 `alert=false`）。最终**拆成两个字段**：`peak`（只用于充值判据，仅在登记充值时前移）与 `maxSeen`（只用于「今日已用」显示，每次刷新取 max）。判定与显示改用同一套持久值后实测闭环：水位 5.0 / 当前 17.91 → 提示出现且 `peak` 保持 5.0；点菜单登记后 `peak` 前移至 17.91、提示收起、`今日已用 ¥12.91` 数值吻合。删除了因此失效的 `alerted` 字段与 `mark_recharge_alerted` 命令（死代码）。测试 110 → **116** |
| 2026-09/16 第十一轮 | **倒计时带秒不带「剩余」；预填值移除；充值额定为「固定值·可随时改·隔天清空」** | ①`fmtPeakCountdownRemaining(totalSeconds, withPrefix=false)`：<2h 带秒不带前缀（实测 `1时30分46秒`），≥2h 仍 `剩余5时23分`。②**移除预填猜测值**（曾用「当前余额 − 水位」，余额一变它就跟着变——用户原话「充值金额为什么跟着余额变」）；改为显示你填过的固定值供修改，从未填过则为空。③跨天清空：抽 `fresh_baseline` 纯函数 + Rust 单测 + 端到端实测（`recharged` 88.5→**0**）。④顺带修探针 bug：`value ?? textContent` 让 `<button>` 的空 value 盖住按钮文字。测试 116 → **114**；Rust 单测 4 → **5** |
| 2026-09/16 第十三轮 | **同步导出改版：加密滑动开关（默认开启）+ 「导出配置文件」按钮；加密格式升版 `AQM2:`（60 万次迭代）；二维码方案 B 暂时禁用** | 桌面导出走**复制形式**（不做另存为、不加 dialog/fs 插件），加密与否由**与设置页同款的滑动开关**控制（**默认开启**，明文需主动关掉）；`AQM1:` 旧密文仍可解（用**升级前产出的真实旧密文**做回归护栏）；开关开着而口令为空**抛错，绝不静默按明文复制**；二维码加密路径改为共用同一纯函数；**方案 B（二维码）按用户要求暂时禁用**（`ENABLE_QR_SECTION=false`，实现保留、一行恢复）。探针新增 `{"click":[…]}` 通用点击，并修掉"主窗口量测样本落盘成 `run-000`、按 run 号永远找不到"的夹具缺陷（我因此白排查了三轮）。测试 114 → **125** |
| 2026-09/16 第十四轮 | **挂件菜单删掉「没充值过」按钮：填 `0`/留空 + 确定 = 今天没充过（收起提示）** | 先读代码确认「确定」本来就无条件把当前余额传给 Rust、且水位前移不以金额为条件 → 填 0 与旧按钮**行为等价**，故是**纯 UI 变更、零行为损失**。顺手把水位前移规则抽成纯函数 `apply_recharge` 并加 4 项 Rust 单测，把"填 0 也要前移水位"钉成不变量（它是**唯一**的收起入口）；并清掉 `alerted`（早已删除的字段）在前端类型/测试 fixture/注释里的残留。Rust 单测 5 → **9**，前端 125 不变 |
| 2026-09/16 第十五轮 | **同步改「只走文件」：`[导入配置文件]`（选 .json）+ `[导出配置文件]`（把文件本身放进剪贴板）；添加站点默认名 `DeepSeek官方`；两按钮加图标（导入左/导出右、导出非蓝）** | 用户口径「加一个导入配置文件按钮放导出左边、删掉文本方案、只保留文件导入导出」+「能否直接复制文件」→ 选**把文件放进剪贴板**（粘出来就是文件）。删掉粘贴文本框与「从剪贴板导入」；导出=`buildExportFileText`（未加密=**标准 JSON**、加密=其 `AQM2:` 密文）+ Rust `export_config_file`（写 `%APPDATA%\...\export\` → Win32 **CF_HDROP**），不需要 dialog/fs 插件。按钮加图标（导入 `Download` 靠左、导出 `Upload` 靠右、都用次级样式不再蓝色），**主界面「导出/导入」图标同步换成 `Upload`**。因 DeepSeek 官方是类型列表第一项，打开「添加站点」即默认填入 `DeepSeek官方`（名字空着或仍是自动值时才改，切走则清空）。⚠️ **安卓端导出因此没有通道了**（CF_HDROP 是 Windows 专有，已记入 §8）。新 Rust 模块 3 项单测（文件名防穿越 / DROPFILES 布局 / 非 ASCII 路径）。Rust 单测 9 → **12**，前端 125 → **129** |
| 2026-09/16 第十六轮 | **设置页每行加「立体」分割线；开关文案改「启动时自动打开鲸鱼娘窗口(上次选择)」；设置标题字体统一；删掉壁纸提示语** | 分割线 = `border-top` 实线 + `box-shadow: 0 1px 0` 淡线（共 3 条：反馈行后 / 壁纸说明后 / 开关后），两色由主题变量给。⚠️ **浅色弹窗底色是纯白，白线画上去完全不可见**（截图取样：暗线行 229、相邻行 255）→ 浅色改成"上暗 + 下更淡"、深色才是"暗线 + 淡亮线"，两套做法都实测过像素。`.advanced-section` 自带的 border-top 已删（否则叠成三条）。开关文案把"打开的是上次那个站点"写进标签（实测宽 200px 单行不换行）。**设置标题**（主题/背景壁纸/开关标签）字重统一成 `600`（与「高级设置」同款；字号颜色本来就一致），用 `.settings-dialog` 作用域，不动全局 `.field-label`。**删掉提示语「选择主界面背景样式，立即生效」**（非自定义壁纸时不再有提示行；自定义时保留"可点选择图片替换"那句） |
| 2026-09/16 第十七轮 | **安卓导出落地（用户口径「AB 结合」）：点导出二选一 —— 系统分享 / 保存到本地** | Kotlin 插件 `ConfigExportPlugin.kt`（`ACTION_SEND` + FileProvider / `ACTION_CREATE_DOCUMENT`，保存结果走 `@ActivityCallback`）+ Rust `export::plugin()` 桥接（`run_mobile_plugin`，命令改 `async` 防死锁）→ **安卓端从此也有导出通道，且零存储权限**。**关键发现**：Tauri 安卓模板自带 FileProvider 与 `file_paths.xml`（`cache-path`/`external-path`）→ **免改 Manifest**，但分享的文件只能放 `cacheDir`/外部目录。⚠️ 踩坑：`use std::path::Path` 被 `#[cfg(windows)]` 过度门控 → 安卓目标编译不过（靠 `cargo check --target aarch64-linux-android` 抓出）；裸跑该命令还需把 NDK clang 接进 PATH/CC/AR，否则卡在 `ring` 的 C 编译。**未验**：安卓运行时（分享面板目标、保存是否落地）需设备实测 |
| 2026-09/16 第十九轮 | **「今日充值金额」面板改独立窗口（挂件正上方 + 右对齐）；删除「缩放后模糊」条目** | 面板原先内嵌在挂件内 → 挂件缩到 160×180 时被裁（用户反馈"显示不全"）。新增 `widget_menu.rs`（`menu_origin` 位置数学 + 4 项单测）+ `WidgetMenuApp.tsx`/`widget-menu.css`；高度由前端量测回报、**底边/右缘固定、向上生长**；由挂件 60Hz 轮询同步跟随（实测拖动全程跟随）；命中保护区 230×176 → **40×72**；真实鼠标点 ☰ 开/关均命中。**同轮**：用户确认"已经没有模糊问题" → §8 该待办与 §4.6 排查段、§11 两处表述全部删除 |
| 2026-09/16 第二十一轮 | **启动开关 → 「启动时自动打开桌宠模式」下拉框三选一**：关闭自动 / 上次站点 / 置顶站点（列表第一张卡） | 文案按用户口径改写；控件由滑动开关换成 `MenuSelect` 下拉；新增 `lib/autoWidget.ts`（`autoWidgetMode()` / `pickAutoOpenUid()` + **12 项单测**）承担三态归一与站点回退链；`config.autoOpenWidgetMode` 落盘、老布尔字段 `autoOpenWidget` 仍兼容读写。⚠️ 踩坑并修掉：给下拉根元素加 `min-width` 会让展开面板**看起来左对齐**（`right:0` 对齐的是根元素、不是按钮）→ 去掉定宽，实测「按钮右缘 == 面板右缘，差 0.0px」 |
| 2026-09/16 第二十二轮 | **安卓端更新 + 模拟器验证**（自动提权出包，全 ABI universal 106.8MB） | `tauri_avd` 是 **x86_64** 镜像 → 惯例的 `--target aarch64` 最小包在本机装不上，改用**不带 `--target` 的 universal 包**（4 ABI 全带）。流程：`pnpm build` 刷 dist → 提权 bat 跑 `pnpm tauri android build --apk`（`BUILD_EXIT=0`）→ apksigner 调试签名 → 桌面 `API余额通-安卓_260916.2048.apk` + 清旧包 → `adb install -r`（保留原有数据）。**验证**：MainActivity resumed、无 FATAL；主界面 7 站点真实余额（可用总额 ¥61.77）；安卓 `usageBaselines` 已迁移到新模型（`opening/last/debit/credit`）；设置页正常且**桌面专用那一行正确地不出现**。⚠️ 同轮修掉自己在 `widget_menu.rs` 引入的 6 条安卓 `never used` 警告（补 `#[cfg(desktop)]`），**仍有 6 条既有的**留在 §7.6 待清理 |
| 2026-09/16 第二十三轮 | **推送前审计：发现历史里的登录态压缩包 → 清洁重建仓库** | 审计跟踪文件时发现 `minmax.7z`（基线提交起就在历史里）内含 `minmax/web_state/minimax_web.json` —— 16 条 MiniMax cookie（含 `_sid`/`_token`），最长有效期到 **2027-10**，推送即公开。其余审计项全干净（无 `sk-`/`LTAI`/AccessKey、历史无 keystore、身份为 GitHub 匿名邮箱）。按用户选择执行：删 `minmax.7z` + `src-tauri/icons.bak/`（3.33MB）→ `git init -b main` 清洁重建（旧 `.git` 备份为 `D:\DeepSeek\api-quota-monitor-tauri-git-backup-260916.zip`）→ 复验对象库已无该 blob。**遗留**：用户须到 MiniMax 后台登出其它会话/改密码；根因教训（`.gitignore` 挡不住打进压缩包的副本）写进 §9 |
| 2026-09/16 第二十轮 | **「今日已用」换模型：消费只由「下降」构成**（充值彻底不再干扰）+ 开发期白屏定位与修复 | 观测账本（`opening/last/lastAt/debit/credit`）+ 定点累加 + 乱序样本丢弃 + 跨天/币种重建；旧模型的 `peak`/`maxSeen`/`recharged`/`set_recharge_amount`/`apply_recharge` 与「设置今日充值金额」提示、`.widget-status.recharge` **全部删除**；☰ 面板保留但暂空（用户要求留作后续功能入口）。**实测**：人为把 `last` 调低 10 → 刷新后 `debit` **原样不变**、`credit`=10 ✓；真实消耗精确累加（48.18→47.77 = `debit 0.41`）✓。另修：改前端导出后 dev **白屏**（旧/新模块在 WebView 缓存里混用）→ 清 WebView2 缓存 + 重启 dev（§10） |

详细记录：

| 日期 | 说明 |
|---|---|
| 2026-08（原版） | 初版交接文档 |
| 2026-08（本次） | 全面审查后更新：方案筛选/设置页/窗口几何记忆改「已完成」；新增 §4.5/§4.7/§4.8/§4.9 与新功能清单；修正挂件文字位置比例（9.5/20.5/34.5 → 17.5/29/40.5）与水平定位（45%→left 2%）；如实记录无 git 仓库、README 未更新、死代码（§8）；随后修复 `sync.test.ts` 2 项缺失 await 的测试，32 项全绿 |
| 2026-08（本次追加） | ①修卡片布局：`.site-card` 加 `min-width:0`（超长报错文本挤掉右侧按钮，仅 Windows 复现，Android 早已用 `minmax(0,1fr)`）②**移除 Sub2API 框架适配器**（多数 Sub2API 站点无统一余额接口，统一由 `generic_login_html` 接管）：登录探测路径加入 `/api/v1/auth/login` 并置首、组合顺序 json+email 提前（Sub2API 类站点 1 请求出余额）；修复探测失败 fallback 吞错 bug（不再用根地址 POST，直接报「未找到可用的登录接口」）；现有 `sub2api_framework` 配置迁移为 `generic_login_html`（`base_url`→`login_url`）；适配器 6→5，测试改「Sub2API 风格站点由通用识别接管」 |
| 2026-08（本次追加） | **新增 MiniMax 适配器（适配器 5→6）**：`/v1/api/openplatform/coding_plan/remains` 读 Token Plan 套餐剩余；2062 无套餐 → 信息性无数据（灰色破折号，非红色错误）；**「Token Plan 套餐」多计费方式自动识别**：登录 JSON 按 key 语义分类 plan/duration/times/paygo，`expires_in` 等过期字段排除在包时外（绿洲API 误判修复）；自动模式每次刷新自愈合并、手动保存退出；主余额优先按量组、套餐置首；**无数据三态**：空余额灰色「○」/错误红色「❌」；**Android UI 修复**：设置按钮右对齐、按钮图标居中、次级卡片溢出、卡片点击弹操作面板（站点链接按钮化）、「打开官网」桌面端显示；测试 32→36 全绿 |
| 2026-08（本次追加） | **新增阿里云适配器（适配器 6→7，`aliyun`）**：BssOpenApi `QueryAccountBalance`（端点 `business.aliyuncs.com`，`bssopenapi` 域名不存在）；**纯 JS 实现 RPC V1 签名**（SHA-1/HMAC/Base64，不依赖 Web Crypto）；RAM 子账号 AK/SK + `AliyunBSSReadOnlyAccess` 授权（未授权给 NotAuthorized 提示）；主余额=可用余额、详情行现金/信用；**0 值方案不自动勾选**（多计费字段为 0 = 未开通，不产出方案卡片）；**显示格式**：按次 `120次`、包时 `1小时20分钟15秒`（去掉 /次、/剩余，时长精确到秒）、概览卡剩余次数 0 → 横线；次卡数值字号调小、多方案间距压缩；DeepSeek 峰谷倒计时 1s → 10s 刷新（数据核对仍 3 分钟）；**主窗口置顶行为**：打开添加/编辑弹窗时置顶、关闭后恢复（启动抢焦点置顶改验证+重试防残留）；适配器注册顺序调整为 DeepSeek → 自动识别 → 阿里云 → 其余；测试 36→40 全绿 |
| 2026-08（本次追加） | **新增火山引擎适配器（适配器 7→8，`volcengine`）**：BssOpenApi `QueryBalanceAcct`（端点 `billing.volcengineapi.com`）；**纯 JS 实现 SHA-256/HMAC + 火山签名**（`Authorization: HMAC-SHA256 Credential=<AK>/<date>/cn-beijing/billing/request, SignedHeaders=host;x-date`——踩坑：Credential 漏带 scope 报 `InvalidAuthorization`，官方 GET 示例仅签 host;x-date 两个头）；子账号授权 `BillingCenterReadOnlyAccess`（只读余额）；主余额=可用余额、详情行现金/信用/冻结/欠费；**适配器顺序调整为 DeepSeek → 阿里云 → 火山引擎 → 自动识别 → 其余**；类型下拉去掉 `（id）` 显示（只显示名称）；「自动识别」名称去掉括号说明；**阿里云/火山引擎「接口地址（可选）」移入高级设置折叠区**；描述文案按用户口吻定稿（含密钥创建入口 + BillingCenterReadOnlyAccess 权限说明）；测试 40→42 全绿（火山签名校验 + AccessDenied 用例，假服务器用 node:crypto 重算签名比对）｜**一次性打包**：`tauri build --no-bundle` 出 27MB exe 放桌面；`tauri android build --apk --target aarch64`（admin）+ `apksigner` 配 `~\.android\debug.keystore`（pass:android）签名，31MB **arm64-only 最小 APK** 放桌面；打包前后停/启 win dev（cargo target 锁）｜**Android UI**：设置按钮移到大标题同行的右上角（绝对定位 + 标题右侧留白 78px，状态区留第二行）｜**液态玻璃按钮**（配方取自 `D:\yzrt-liquidglass`）：contrast(3) 压边 + backdrop blur + inset 翻边高光 + ::after 45° 扫光，应用于 ⚙️设置/卡片操作（完整液态）、主按钮（accent 保留+扫光）、次级按钮（完整液态）｜**悬浮提示收敛**：仅「导出/导入」「方案筛选」「展示窗口」保留 tooltip，其余（全部刷新/添加/卡片刷新/编辑/删除/打开官网）去掉 `data-tip`（tooltip.ts 钩子 `.tip[data-tip]` 不再命中）｜**设备间迁移**：讨论中（建议文件导入导出 + 分享面板，未实施，见 §8） |
| 2026-08（本次追加） | **清理待办（§8 一批清空）**：①**死代码清理**——删 `worker.ts refreshAll`（App 自实现逐站点刷新，导出无调用；`ProviderEntry` 等 import 保留复用）、`store.ts moveProvider`（前端从未调用，invoke 只在 store.ts 内）连同 Rust 侧 `config.rs move_provider` 命令与其 `lib.rs` invoke_handler 注册（拖拽排序早已改为整体 saveConfig）、`qr-decode.mjs` 调试残留（import pngjs 但 package.json 无此依赖、不可运行）②**残留清理**——删 `icon_sprbaoxtamr/`（旧图标素材目录）、`public/vite.svg`（模板 favicon，index.html 实际用 `/app-icon.png`，全仓无引用）③**Maple Mono 字体 woff2 压缩**（fonttools 4.63 + brotli 无损转换）：Regular 19.7MB→6.3MB、Bold 19.6MB→6.4MB，`theme.css` 两个 @font-face 改 `format('woff2')` 并删除源 TTF，dist 40.3MB→13.1MB（-67%），Windows exe/Android APK 体积同步下降，Android OTS 警告消除、视觉零损失（woff2 为 WebView2/Android WebView 标准支持格式，`font-display:swap` 兜底）④**README 确认已更新**（项目说明而非模板）⑤验证：`pnpm test` 42 项全绿、`tsc --noEmit` 通过、`cargo check` 通过、`pnpm build:vite` 产物正常（woff2 带 hash 进 dist/assets） |
| 2026-08（本次追加） | **DeepSeek 峰谷倒计时格式对齐包时 + 分级细化**：`fmtPeakCountdown` 改为分级显示——**≥2 小时不显示秒**（天级「2天1小时」同包时；小时级「23小时59分钟」，分钟为 0 时省略「2小时」），**<2 小时显示秒**（「1小时59分钟59秒」/「59分钟59秒」/「59秒」，复用 `fmtDuration`）；`SiteCard.tsx` 倒计时刷新节奏联动：剩余 **<2 小时 → 1 秒/次逐秒跳动**，**≥2 小时 → 10 秒/次**（`tick` 内动态重建 interval，`ReturnType<typeof setInterval>` 类型安全）；`deepseekPeak.ts` 新增 `import { fmtDuration } from './fmt'`（fmt 不依赖 deepseekPeak，无循环）；`fmtDuration`（包时卡显示）**保持不变**；`tsc` 通过，阈值验证：178752→「2天1小时」、86399→「23小时59分钟」、7200→「2小时」、7199→「1小时59分钟59秒」、59→「59秒」 |
| 2026-08（本次追加） | **dev 与桌面打包 exe 并存互不干扰**：根源是 dev 与打包版共用 identifier `com.apibalance.monitor` → ①`tauri-plugin-single-instance` 基于 identifier 的 Windows 互斥锁（后启动一方退出聚焦前者）②`app_config_dir()` = `%APPDATA%\com.apibalance.monitor\` 共用 config.json（跨进程无写锁，互踩覆盖）。方案：Tauri CLI 无自动 dev 覆盖文件（`tauri.dev.conf.json` 不自动加载，需 `-c/--config` 显式合并），新增 `src-tauri/tauri.dev.conf.json`（identifier=`com.apibalance.monitor.dev`、窗口标题「API余额通 (dev)」）+ package.json script **`pnpm dev:app`**（=`tauri dev --config src-tauri/tauri.dev.conf.json`）→ dev 用独立单实例锁 + 独立数据目录 `%APPDATA%\com.apibalance.monitor.dev\`；正式 `pnpm tauri build` 不带 --config 不受影响，桌面 exe 保持 `com.apibalance.monitor`（既有数据零影响）。验证：dev 与桌面 `API余额通.exe` 双实例同时存活、dev 读 .dev 目录（`window restored to 436.8x600` 默认尺寸证明未读正式配置）。注意：今后启动 dev 用 `pnpm dev:app`，若再跑裸 `pnpm tauri dev` 仍会与正式 exe 共享；「撤回 yzrt-liquidglass 全部改动」与本改动同日完成（液态按钮/fx-layer/容器玻璃框/胶囊实验全部还原为接手时状态）**；随后用户确认「彻底无液态」——设置按钮/卡片四按钮/弹窗次级按钮/主按钮的液态配方（contrast(3) 压边、::before 水珠内沿、::after 45° 扫光、inset 翻边高光阴影）**全部移除**，回归朴素毛玻璃/纯色按钮（设置：chip-bg 底+blur+border；卡片：overview-card-bg+blur+border，hover 保持用户「去蓝」白底+加深字+上浮；次级：chip-bg+blur+border+hover 上浮；主按钮保留 accent 实底+右上微高光阴影，删除扫光伪元素） |
| 2026-08（本次追加） | **图标抓取（Rust `fetch_site_icons`）**：抓首页 HTML 提取 `<link rel=icon>`（rel 属性级判断）/ 含 logo 的 img·href（排除 logout）/ og:image；相对地址 absolutize；空兜底 `/favicon.ico`；前端 JS `Image()` 预加载探测（naturalWidth>0 才显示，img `referrerPolicy="no-referrer"`）｜**「为何显示API三个字」修复**：generic_login_html 不在 MARKS → 兜底取 displayName 前 2 字符（minimax 删除后遗留）+ 移除失效 DDG favicon 源（假 200 HTML）｜**死锁 bug 修复**：img 渲染条件是 `favOk===true` 但只有 img 回调能变 → 永不渲染；改为 JS Image 预加载探测成功才渲染｜**favicon 候选误抓修复**：`logoish` 排除含 `logout` 的 URL（max66 `/user/logout` 被误当 logo）｜**桌面图标**：复制 `src-tauri/icons/icon.png`（512×512）到桌面「API余额通-图标.png」｜**minimax 研究结论**：官方站账号密码登录必须浏览器自动化（SPA + OAuth2 SSO + 可能滑块），HTTP 无解；官方文档仅 API Key 认证 |
| 2026-08（本次追加） | **「模拟浏览器登录」兜底适配器 + 集成**：①`minmax/query_balance.js`（通用单次查询脚本：参数化 loginUrl/balanceUrl/balanceKeyword/username/password + 登录态按 host 存 `web_state/browser_<域名>.json`，兼容旧 `minimax_web.json`；stdout 输出 JSON `{success, message, balance?}`）②Rust `browser_login_query` 命令（spawn 一次性 `node` 进程，90s 超时 kill，解析 stdout JSON；Android 端 `cfg!(android)` 返回「仅支持桌面端」）③前端 `browser_login.ts`（配置：登录页/余额页/账号/密码/接口关键字）④`SiteDialog` Android 平台隐藏该类型｜**「自动识别」改名「自动识别(中转站用这个)」**｜**站点类型排序**：DeepSeek → 自动识别 → 阿里云 → 火山引擎 → Code AI → 其余（多次调整，§4.3/§9 同步）｜**一次性打包**：`tauri build --no-bundle` → 桌面 `API余额通.exe`；安卓 UAC 构建 + apksigner 签名 → `API余额通-安卓.apk`（arm64 单 ABI 28.8MB） |
| 2026-08（本次追加） | **OpenCode 研究 + 集成**：Go 用量官方未文档化接口 `GET /zen/go/v1/usage`（Bearer，5h/周/月用量百分比，社区 cc-switch 背书）；Zen 预付费余额无官方 API（feature request 仍 open）→ auth cookie + workspace id 抓 dashboard HTML 提取 `$X.XX`（纯 HTTP，无需浏览器自动化）｜**CommandCode 研究 + 集成**：官方 CLI 同款 `/alpha/*` 接口（whoami → billing/credits + subscriptions，CLI 指纹头 `x-command-code-version`/`x-cli-environment`）｜**Code AI 合并入口**：OpenCode + CommandCode 一个适配器（`code_ai.ts`），配置首字段 `service` **下拉选择**（`ConfigField` 新增 `type:'select'` + SiteDialog 下拉渲染）；旧 `opencode` id 保留别名；删除独立 `opencode.ts`｜**登录 Token 缓存机制**（`generic_login_html`）：`_cached_token`/`_cached_token_at` 持久化 + 30 分钟 TTL（`token_ttl_minutes` 可配）+ 401/403 自愈 + 409 `AUTH_SESSION_LIMIT` 自动登出清理与明确报错｜**壁纸替换修复**：CSS 变量 URL 加 `?t=Date.now()`（WebView2 缓存旧图）；「选择图片」按钮与背景选择同一行（`.wallpaper-row`）｜**安卓设置按钮下移**：不再绝对定位贴右上角，与站点数量（summary）同一行（title-right 流内）｜**worker 币种白名单加 `%`**（配额百分比不被强转 CNY）｜**打包脚本 `打包.ps1`**：产物自动带时间戳命名（`API余额通_yyMMdd.HHmm.exe` / `-安卓_<时间戳>.apk`），`-Android` 全自动（UAC→构建→签名）；测试 42→56 全绿 |
| 2026-08（本次追加） | **「模拟浏览器登录」脚本补全 + 实测通过**：minmax 目录此前只有旧独立版（server.js HTTP 服务，MiniMax 专用），Rust `browser_login_query` 引用的 `minmax/query_balance.js` 缺失 → 适配器是断的。已重建通用版脚本（参数化 loginUrl/balanceUrl/balanceKeyword/username/password，与 `BrowserLoginParams` camelCase 契约一致；MiniMax host 特判复用旧版已验证流程，其它站点通用选择器启发式；登录态按 host 存 `browser_<域名>.json` 且兼容复用旧 `minimax_web.json`；stdout 输出 `{success,message,extra,balance}`，调试日志走 stderr；参数支持 JSON 字符串或文件路径）｜**真实账号实测通过**：①复用旧 minimax_web.json 登录态直连 → 成功（无需登录）②备份后删登录态强制全新登录 → 成功（storageState 落盘 `browser_platform.minimaxi.com.json`）③复测复用新登录态 → 成功；修复「goto 超时误判登录态失效导致无谓重登」逻辑（仅确认在登录页才判定失效，页面已加载但无接口则直接报错）；测试账号查询结果：可用 0 / 现金 0 / 赠金 0 / 信用 0 / 欠费 0（新账号未充值） |
| 2026-08（本次追加） | **「模拟浏览器登录」App 端到端打通（连修 4 个隐藏 bug）**：①**Rust 脚本路径 `../../minmax` 跳两级 bug**（`src-tauri/../../minmax` = `D:\DeepSeek\minmax` 不存在；正确 `../minmax`）——接口从一开始就是断的，独立跑 node 脚本绕过 Rust 路径解析所以此前未暴露 ②**`canonicalize()` 返回 `\\?\` verbatim 前缀**，node 解析报 `lstat 'D:'`——剥离前缀后再传 ③**Rust 90s 超时**对最坏路径（直连失败→重登）可能误杀，放宽 180s ④**MiniMax 新版登录页（`account.minimaxi.com/unified-login`，旧 `platform.minimaxi.com/login` 已弃用）适配**：默认手机号验证码登录 tab → 切「账号密码登录」；必须勾选协议（自定义 button 勾选框，「我已阅读并同意」文案行，不勾选点登录无反应）；**登录成功判定改监听 `/oauth2/login` 响应 `code===0`**（无头下 SPA 不跳转 URL，原 URL 轮询必超时；响应后前端 JS 写会话 cookie，直接手动 goto 余额页）；错误探测正则去裸「验证码」（页面「邮箱验证码登录」tab 文本误判）；`打包.ps1` 构建后自动复制 minmax 到 exe 旁（剔除 web_state 登录态）。实测：全新登录 11.1s / 复用登录态 4.9s 均成功，`browser_account.minimaxi.com.json` 落盘 |
| 2026-08（本次追加） | **零依赖分发增强（方案 B）**：`minmax/` 放入 **node.exe（node v24.19.0 win-x64 单文件，92.8MB，npmmirror 下载官方 zip 提取）**；Rust `browser_login_query` 优先用 **exe 同级 `minmax/node.exe`**（打包版目标机免装 Node），无则回退 PATH `node`（dev 不受影响）；`打包.ps1` 全量复制 minmax（自动带走 node.exe，仅剔除 web_state）；随包 node.exe 实测跑通完整链路（8.4s 复用登录态出余额）。分发物：exe ~25MB + minmax ~111MB（node.exe 92.8MB + playwright 17.7MB）≈ 136MB；目标机仅需系统 Edge（Win10/11 标配）+ WebView2 + VC++ 运行库（系统普遍已装） |
| 2026-08（本次追加） | **方案 A 假单文件落地**：`打包.ps1` 构建前用 .NET ZipFile 把 minmax 全量压成 `src-tauri/runtime.zip`（38.8MB，node.exe 93MB→37MB 高压缩）；`network.rs` release 配置 `include_bytes!` 嵌入 + `ensure_runtime()` 首次调用解压到 `%APPDATA%\com.apibalance.monitor\runtime\`（zip-slip 防护、幂等，登录态 web_state 可写）；运行时目录三级优先：exe 旁 minmax（便携分发兼容）→ 内嵌 runtime → dev 项目路径；node.exe 取运行时目录内自带（否则 PATH 回退）；`browser_login_query` 注入 `AppHandle`（需 `tauri::Manager` trait）拿 app_data_dir；Cargo.toml 加 `zip = "2"`（default-features=false + deflate）；release 集成测试 `cargo test --release --lib` 通过（解压 + 幂等断言）——**最终分发物：单个 exe ~65MB（exe 25MB + 内嵌 38.8MB zip），目标机仅需系统 Edge** |
| 2026-08（本次追加） | **OpenCode Go 三窗口展示 + Go 徽标**：`code_ai.ts` 的 Go 用量 extra 改为 `\n` 三行逐窗口（**5h → 周 → 月**，每行 `窗口 剩X% · 已用X% · 重置MM-DD`）；`.card-detail` 由单行省略改为 `white-space: pre-line` 多行（任意适配器用 `\n` 分隔 extra 自动获得多行能力）；**次级卡/安卓方案卡补详情行**（`.plan-subcard-detail`，plan-subcard 加 flex-wrap）——Zen 主卡时 Go 次级卡三窗口也能看到；**Go 徽标**：Code AI + service=opencode + 填了 API Key 时，站点名称右侧显示青蓝 `Go` 小标（仿 `.ds-peak`）；**概览「可用总额」排除 `%` 配额数据**（`App.tsx totalByCurrency` 跳过 currency='%'，配额百分比不是金额，不再混入顶部可用总额）；无订阅时曾用临时 mock（`OC_GO_MOCK`）验证 UI 效果后已移除；测试更新断言（三行 + 各窗口剩/已用/重置），56 项全绿 |
| 2026-08（本次追加） | **桌面 + 安卓重新打包验证（含两个修复）**：①**隐私修复**——`runtime.zip` 生成改为「临时干净目录」方式，**剔除 `web_state/` 登录态**（原直接压 minmax 全量会把 MiniMax 站点 cookie 打进 exe 分发）；已重新生成干净的 runtime.zip（37MB）并重新构建桌面单文件 exe `API余额通_260830.1138.exe`（62.2MB）替换桌面旧版（旧 26MB 老包已删）②**打包.ps1 编码修复**——无 BOM 的 UTF-8 中文脚本在 PowerShell 5.1 下解析错乱（引号错位导致后半段源码被当输出、构建中断），已转 UTF-8 BOM ③**安卓端更新**：`pnpm build` 刷新 dist → UAC 管理员构建（`tauri android build --apk --target aarch64`，BUILD_EXIT=0）→ apksigner 签名 → 桌面 `API余额通-安卓_260830.1143.apk`（28.8MB）→ 模拟器 tauri_avd（Android 15）验证：安装成功（先卸载旧签名包）、启动正常（topResumedActivity=MainActivity）、截图确认 UI 完整渲染无崩溃、安卓窄屏适配良好（截图 `D:\DeepSeek\aqm_android_main.png`）；安卓端配置独立（全新空状态，OpenCode 三窗口等效果需在安卓端添加站点后查看） |
| 2026-08（本次追加） | **安卓端导入桌面站点数据（含重要路径发现）**：⚠️ **安卓 `app_config_dir()` = `activity.dataDir` = 应用根目录 `/data/data/<包名>/`**（tauri-android `PathPlugin.kt` `getConfigDir` 直接返回 dataDir，**不是 files/**）→ config.json 在 `/data/data/com.apibalance.monitor/config.json`；导入方法：`adb root` → 合并 dev+正式配置（dev 优先、同 uid 去重、**剔除 browser_login 站点**——安卓不支持浏览器登录）→ `adb push` 到该路径 → `chown u0_a217:u0_a217` + `chmod 600` → force-stop + 重启；模拟器验证：**7 站点全部加载并实时刷新出余额**（DeepSeek ¥49.03 / Poke API ¥2.87 / Max66 ¥8.79 / 绿洲API ¥4.99 / 阿里云百炼 ¥8.96 / 基元律动 ¥0.01 / MaoMaoAI 无数据黄标），概览可用总额 ¥74.65 合计正确，无报错白屏；临时含密钥配置文件已清理 |
| 2026-08（本次追加） | **安卓端「模拟浏览器登录」方案 A 落地（内嵌 WebView 手动登录）**：**Kotlin**——`gen/android/.../WebLoginPlugin.kt`（`@TauriPlugin` 自动注册）：`openLogin` 打开全屏 WebView（顶部操作栏 取消/完成），用户手动登录（验证码/滑块真人可过），`shouldInterceptRequest` 拦截含 `balance_keyword` 的接口请求 → `HttpURLConnection` 带 Cookie 重发读响应体 → 递归解析余额字段（字段别名与桌面 `query_balance.js` 一致）→ 返回 `{success,message,balance,cookie}`；**登录态由系统 `CookieManager` 持久化**（应用级，重启不丢）；**Rust**——新模块 `src/web_login.rs`（`register_android_plugin("com.apibalance.monitor","WebLoginPlugin")` + `run_mobile_plugin("openLogin")`，`WebLogin<tauri::Wry>` 状态），`browser_login_query` 安卓分支改为走 WebView 流程（桌面分支不变）；**前端**——`SiteDialog` 安卓不再隐藏 browser_login 类型，适配器描述更新；**验证**：APK 重签名安装 → 模拟器配置加入 MiniMax 站点 → 自动刷新触发 → **截图确认 MiniMax 登录页在应用内嵌 WebView 完整弹出**（顶部操作栏正常，无白屏）——桥接+WebView 链路全通；**待用户实测**：模拟器内手动登录 → 点「完成」→ 抓 `query_balance` 接口返回余额；后续可用返回的 cookie 走纯 HTTP 查询（CookieManager 持久化，无需重复登录）；截图文件使用完成后已按用户要求删除 |
| 2026-08（本次追加） | **①node.exe 黑窗口修复（桌面）**：Rust spawn node.exe（控制台子系统）时 Windows 会新建黑窗口 → `creation_flags(0x08000000)`（`CREATE_NO_WINDOW`，`std::os::windows::process::CommandExt`，仅 `#[cfg(windows)]`）静默后台运行，无感 ②**MiniMax login_url 必须带 `login_redirect` 参数**（简化 `unified-login` 会报「缺少重定向URL，请返回并重试」）——SiteDialog 填 `https://account.minimaxi.com/unified-login` 时需从浏览器地址栏复制完整 URL ③**Kotlin `shouldInterceptRequest` 关键字留空时用默认列表**（query_balance/get_balance/account_balance/available_amount/balance_info/wallet/quota//balance，对齐桌面 query_balance.js），用户不必填 balance_keyword ④lib.rs builder 加 `#[cfg_attr(mobile, allow(unused_mut))]`（桌面 cfg(desktop) 块需重新赋值）。模拟器验证：带完整 login_url 的 MiniMax 登录页正常加载无报错；桌面 CREATE_NO_WINDOW 需重打包 exe 后生效 |
| 2026-08（本次追加） | **安卓 WebView 登录 v2（修 3 个实测问题）**：①**来回闪跳**——用户点「完成」时自动 `loadUrl(balanceUrl)` 与页面自身 OAuth 回调冲突；改为**自动流程**：`onPageFinished` 检测 URL 离开登录页（含 login/signin/account.minimaxi/unified-auth）→ 自动跳余额页 → 抓到接口 800ms 后**自动完成**（无感，无需点完成）②**按钮不好用/多 WebView 叠加**——应用自动刷新（30s tick）会重复触发 `openLogin` 叠开多个 WebView；加**单实例保护**（`WebLoginPlugin.activeView` 静态标记，已有登录页时 reject「已有登录页面在打开中」）③**返回后获取不到**——完成按钮仅作兜底（已捕获才返回，否则明确提示接口关键字）；顶部栏文案改「站点登录 · 登录成功将自动完成」。模拟器验证：单个登录页无叠加、页面完整、无闪跳；**完整登录（输账号密码→自动跳余额页→自动抓取完成）待用户实测** |
| 2026-08（本次追加） | **安卓 WebView 登录 v3（完整闭环 + 3 个构建/解析修复）**：①**首屏直连余额页复用登录态**——`loadUrl(balanceUrl)` 而非 loginUrl：有 CookieManager 持久化登录态时直接加载余额页抓取自动完成（不再每次回登录页）；无登录态被重定向回登录页，用户登录后 OAuth 链自动回余额页 ②**DOM 提取兜底**——`shouldInterceptRequest` 接口拦截未命中（MiniMax 新版接口/SSR）时，`evaluateJavascript` 读 `document.body.innerText` + 正则提取（可用额度/现金/代金券/授信/欠费，中英文标签，本地验证全命中）；**关键修复：evaluateJavascript 返回值用 `JSONObject` 解析会因页面 Unicode 字符抛异常被吞 → 改 `encodeURIComponent` 纯 ASCII 通道（`JSON.stringify(encodeURIComponent(innerText))` → Kotlin `JSONTokener` + `URLDecoder.decode`）**；SPA 渲染晚于 onPageFinished，2s 间隔最多重试 8 次 ③**R8 构建锁**——`classes.dex` 被 Defender 实时扫描锁定反复失败 → **`isMinifyEnabled = false`**（无混淆需求）+ **Defender 排除 Android build/target 目录**（UAC `Add-MpPreference`）。**模拟器实测通过**：自动刷新 → WebView 直连余额页 → DOM 提取（可用额度 ¥0.00/现金/代金券/授信/欠费）→ **自动完成返回主界面，MiniMax 卡片 ¥0.00 ✅，8 站点全显示**（日志 `Log.d("WebLogin", ...)` 诊断保留） |
| 2026-08（本次追加） | **打包产物清理**：`打包.ps1` 安卓段签名成功后自动删除桌面旧 `API余额通-安卓_*.apk`（只保留本次产物，用户要求每次构建后清理）；已清理桌面历史 6 个旧 APK |
| 2026-08/31（本次追加） | **剪贴板导出压缩（同步体积优化）**：新增 `sync.ts buildClipboardPayload()`——**紧凑 JSON（schemaVersion=2 短字段）→ deflate 压缩 → `Z1:`base64url 文本**，替代旧版标准 JSON 全文导出；`SyncDialog` 剪贴板导出（明文 + 口令加密都先压缩）改用它，加密路径先压缩再 AES-GCM；**实测 4 站：3143 → 551 字符（-82%）**，10 站约 1400 / 20 站约 2800 字符（QQ 5000 字内）；导入端 `parseAnyImportPayload` 自动识别 `Z1:` 前缀解压，**新旧格式（明文/加密/标准 JSON/紧凑）全部兼容**；导出成功提示显示字符数。**容量实测**：每站平均紧凑文本 ≈203 字符、deflate 后 ≈103 字节；二维码（byte 模式 L 纠错 v40 上限 2953 字节）**约可容纳 28 站**，超 10 站建议剪贴板、30+ 站建议文件导入导出（§8 待办仍未实施） |
| 2026-08/31（本次追加） | **①主窗口关闭退出修复（方案 A，修「关闭残留」）**：挂件窗口 `prevent_close()+hide()` 常驻 → 主窗口关闭后 Tauri「全部窗口关闭才退出」不满足 → 进程残留任务管理器；主窗口 `CloseRequested` 保存几何后 **`api.prevent_close()` + `app2.exit(0)`** 显式退出（`CloseRequested` 模式需解构 `{ api, .. }`）②**node 子进程残留清理**：`network.rs` 全局 `ACTIVE_NODE_PID`（`record_node_pid`/`clear_node_pid`/RAII `NodePidGuard` 覆盖所有 return）+ `lib.rs` `.build().run(\|_,e\| RunEvent::Exit → kill_active_node())` → Windows `TerminateProcess`（`Cargo.toml` 加 `[target.'cfg(windows)'.dependencies] windows-sys = "0.52"` features `Win32_System_Threading`+`Win32_Foundation`；其它平台 std kill）③**安卓 MiniMax 无感刷新（v4）**：`LoginWebView` **不再立即显示**——有登录态时后台加载余额页（界面隐藏）抓取自动完成，仅被重定向回登录页时 `attachIfNeeded()` 才弹出（修复「点刷新就弹钱包页」）；**刷新兜底**——DOM 提取 8 次重试耗尽 → `finish(success:false, …)` 返回失败 + **45s 整体超时**（修复「一直刷新中」= invoke 永不返回），`finish` 加 Log 诊断 ④**安卓端表单精简**：`ConfigField` 新增 `desktopOnly`，`browser_login.ts` 的 username/password 标记之，`SiteDialog` 安卓端隐藏（普通/高级区）+ 校验/保存跳过——安卓为 WebView 手动登录，账号密码字段多余；登录页/余额页地址仍必填 |
| 2026-08/31（本次追加） | **打包/构建经验补充**：①**`打包.ps1` 用 edit 工具修改会丢 UTF-8 BOM** → PowerShell 5.1 按 GBK 解析中文乱码、后半段源码被当字符串输出、构建中断——**改完必须用 pwsh 脚本写回**（`[IO.File]::ReadAllText(UTF8)` + `WriteAllText(UTF8Encoding($true))`）②安卓构建 **`classes.dex` 被 Defender 实时扫描锁定**反复失败（R8/mergeDex 都中招）→ `build.gradle.kts` release **`isMinifyEnabled = false`** + Defender 排除 `src-tauri\gen\android` 与 `target` 目录（UAC `Add-MpPreference`）③桌面旧 exe 删除可能被系统权限/Defender 锁定（Access denied，普通+管理员删除均失败），需重启后手动清理 |
| 2026-09/02（本次追加） | **挂件悬停滚轮缩放**（用户需求清单第 6 条）：`WidgetApp.tsx` 新增 window `wheel` 监听（passive）——滚上放大/滚下缩小 ≈10px/格（`deltaY×0.1` 取反，单事件限幅 ±50 防平滑滚动突变，deltaMode 行模式 ×16 兜底），固定左上角、1:1 正方形（对齐边缘缩放），范围 180~800（文字缩放 2x≈616px 封顶）；**目标尺寸 JS 侧累计**（`target` 变量：`setSize` 异步落地有延迟，逐次读 `outerWidth` 会吞档——实测 5 格只涨 24px 的坑），滚轮停歇 400ms 后的 resize 事件才重置累计基准（兼容边缘缩放等外部尺寸变化）；停止滚动 300ms 后防抖 `widget_save_size` 持久化；拖动/边缘缩放进行中不响应（`resizeRef`/`pressRef` 判定）。验证：dev 实例 + 真实滚轮输入实测放大 203.2→331 落盘 ✓，tsc + 56 测试全绿。⚠️ 测试经验：**Win32 `mouse_event` 合成滚轮事件在 WebView2 上不可靠**（DPI 虚拟化：非 DPI 感知进程坐标被缩到 1536×960 空间，注入位置全偏；声明 `SetProcessDpiAwarenessContext(-4)` 修正后点击可命中但合成滚轮仍可能被 WebView 输入管线吞掉），自动化验证滚轮用真实输入或 `eval` 直调 JS 为宜 |
| 2026-09（本次追加） | **挂件滚轮缩放重构为鼠标焦点版（连续丝滑方向，多轮抖动修复）**：①**锚点数学移至 Rust**（`widget.rs widget_set_bounds(mouse_x,mouse_y,target_w,target_h,first)`，返回落地边长；ZOOM_ANCHOR 手势首帧缓存 P0/S0；`SetWindowPos` 原子设置位置+尺寸，Cargo.toml 加 `Win32_UI_WindowsAndMessaging`；`widget_save_size` 改由 Rust 读真实位置）——不读前端位置值（WebView2 高 DPI 读数差 1000px → 漂移根源）②**内容层固定 308 画布 + CSS transform scale**（`widget.css .widget-canvas`、`canvasRef`、`syncCanvasScale`）——消除 vw/cover 每帧重排滞后（内容抖根源），GPU 合成器缩放 ③**Promise 链串行泵**（`wheelStep` 递归：落地真实值作下一步基准、自适应步长 10/6/3px、画布由 resize 同帧同步）④**模块级单例监听**（`widgetRefs` 注入 + handler 引用存 `window.__dshWidgetWheel` 跨 HMR 幂等）根治双监听 ⑤踩坑全记录（漂移/双监听/IPC 风暴/乐观回退震荡/canvas 预置时机/诊断 eprintln 阻塞/窗口复用不重载）见 §4.6「挂件滚轮缩放实现说明」。验证：落地 req vs got 差 <0.5px、位置单调无回退；残余抖动为 125% DPI 物理量化 + 双合成路径亚像素错位（幅度显著缩小，用户验收待定） |
| 2026-09/11（本次追加） | **挂件滚轮缩放：建立客观量测 + 真实根因定位 + 显著改善（推翻上一版的根因结论）**。①**新建量测夹具 `probe/`**（vite serve 注入探针 + DPI 感知屏幕捕获 + 一键触发脚本）：把挂件临时换成品红底 + 锚点黑方块，用屏幕像素逐帧量出「锚点真实屏幕位置」——把抖动从主观观感变成可比数字（详见 §4.6「量测夹具」）。②**实测推翻旧结论**：旧文档称"窗口层数据已证完美、残余是 125% DPI 亚像素错位"——实测显示内容层与窗口层**每帧严格一致**（`画布scale×308 == innerWidth` 恒成立），真正根因是**窗口几何被 DWM 即时应用、而内容层 transform 要等渲染进程下一帧提交**，两者错位一帧 → 锚点被拖回一步；**偏移量 ≈ 0.4 × 单帧窗口位移**（实测标定：单帧 1px→低于噪声底、3px→2 物理像素、9px→5 物理像素）。③**解法**：rAF 逐帧泵（替代 Promise 链，使尺寸变化规整）+ **单帧位移钳制** `MAX_STEP=2`/`STEP_FRAMES=6` + 步长基准改**前端浮点游标**（回读落地取整值会把 1.25px 步长顶到 2px，抖动同比放大）+ 每帧把内容层与窗口几何放进同一任务发出。④**实测结果**（屏幕捕获、亚像素质心、单位物理像素）：快速滚动场景抖动 3.94→**0.87** 均值、5.0→**1.0** 峰值（**4~5 倍**）；标准场景 0.98→**0.64**、峰值 2.0→1.42；残余 ≤1.4 物理像素。⑤**修复连带 bug（重要）**：`resizeHandler` 的「距上次滚轮 >400ms 清 pending」阈值是给旧快泵定的，单帧位移钳小后会把 600~900ms 的动画**腰斩**（实测目标只走到 29%，且表现为"手感不灵敏"而非报错）→ 改为 `if (wheelPumping) return`。⑥**过程中自查并纠正三次自身错误**：一是早期 A/B 样本受"窗口漂移跑出捕获区域"污染（量到被裁剪的方块）得出过错误的 5.3 倍结论，修正夹具（每轮窗口位置归位 + 分析强制校验裁剪）后重测才得到可靠数字；二是内容层与窗口几何"写入顺序"经 n=3 对照证明**无影响**（原以为是修复本身）；三是把 `landed` 取整后当前端基准反而**放大**了有效步长（1.25→2px），改用浮点游标才兑现钳制效果。⑦**顺带修正**：`widget.rs` 位置数学改用前端请求值（原用落地取整值会引入 0.16px 系统性偏移）、`cur_w`/`anchor.size` 统一取整到 CSS 整数像素、`target_h` 未使用警告消除（1:1 正方形改取 `max(w,h)`）。⑧**新增待办**：登录态凭据裸放项目目录、PowerShell 7 未安装导致文档指引失效、建库前忽略规则缺失（见 §8）。验证：`tsc` 0 错、`pnpm test` 56/56、`cargo check` 无警告、`pnpm build` 生产构建无探针注入 |
| 2026-09/11（本次追加·同日第二轮） | **挂件缩放"既快又不抖"（用户追问"动画能变快吗"）**。上一轮的解法（压小单帧位移）换来平滑但动画变慢（+100px 要 932ms），属于"速度换平滑"的二选一。本轮顺着已测出的机制**反问**：既然上屏的内容总是落后窗口几何一帧，那就**反过来让窗口几何晚一帧发布**，使两者在同一呈现帧里取自同一步 → 错位从根上消失，于是步长可以放大回去。①实现 `GEOM_DELAY_FRAMES=1`（几何用上一帧的内容尺寸；内容层最后一步追平后补发一次几何再收尾，否则窗口永远差一步）。②实测（n≥3 有效样本，物理像素）：**快速滚动 抖动均值 0.87→0.285、时长 932ms→399ms；标准 0.64→0.286、666ms→566ms**；对照最初旧策略（3.94 均值 / 5.0 峰值 / ~300ms）为 **13.8 倍抖动改善、峰值 9.4 倍，且动画仅慢 33%**。③步长旋钮定为 `MAX_STEP=6`/`STEP_FRAMES=4`。④**已知副作用**：延后期间内容层比窗口大 ≤6px → 右/下边缘被裁一条（用探针逐帧数据验证：`内容cs*308 − 窗口iw` 在 0~9px 间波动）；可见框仍由窗口驱动平滑增长，实测幅度下不易察觉；彻底消除需 Rust 分离「锚点尺寸」与「窗口尺寸」并让返回按钮相对内容定位（已记入 §4.6，未实施）。⑤**量测可信度教训**：本轮 10 份捕获有 3 份被污染（静止底 8~50px，疑为人为操作挂件或前台争夺），**"静止帧噪声底"是识别废样本的有效指标**，分析脚本必须先看它再看抖动数字。验证：`tsc` 0 错、`pnpm test` 56/56、`cargo check` 无警告 |
| 2026-09/11（本次追加·同日第三轮） | **挂件加 DeepSeek 峰谷徽标**。①**峰谷徽标**：用户要求「在 DeepSeek 官方余额的挂件小窗站名上面加个峰字，像主页面站点卡片那个一样」→ 复刻 SiteCard 的 `.ds-peak`（`deepseekPeakNow` + `DEEPSEEK_PEAK_INFO`，仅 `deepseek_official` 显示、10 秒刷新），落在站名行上方 `.widget-peak`。落点用**背景图亮度图**确定气泡内区（约画布 y 18~41px，站名框上缘 43px），再用探针的**布局报告**验收：徽标占 20.1~40.9px、与站名间距 2.2px 无重叠、水平中心与站名列完全对齐、所在背景亮度 255。随后按用户要求把字号 10px→13px、16px→19px。②**工具**：探针新增**布局报告**模式（`{"layout":true}`：回传画布内各元素几何 + 背景图对应位置亮度采样 + 可运行时切换 `setSize`/`setWillChange`/`realLayout`）——用像素数据替代眼睛核对 UI 落点。验证：`tsc` 0 错、`pnpm test` 56/56、`cargo check` 无警告、`pnpm build` 通过 |
| 2026-09/11（本次追加·同日第四轮） | **挂件空白处点击穿透**（用户需求）。背景图实测 **47.8% 像素完全透明**（鲸鱼娘是异形角色），但窗口是矩形 → 那近一半空白会吃掉鼠标、挡住后面的窗口。①**实现**：动态 set_ignore_cursor_events + Rust 侧 60Hz 全局光标轮询 + 前端上传的 128×128 命中遮罩；**不用 SetWindowRgn**（硬边会把图片抗锯齿边缘切出锯齿，且每次缩放要重建区域）——本方案只改命中性，渲染零影响。②**遮罩**由前端从 alpha 通道降采样生成（Rust 读不到前端打包资源），块内平均 alpha + 低阈值 ≈ 膨胀：宁可多捕获，也不能把本体判成空白；实测实体格 8833/16384 = 53.9% 实体 / 46.1% 穿透。③**关键坑**：返回按钮 .widget-back 实测**压在透明区上**，只按遮罩判定会点不到、关不掉挂件 → Rust 侧把它的固定占位始终算作实体（已写进 §4.6）。④**安全兜底**：遮罩缺失/越界/加锁失败一律按实体（不穿透）、全空遮罩直接拒绝、窗口隐藏时复位穿透态。⑤**验收**（脚本把光标移到指定点后读 widget_hit_state）：空白处穿透=true、本体=false、返回按钮=false（保护生效）、按钮下方空白=true、窗口外=false，五项全过。验证：	sc 0 错（**本轮起 tsconfig 的 include 加入 probe**——此前探针不在类型检查范围内，这是个隐患）、pnpm test 56/56、cargo check 无警告、pnpm build 通过 |
| 2026-09/11（本次追加·文档完善轮） | **交接文档系统性完善（无代码改动）**。①**新增 §0「接手须知」**：当前状态、起 dev 必须用 `pnpm dev:app` 的原因、PowerShell 5.1 限制，以及三条用血换来的方法论（先建客观口径再改代码 / 量测口径本身要先验证 / 别在「操作尚未发生」的稳态下做 A/B）。②**修正错误指引**：全文 `pwsh -File 打包.ps1` → `powershell -File`（本机**没有 pwsh 7**），并把 BOM 写回改成 5.1 下可直接照做的 .NET API 写法（含绝对路径坑）——原指引在本机根本执行不了。③**修正结构错误**：§7 小节顺序原为 7.1→7.2→**7.5**→7.3→7.4（7.5 插队），已重排为 7.1~7.5；§4.3 把整段说明塞在标题里，已拆到正文；三处紧贴标题的表格补空行（GFM 渲染更稳）。④**新增 §4.10「开发期量测夹具」**（自 §4.6 抽出独立成节）：夹具组成表、探针三项能力（抖动量测 / 布局报告 / 清晰度对照）、判定口径，以及**六条踩过的坑**（窗口漂移致样本被裁剪、跃迁宽度口径会随字号骗人、A/B 状态必须对、探针绝不可默认注入主窗口、ps1 必须 BOM、probe 需纳入 tsc）。⑤**§10 补环境事实**：只有 PowerShell 5.1、1920×1200@125%（PS 默认 DPI 不感知会报 1536×960）、dev 进程残留占 1420 端口的清理方法（勿误杀 DSH 自身进程）。⑥**§8 更新**：「PowerShell 7 未安装」由「文档需修正」改为「已修正」；新增「`window.screenX` 高 DPI 读数旧结论存疑」一行。⑦**§11 增加「近期改动速查」表**（原表已积累到单行数千字，难以阅读）。文档 429 → 538 行。 |
| 2026-09/15（接管轮） | **接管评估 + 建立版本管理 + 登录态凭据迁移（本轮，无功能改动）**。①**独立复验四项检查**（不依赖文档结论）：`tsc --noEmit` 0 错、`pnpm test` **56/56**、`cargo check` 无警告、`pnpm build` 通过（dist 13.6MB）。②**实测复核 §8 待办**：`src-tauri/target` = 47.0GB、`gen` = 10.8GB（其中 **10.76GB 全在 `app/build/`**，`app/src/` 仅 0.2MB 且含**手写的 `WebLoginPlugin.kt`**）；项目内三份真实账号会话 cookie 仍在 `minmax/web_state/`。③**建库**：`git init` + 补全根 `.gitignore`（target / gen 的 build 产物 / runtime.zip / minmax/node.exe / minmax/web_state / probe 量测产物）+ 新增 `.gitattributes`（二进制不转换；换行统一 LF，`.bat` 保 CRLF）→ **实测入仓 197 文件 / 20.6MB**（若无规则约 57GB）；建立基线提交，**未配置 remote**。④**凭据迁移（security）**：`query_balance.js` 的 STATE_DIR 改为优先读环境变量 `AQM_WEB_STATE_DIR`，`network.rs` spawn node 时注入 `app_data_dir()/web_state`（dev/release 各自的应用配置目录，与 config.json 同级），未注入时回退脚本同级（独立运行行为不变）；原凭据按 SHA256 校验迁移到两个应用配置目录、ACL 收紧为仅当前用户/SYSTEM/管理员，项目内副本删除。⑤**验证**：脚本 `--check` 通过；注入/不注入两路径实测分别解析到迁移后文件与原项目文件；端到端真实查询跑通链路但报「需要账号密码（登录态已过期）」——**该会话 cookie 本身已于 2026-08-30 失效**（非本次改动引入，迁移前同样会失败），故"迁移后免登复用"未能取得阳性样本。⑥**修正文档**：§3 目录树、§4.3 登录态存放位置、§9 安全条目、§8 三行待办改「已完成」。⑦**顺带发现的两处物理事实**（与原文档表述不同，已在对应位置更正）：原文档称 `src-tauri/gen` 需整体忽略——实测不可，会丢掉安卓端手写源码；原文档称改 `.ps1` 用 `edit`/`write` 会丢 BOM 且必须用 `pwsh` 写回——本机无 pwsh，且本轮实测 `.ps1` 为 **LF+BOM**（非 CRLF），故 `.gitattributes` 必须用 `eol=lf`，否则 git 检出时就会改写脚本换行 |
| 2026-09/15（接管轮·第二轮） | **挂件状态行「数据正常」→「今日已用」（用户需求）**。①**先定口径再动手**（遵守本项目方法论）：这个 app 原先**没有任何「今日已用」数据源**——DeepSeek `/user/balance` 只返回充值/赠送/总额，凭空造数字会让一张余额卡片开始显示假数据，故先向用户确认数据来源，用户选定**本地推算：今日余额差**。②**实现**：新增 `src/lib/usage.ts`；`AppConfig` 新增 `usageBaselines` 与 Rust 命令 `record_usage_baseline`（**幂等**：当天已有基准即原样返回，判断放在 Rust 侧配置锁内）；`App.tsx` 在 `commitResult`（所有成功结果必经处）上报，挂件 `WidgetApp.tsx` 回读基准算差值。③**一处实现缺陷由测试抓出**：`todayUsed` 的币种比较只对当前币种做了 `trim()`、基准币种没做 → 断言失败；**改实现而非放宽测试**。④实测验收：探针布局报告回传 `.widget-status` 文字与「基准 − 当前」逐位吻合、元素宽 258.7 未溢出。⑤`test/usage.test.ts` 14 项边界测试（跨天/币种不一致/百分比/充值致负/零消耗）。⑥**跨天分支实测**：把某站点基准改成「昨天 + 假值 999」→ 等 30s tick 后读回，确认 `day` 回到今日、`value` 重置为真实余额（999 → 4.377）→ 幂等与换基准两条路径均已被真实运行验证。⑦**过程中把 config.json 写坏，并从中发现一个真实缺陷**：用 PowerShell 5.1 的 `Set-Content -Encoding UTF8` 改配置（**会写入 BOM**）→ `serde_json` 解析失败 → 程序按设计备份并回退默认值，**差点丢掉 6 个站点配置**（已从 `.bak` 完整恢复）。根因是 `load_config` 不容忍 BOM（记事本/编辑器另存也会产生这种"看起来合法的 JSON"），顺手修掉：新增 `strip_utf8_bom` 容错 + Rust 单测（含"不剥 BOM 确实会失败"的反证断言），并用**带 BOM 的真实配置启动实测**验证。教训：改这类数据文件必须明确编码与 BOM；**"我造成的事故"本身就是最好的测试用例来源** |
| 2026-09/15（接管轮·第三轮） | **挂件峰谷行加倒计时；先下移、再按用户要求归位顶部**。①用户要求「把峰标识移到今日已用下面，并加倒计时如『剩余5时23分』」→ 新增 `fmtPeakCountdownRemaining`（措辞按用户指定，但**与主卡片 `fmtPeakCountdown` 同口径**：同一套剩余秒数、同样 2 小时分级阈值，避免两个互相矛盾的倒计时）。②**下移落点先量后定**：新建 `probe/bg-sample.mjs`（零依赖 PNG 解析）实测发现该处（x≈110~200、y≈152~176）是角色头肩之间**近乎全透明的横带**（平均 alpha 1~30%，点击会穿透、观感悬空），y≥184 即角色白肩。③用户看过效果后要求**放回顶部** → 恢复站名上方落点，试错结论写进 CSS 注释与文档。④实测：峰谷行 y=21 h=19、与站名间隙 3.1px；文案「剩余13时29分」与「北京时间 19:30 → 次日 09:00」逐位吻合 |
| 2026-09/15（接管轮·第四轮） | **挂件排版微调（两处）+ 币种符号间隙**。①用户「站名和谷中间加一点点距离」→ 峰谷行上移 0.3%（9.9%→9.6%），与站名间隙 3.1→4.2px。②用户「让站名与余额数据这两行离今日已用行近一点」→ 把**站名与金额作为一组整体下移 2% 画布（≈6.2px）**（组内 6.6px 不变），使「金额 ↔ 今日已用」10.9→**4.7px**（实测）。③用户「今日已用行的 ￥ 与数字之间加一点点间隙」→ `fmt.ts` 新增 `fmtMoneyParts`/`fmtMoneySpaced`，挂件该行用 **U+2009 THIN SPACE**；其余金额仍走 `fmtMoney` 紧贴格式（新增 `test/fmt.test.ts` 18 项含 **fmtMoney 既有行为回归**，防止拆分重构改坏卡片输出） |
| 2026-09/15（接管轮·第五轮） | **设置页改版 + 启动自动打开挂件开关**。①主题改**三按钮横向**、壁纸下拉移到**同一行右侧**；②全局默认自动刷新间隔**收纳进「高级设置」折叠区**；③新增 `config.autoOpenWidget` + 开关，启动 1.2s 后自动打开挂件窗，并记住 `config.lastWidgetUid`（用户要的"上次那个"）。④探针新增**通用元素量测** `{"measure":[…]}`（主窗口也能用）+ `App.tsx` 仅 dev 的 `?settings=1` 钩子（已实测生产构建无残留）。⚠️ **实测踩坑**：自动打开 effect 初版依赖 `[cfg]`，挂件还没打开 `cfg` 就被后续动作改掉、React 在 cleanup 里 `clearTimeout` 取消定时器 → **挂件永远打不开**，且表现为"开关没反应"而非报错；靠 DOM 诊断标记 `#__aqm_autoopen` 定位，改 `[]` + `cfgRef` 后实测成功 |
| 2026-09/15（接管轮·第六轮） | **挂件纵向分布「等间距重排」→ 已按用户要求全部撤回**。曾把三行改为站名 17.5%/金额 31.5%/状态 44.1%（相邻间隙实测各 14.3px 完全均等，气泡内区实测 y≈30~133）。用户看过效果后要求**撤回该句对应的全部改动** → 用 `git diff <前一提交> -- <文件>` 逐字节核验回退（`widget.css` 与 `zoom-probe.ts` 零差异）。**结论保留**：气泡内区仅 103px 而内容总高 70.6px，「等间距」与「不越界」不能兼得 |
| 2026-09/15（接管轮·第七轮） | **开关控件改版：文案更正 + 左右滑动开关**。用户要求文案改为「自动打开鲸鱼娘窗口」（原「滑动效果」名实不符）、控件改**左右滑动开关**、不要勾选框、不要说明文本。实现 `.switch`/`.switch-thumb`（`role="switch"`、`aria-checked`、拇指 `left` 过渡、焦点环）；实测两态滑块 2.8px ↔ 22.4px（右缘余量 1.6px）、`.check-label` 消失、开关下无说明文本。顺带把 `App.tsx`/`store.ts`/`config.rs` 注释里的旧名一并更正（避免界面与注释不同源） |
| 2026-09/15（接管轮·第八轮） | **无特殊内容行的站点三行整体上移 10.2px**。用户要求「显示没有特殊显示内容的站点数据（如峰谷等）时三行上移」。实现 = 画布加 `data-has-peak` + `.widget-canvas[data-has-peak='0']` 条件位移。实测 Max66（无峰谷行）：三行 y 49.3→**39.1**、77.5→**67.3**、118.2→**108.1**（各上移 10.2px），行间间隙仍 6.6/4.7px。上移量取「有峰谷行时的 峰谷行→站名 间隙」，故两类站点顶部留白一致 |
| 2026-09/16（接管轮·第九轮） | **充值干扰修复：右上角菜单 + 「设置今日充值金额」**。用户反馈「今日已用数据受到了刚刚充值余额的数据干扰」。机制：余额差推算无法区分消耗与充值，充值当天差值变负。方案（用户指定）= 状态行改显提示，右上角新增与返回键同款**菜单按钮**弹出面板手填充值额。⚠️ **命中保护踩坑**：菜单键与面板落在背景图透明区，不特判会被**点击穿透**（实测点击无效、面板打不开）→ 把右上角 230×176 整块纳入实体（`in_fixed_overlay`，`widget_hit_state` 字段 `backButton`→`fixedOverlay`）。⚠️ 顺带修：探针 `{"measure":[…]}` 此前只接在主窗口循环、挂件侧不响应（量测永远拿到主窗口那份，白等了好几轮）。实测全链路：真实鼠标点开面板 → 真实键盘输入 → 写入配置 → 面板收起 → 显示恢复且数值吻合 |
| 2026-09/16（接管轮·第十轮） | **充值判据按用户口径重构：余额上升即充值；拆开 peak / maxSeen**。用户指出「今日消耗单调递增，数值变小时不就代表充值吗」—— 原判据（只看差值为负）会漏掉**充值额小于已消耗**的情形。重构中**连踩三坑**：①把"上升"当瞬态（本次>上次）→ 挂件 60s 才刷一次，下次读数回落信号即消失、提示闪一下就没；②判据方向写成「当前余额 < 水位」→ 每次正常消耗都误报；③**`peak` 一个字段担了两种语义**（已入账水位 + 当日最高余额）→ 刷新时抬高水位就把充值信号**自己消费掉**，提示永不出现（实测 `peak` 被抬到 18.32 后 `current > peak` 恒不成立、界面 `alert=false`）。最终**拆成两个字段**：`peak`（只用于判据，仅在登记充值时前移）与 `maxSeen`（只用于显示，每次刷新取 max）。删除了因此失效的 `alerted` 字段与 `mark_recharge_alerted` 命令（死代码）。实测闭环：水位 5.0 / 当前 17.91 → 提示出现且 `peak` 保持 5.0；登记后 `peak` 前移、提示收起、数值吻合 |
| 2026-09/16（接管轮·第十一轮） | **倒计时带秒不带「剩余」；预填值移除；充值额定为「固定值·可随时改·隔天清空」**。①用户要求「显示秒数时不显示『剩余』两个字」→ `fmtPeakCountdownRemaining(seconds, withPrefix=false)`：<2h 带秒文案不带前缀（实测高峰时段显示 `1时30分46秒`），≥2h 仍 `剩余5时23分`；阈值与主卡片一致。②用户质疑「为什么会有预填值，这个功能是用户手动填入」→ **移除预填猜测值**（曾用「当前余额 − 水位」，余额一变它就跟着变，用户原话「充值金额为什么跟着余额变」；查配置确认 `recharged` 全为 0，跟着变的正是猜值）；现打开菜单显示**你填过的固定值**供修改、从未填过则为空；金额按分四舍五入后再落盘。③跨天清空：抽 `fresh_baseline` 纯函数供生产与测试共用（避免测试写成同义反复）+ Rust 单测，并端到端实测（`day` 改回昨天→刷新→`recharged` 88.5→**0**）。④顺带修探针 bug：`value ?? textContent` 让 `<button>` 的空 value 盖住按钮文字。⑤**本轮另有两个环境级教训**：改挂件前端后不重启 dev 会量到**上一会话的挂件**；旧 dev 残留占 1420 端口会让新 dev 启动失败（`Port 1420 is already in use`）——两者都会让人对着假象做判断，已写进 §0/§10 |
| 2026-09/16（接管轮·第十二轮·文档轮） | **交接文档系统性完善（无功能改动）**。①**修正过期状态**：§0「当前状态」的测试数（56→**114 前端 / 5 后端**）、最后核对日期改 2026-09/16、提交数与入仓体积；§2 技术栈的测试行（3 文件 56 项 → 6 文件 114 项 + 后端 5 项）；§6 开发命令补 `cargo test --lib`。②**修正矛盾/重复**：§8 中「仓库体积与忽略规则」**重复两行**（其中一行还是建库前的旧状态）→ 合并为一行并保留"`gen/` 不可整目录忽略、否则丢手写 `WebLoginPlugin.kt`"这条关键结论；§5 里「充值登记」条目仍写着旧判据（差值为负 / 基准+充值）→ 改为新判据与公式。③**补 §8 新条目**：充值登记（含「peak 与 maxSeen 必须分开」与「禁止再加自动填入的猜测值」两条硬结论）、挂件纵向分布（已撤回一版）、倒计时文案定稿。④**补 §9/§10 实操经验**：git 提交用 `-F <消息文件>`（`-m` 带中文引号会被拆成 pathspec）、临时 `.ps1` 必须带 BOM、停 dev 必须确认 1420 已释放、量测挂件控件的坐标换算与探针取值细节。⑤**补 §4.6/§4.10**：右上角固定 UI 整体保护（`fixedOverlay`）、探针 `measure` 双窗口响应与取值规则、真实交互验证的坐标换算（不要用配置里的 `showWidgetPos` 推算窗口位置）。⑥**§11 补全**：速查表新增第九~十二轮（修掉原先"第八轮后直接第十轮"的跳号），详细记录表补齐接管轮第三~十二轮。文档 543 → **675 行** |
| 2026-09/16（接管轮·第十三轮） | **同步导出改版「导出配置文件」（复制形式）+ 加密滑动开关；加密格式升版 `AQM2:`（KDF 参数入密文、迭代 60 万）**。**触发**：用户问「导出导入 json 文件可以加密吗」→ 读代码确认**加密层早已存在**（`crypto.ts`，剪贴板/二维码两路都在用），逐条说明现状与三个真实注意点（`AQM1:` 无 KDF 参数/版本协商、弱口令在长期留存载体上风险更高、加密封装保护不了文件名等元信息），用户选定「方案 1 + 桌面端导出走复制形式 + 是否加密做成与设置页同款的滑动开关」。**改动**：①**`src/lib/crypto.ts` 升版 `AQM2:`** —— `ver(1)\|iter(4,BE)\|salt(16)\|iv(12)\|ciphertext`，迭代 10 万→**60 万**；`isEncryptedPayload`/`decryptPayload` 两前缀都认，**旧 `AQM1:`（固定 10 万次、无头部）仍可解密**，只是不再产出。**升版理由（关键）**：旧格式把迭代次数硬编码在代码里，**一旦想调整就永久解不开历史导出物**——这不是"以后再说"，是没有补救机会。安全护栏另加三项：篡改 1 bit 必失败（GCM 完整性）、未知版本号明确报错、截断报「已损坏」（而非误报口令错误）；迭代下界 `<1000` 一律判损坏，防被构造成 1 次迭代的弱 KDF。②**`src/lib/sync.ts` 新增纯函数 `buildExportText(cfg,{encrypt,password})`** 承载"复制明文还是密文"的口径 —— 加密开启而口令为空/纯空白**抛错，绝不静默回退成明文复制**（否则用户以为已加密）；口令首尾空格自动去除。③**`SyncDialog` 方案 A 由两个按钮改成一个开关 + 一个按钮**：原「导出到剪贴板」+「加密导出」→ **滑动开关（`role="switch"`，复用设置页 `.switch`/`.switch-thumb`）+「导出配置文件」**，开关打开时展开口令输入框；二维码加密路径改为**共用 `buildExportText`**（删掉手写 base64 拼装，剪贴板与二维码两路产物从此同构可互认）。④**`App.tsx` 新增仅 dev 钩子 `?sync=1`**（与既有 `?settings=1` 同体例，生产构建被摇树移除，实测 dist 无 `sync=1`/`settings=1`/`__aqm` 残留）。⑤**`probe/zoom-probe.ts` 新增通用点击** `{"click":[…]}`：按顺序等元素出现（最多 2s，**因此可以串联"点 A 打开弹窗、再点弹窗里的 B"**）→ 点击 → 等 260ms 过渡 → 再量测，回传 `clicked[].before`（点击前的 `aria-checked`/`class`）。**验证（探针实测两态，非肉眼）**：`clicked.before=false` → 点击后 `aria-checked` 变 true（证明真的切换了）；开关 `x=338 w=40` → 右缘 **378** 与文本域右缘 **378** 齐平；拇指 **2.8px ↔ 22.4px**；`.sync-pwd-row` 关闭态 count=**0**、开启态 count=1（宽 318.4 与文本域一致）；按钮文字未折行（scrollW 110 == clientW 110）；方案 A 卡片高 267.6 → 310（+42.4 = 口令行 34.4 + 间隙 8，算术自洽）；弹窗两态都在视口内（开启态底 736.9 < 视口 862）。⑥**过程中踩到一个夹具缺陷并修掉**：`vite-plugin-aqm.ts` 用 `data.plan.run` 命名落盘文件，而**主窗口的量测回传体原本不含 `plan`** → 所有主窗口量测样本都写成 `run-000-*.json`，**按 run 号去 `Get-ChildItem -Filter 'run-<N>-*'` 永远找不到**；我据此误判成"主窗口探针没响应 / 被 Chromium 节流"，连着用错口径排查了三轮（还去查了窗口 Z 序、最小化状态、甚至截屏——而截屏截的是**盖在上面的浏览器窗口**）。现已修：两类窗口的量测回传体都带 `plan:{run}`。**附带事实**：窗口最小化时探针照常轮询回传；**量测"没数据"时先确认自己找对文件名，再去怀疑被测对象**（已写进 §4.10 踩坑第 7、8 条）。**未做/未验**：桌面端**不做**「另存为」文件对话框（用户定案，故不加 dialog/fs 插件）；安卓端文件落盘与系统分享面板未做；**安卓 `crypto.subtle` 是否可用未实测**；未点「导出配置文件」做端到端复制验证——那会把含真实 API Key 的配置**写进用户剪贴板并覆盖其当前内容**，故只验证到「按钮存在、开关两态正确、纯函数单测覆盖」，`writeText` 本身是既有已验证 API。**结果**：测试 114 → **125**（crypto 4→11、sync 8→12）；`tsc` 0 错、`pnpm build` 通过；改动 7 文件（`crypto.ts`/`sync.ts`/`SyncDialog.tsx`/`App.tsx`/`zoom-probe.ts`/两个测试文件）+ 本文档 |
| 2026-09/16（接管轮·第十三轮·补充） | **加密开关改为默认开启；方案 B（二维码）暂时禁用**。①用户口径「加密功能默认开启」→ `SyncDialog` 的 `encryptExport` 初值 `false` → `true`，注释写明理由（导出物含 API Key / AccessKey Secret，让加密成为默认路径、明文需主动关掉）。**实测**（探针，打开弹窗即测）：拇指距轨道左缘 **22.4px**（开启态；关闭态为 2.8px）、`.sync-pwd-row` count=**1**（口令框默认展开）、弹窗高 611.5 且底 736.9 < 视口 862。**副作用（有意为之）**：默认开启后直接点「导出配置文件」而不填口令会得到「导出失败：口令不能为空」，不会悄悄复制明文。②用户口径「方案 B 暂时禁用」→ 新增模块级常量 `ENABLE_QR_SECTION: boolean = false`，把整段二维码入口（导出为二维码 / 加密导出二维码 / 从图片识别导入 / 二维码展示区）用 `{ENABLE_QR_SECTION && (…)}` 包住收起；**实现、state、handler、依赖与测试全部保留**，改回 `true` 即恢复；块内缩进刻意不动，好让恢复/再禁用都是两行以内 diff。**实测**：`.sync-section` 由 2 个变 **1 个**、`.sync-section-title` 仅剩「方案 A · 剪贴板」、`.qr-box`/`.qr-img` count=**0**、弹窗高 611.5 → **444**、仍在视口内（底 653.2 < 862）；关闭按钮仍可点（clicked found=True → `.sync-dialog` count=0）。**未做**：禁用期未从打包产物中剔除 `qrcode`/`jsqr`（保持一行可恢复优先于体积）。`tsc` 0 错、`pnpm test` 125 全绿 |
| 2026-09/16（接管轮·第十四轮） | **挂件菜单删掉「没充值过」按钮 —— 填 `0`/留空 + 「确定」= 今天没充过（提示收起）**。用户口径：「不需要"没充值过"按钮，只要是 0 然后点确定就不显示那个提示」。①**先确认行为等价再动手**：读代码发现 `确定`（`saveRecharge`）**本来就**无条件把当前余额 `value` 传给 `set_recharge_amount`，且 Rust 侧的水位前移**不以金额为条件**（`base.recharged = amount;` 之后才是 `if bal > peak { peak = bal }`）→ 填 `0` 与旧按钮**完全等价**，所以这是**纯 UI 变更、零行为损失**（不需要改判据）。②删除 `wmp-actions` 里那颗按钮（面板只剩「取消」/「确定」）。③**顺手把不变量钉住**：把水位前移规则从命令里抽出纯函数 `apply_recharge(base, amount, balance)`，新增 4 项 Rust 单测 —— 核心是 `recharge_zero_still_advances_peak`（**填 0 也必须前移水位**，否则"填 0 → 确定 收起提示"是假的、提示永远收不起来）；另覆盖覆盖式写入、无 balance 不改水位、非法/非正/低于水位的 balance 一律忽略（绝不把水位往下拉）。④**清理会骗人的残留**：`alerted`（早于第十轮就被删除的字段）仍留在前端 `UsageBaseline` 类型、`test/usage.test.ts` 的 fixture 与两处注释里，且注释描述的判据（"本次 > 上次"的瞬态判定）**早已废弃**——已全部清除并改写成现判据，避免后人照旧注释改回去。**实测**（探针挂件侧，`{"click":[".widget-menu"]}` → measure）：`.wmp-btn` count=**2**（取消 / 确定）、两键同行 y=103.2、右缘 255.2 与面板内容右缘齐平、未折行（scrollW 156 == clientW 156）；再点一次 `.widget-menu` 面板关闭（count=0）。**过程记录**：`cargo test` 首次跑挂了我自己写错的一条断言（把"再创新高的阈值"当成 `peak` 而非 `peak + recharged`）——是**测试算术错**，不是产品问题，已修正为断言下界 110 并说明口径。**验证**：Rust 单测 5 → **9** 全绿、`pnpm test` 125 全绿、`tsc` 0 错。⚠️ 改 Rust 会让 dev 实例自动重编译重启（本轮实测：`config.rs` 11:26:12 → debug exe 11:26:22 → 应用重启），属预期行为 |
| 2026-09/16（接管轮·第十五轮） | **同步改「只走文件」 + 添加站点默认显示名**。**触发**：用户两条口径 —— ①「加一个导入配置文件按钮，放在导出文件按钮左边，删掉文本方案，只保留文件导入导出功能」；②「在添加站点时，如果站点类型选的 deepseek 官方，显示名称就默认填入 DeepSeek官方」。①**先问清"文件"到底怎么落**（三选一：放进剪贴板 / 写下载目录 / 另存为对话框）→ 用户选**把「文件」本身放进剪贴板**（粘出来就是文件）。于是新增 Rust 模块 `src-tauri/src/export.rs`：`export_config_file(text, file_name)` 写文件到 `%APPDATA%\com.apibalance.monitor[.dev]\export\`，再用 Win32 **`CF_HDROP`**（`OpenClipboard`/`EmptyClipboard`/`GlobalAlloc(GMEM_MOVEABLE)`/`SetClipboardData`/`CloseClipboard`）把该文件放进剪贴板；`Cargo.toml` 加 `Win32_System_DataExchange` + `Win32_System_Memory` 两个 feature。⚠️ 两个 Win32 细节：`GlobalFree` 在 `Win32::Foundation` 而**不在** `Win32::System::Memory`（本轮编译报错才发现的）；`SetClipboardData` 成功后内存归系统，**不能再 `GlobalFree`**（双重释放）。②**导出内容拆成两份口径**：`buildExportFileText`（文件用）= 未加密直接给**标准 JSON**（真 `.json`、可读可手改）、加密给该 JSON 的 `AQM2:` 密文；`buildExportText`（压缩 `Z1:` 文本）保留给暂时禁用的二维码路径。**导入端零新增格式**。③`SyncDialog` 删掉粘贴文本框与「从剪贴板导入」，按钮行改成 `[导入配置文件]（左）[导出配置文件]（右）`，导入改 `<input type="file">` + `file.text()` → 原 `applyImportText`（加密文件先弹口令行）。④`SiteDialog` 新增 `DEFAULT_DISPLAY_NAMES = { deepseek_official: 'DeepSeek官方' }`：只在名字空着或仍是自动填的值时才改（**不覆盖用户手敲的名字**），切走时清空。⑤**实测（探针，非肉眼）**：弹窗只剩 1 段、标题「配置文件」、`.sync-input`（文本域）count=**0**、导入按钮 x=59.6 在导出按钮 x=179.2 **左侧**；默认态（加密开、口令空）点导出 → 状态栏「**导出失败：口令不能为空**」（守住了"不静默出明文"）；关掉开关再点导出 → 状态栏「已把配置文件放进剪贴板：API余额通-配置_260916.1408.json」→ **Rust 命令返回 Ok（= 文件写入 + `SetClipboardData` 都成功）**，落盘文件 5496 字节、`JSON.parse` 通过（schemaVersion 1 / 6 站点）。添加站点：打开即 `显示名称='DeepSeek官方'`；切到自动识别 → **清空**；切回 DeepSeek 官方 → **重新填回**。⑥**遗留（已记 §8）**：`CF_HDROP` 是 Windows 专有 → **安卓端本轮起没有导出通道**（导入仍可用）；文件留在 `export/` 目录里不能删（剪贴板引用它）。⑦**追加（同轮）**：用户要求「导出按钮加图标、按钮右对齐、导入用外面按钮那个图标」→ 两按钮加 `lucide` 图标（**导入 = 主界面「导出/导入」那颗 `Download`**、导出 = `Upload`，14×14/2.2 与 `SiteActionsDialog` 同约定），`.sync-actions` 加 `justify-content: flex-end`；实测 `.sync-actions .btn svg` count=**2**（各 14×14）、导出按钮右缘 **378** = 分区内容右缘（改动前整行靠左、导入在 x=59.6）。⑧**再追加**：用户改为「**导入靠左、导出靠右**；导出不要蓝色；主界面按钮图标换成导出那颗」→ 新增 `.sync-actions.split { justify-content: space-between }`（只给这一行用；默认的右对齐留给按钮更多的二维码段，避免被拉散）、导出按钮 `btn primary` → **`btn secondary`**、`App.tsx` 的「导出/导入」`Download` → **`Upload`**。实测：导入 x=**59.6**（左缘）、导出 x=244.4 → 右缘 **378**（右缘）、两按钮 `cls='btn secondary'`、`.sync-actions .btn.primary` count=**0**；**顺带给探针量测加 `cls` 字段**（光看矩形看不出"颜色/样式"这类改动），并修掉 SVG 元素 `className` 是 `SVGAnimatedString` 对象、`toString` 只得 `[object SVGAnimatedString]` 的坑（改 `getAttribute('class')`）→ 从而能直接验证图标身份：`.action-row .tip svg` 首颗 = `lucide-upload`、导入 = `lucide-download`、导出 = `lucide-upload`。**验证**：Rust 单测 9 → **12**（文件名清洗防目录穿越 / `DROPFILES` 二进制布局 / 非 ASCII 路径往返）、`pnpm test` 125 → **129**、`tsc` 0 错、`pnpm build` 通过 |
| 2026-09/16（接管轮·第十六轮） | **设置页行间「立体」分割线 + 开关文案改「启动时自动打开鲸鱼娘窗口(上次选择)」**。用户口径：①「自动打开鲸鱼娘窗口，改为：启动时自动打开鲸鱼娘窗口(上次选择)」；②「设置中每一行加一条立体的分割线」。①**文案**：`.switch-label` 与 `aria-label` 同步改成新文案（不加说明文本，仍是左标签 + 右开关），实测标签宽 **200px、单行不换行**（h=15.2），与开关不重叠；行为未变（打开的是 `config.lastWidgetUid` 记的那个）。②**分割线**：新增 `.settings-divider`（`border-top` 实线 + `box-shadow: 0 1px 0` 淡线，布局占 1px、视觉 2~3px），插 3 条 —— 反馈行后、背景壁纸说明后、滑动开关后（高级设置前）；`.advanced-section` 原先自带的 `border-top` 删掉（否则与分割线叠成三条）。实测 `.settings-divider` count=3、各宽 348（= 内容宽）。③⚠️ **踩坑并修掉（本轮最有价值的一条）**：第一版按"暗线 + 更亮的白线"做浮雕，**在浅色主题下白线完全不可见**——弹窗底色本来就是纯白（`--dialog-bg: #ffffff`），截图像素实测：暗线行 229、相邻行 255（=纯白），等于只画了一条平线、毫无立体感。改法是**让两个主题用不同做法**：浅色 = "上暗(0.14) + 下更淡的暗色(0.05)"；深色弹窗底色 `#1c1d24` = 经典"暗线(0.45) + 淡亮线(0.08)"。两色都抽成主题变量（`--divider-line` / `--divider-highlight`）。**两套都实测过像素**：浅色 **219 → 245 → 252 → 255**（刻线 + 下缘淡影）；深色 底色 **28,29,36** → 暗线 **15,16,20** → 亮线 **41,42,49**（比底色亮、看得见）。教训：**"亮线/高光"这类视觉手法必须按底色实际取样验证**，光算透明度会得出"看不见"或"很显眼"的错误结论（本轮浅色就是这么栽的）。④**顺带**：为验证用了窗口截图 + 像素取样（临时脚本已删），并切到深色主题实测后**切回浅色**（`config.theme` 已确认为 `light`，与动之前一致）。⑤**追加（同轮）**：「设置标题字体统一成高级设置那种」→ 字号/颜色本来就一致、**差的只有字重**，故在 `.settings-dialog` 作用域下把 `.field-label`/`.switch-label` 统一为 `font-weight: 600`；设置页根节点加了 `settings-dialog` 类（不动全局 `.field-label`，它在「添加站点」等弹窗也在用）。⑥**再追加**：「删掉这个文本：选择主界面背景样式，立即生效」→ 该提示行改为**仅自定义壁纸时**才渲染（保留"可点选择图片替换"那句），非自定义时不再有提示行。**验证**：`tsc` 0 错、`pnpm test` 129 全绿 |
| 2026-09/16（接管轮·第十六轮·后记） | **用户口径变更：外观类改动不再做量测/截图验证**（原话「每次外观改变不用测试，我来确认就好了」）。→ 今后**外观/排版类**改动只做编译与回归门（`tsc` + `pnpm test`），改完直接交给用户看；**功能/逻辑类**仍按项目方法论实测（`probe/` 夹具、像素取样、端到端点击）。本轮据此把 `.field-label` 字重统一后**未再截图核对**（用户自行确认）。另：探针量测新增 `cls` / `fontWeight` / `color` 字段仍保留 —— 它们是**功能类核对**（如"导出按钮不再是 primary"、"某个 class 是否被换掉"）的硬指标，与外观验收无关 |
| 2026-09/16（接管轮·第十七轮） | **安卓端导出落地：AB 结合（系统分享 / 保存到本地）**。**触发**：用户先问「安卓导入导出有什么解决方案」→ 我给出四选项（A 分享面板 / B 保存到本地 / C 复活二维码 / D 剪贴板文本兜底）并核实了「安卓导入已具备」（wry 的 `RustWebChromeClient.onShowFileChooser` 实现，走系统文件选择器）→ 用户选 **A+B 结合，点导出后二选一**。**改动**：①**Kotlin** `gen/android/app/src/main/java/com/apibalance/monitor/ConfigExportPlugin.kt`（`@TauriPlugin`）：`share` = 文件写进 `cacheDir/exports/` → `FileProvider.getUriForFile(activity, "${packageName}.fileprovider", f)` → `ACTION_SEND` + `EXTRA_STREAM` + `FLAG_GRANT_READ_URI_PERMISSION` + `clipData`（只给 EXTRA_STREAM 时部分目标拿不到读权限，会读不到文件）→ `createChooser`；`save` = `ACTION_CREATE_DOCUMENT`（`EXTRA_TITLE` = 文件名）→ 结果在 `@ActivityCallback saveResult(invoke, activityResult)` 里 `contentResolver.openOutputStream(uri)` 写入；**用户取消 resolve `{ok:false,cancelled:true}`**（中性提示，不报红）。待写内容放 `companion object`（Activity 被重建时静态字段才活得下来）。②**Rust** `export.rs`：新增 `export::plugin()`（`api.register_android_plugin("com.apibalance.monitor","ConfigExportPlugin")` + `app.manage(ConfigExport<Wry>)`，照 `web_login.rs`）、`ConfigExport<Wry>::run(cmd, file_name, content)`、`ExportOutcome{ok,mode,path,cancelled,message}`；`export_config_file` 加 `mode: Option<String>` 参数并**改成 `async`**（`run_mobile_plugin` 会阻塞当前线程，而 Kotlin 的 UI 要跑主线程 → 同步命令占主线程必死锁；`browser_login_query` 同理）。③**前端** `SyncDialog.tsx`：安卓点「导出配置文件」→ 露出 `[系统分享][保存到本地][取消]`；`invoke` 返回改为结构化 `ExportOutcome`；桌面分支文案不变。**关键发现**：Tauri 安卓模板**自带** `androidx.core.content.FileProvider`（authority `${applicationId}.fileprovider`）与 `res/xml/file_paths.xml`（`cache-path`/`external-path`）→ **完全不用改 AndroidManifest.xml**；代价是**只能分享 `cacheDir`/外部目录下的文件**，写成 app 私有 dataDir 会抛 `Failed to find configured root`。**踩坑**：④`#[cfg(windows)] use std::path::Path;` 属**过度门控**——`Path` 被跨平台存在的 `build_dropfiles_payload` 用到 → 安卓目标编译直接失败；**是靠 `cargo check --lib --target aarch64-linux-android` 抓出来的**（桌面 `cargo check` 看不见）。⑤裸跑安卓目标 check 需先把 NDK 的 clang 接进环境（`PATH` + `CC_aarch64_linux_android`/`AR_aarch64_linux_android`），否则卡在 `ring` 的 C 编译（`ToolNotFound: aarch64-linux-android-clang`）；**且这些变量不能留在环境里跑桌面构建**（`CC` 指向安卓 clang 会把 `ring` 的 Windows 构建搞崩，本轮踩了一次）。**验证**：安卓目标 `cargo check` **通过**（7 个 warning 全是既有的 Win 专用/挂件常量，无一条来自本模块）；**Gradle `:app:compileArmDebugKotlin` BUILD SUCCESSFUL**（Kotlin 侧真编译过，唯一 warning 是 `WebLoginPlugin.kt` 里既有的废弃 API）；桌面 `cargo test --lib` **12 项全绿**；`tsc` 0 错、`pnpm test` **129** 全绿、`pnpm build` 通过；**桌面导出端到端实测**（新的 `mode`/`ExportOutcome` 契约没改坏：状态栏正确显示「已把配置文件放进剪贴板：API余额通-配置_260916.1531.json…」）→ 测完已清空剪贴板并删掉 export/ 里那个测试文件。⑥**整包 `tauri android build` 没能跑完**：Rust 安卓 release 编译成功，但卡在**把 `.so` 符号链接进 `jniLibs/`** 一步 —— Windows 建符号链接需要**开发者模式或管理员权限**（`Creation symbolic link is not allowed for this system`），而本会话审批提示被禁用、无法提权（项目以前用 `打包.ps1 -Android` 走 UAC 正是为此）。**绕过办法（本次采用，仅用于编译检查）**：把 `target/aarch64-linux-android/release/*.so` **拷贝**进 `jniLibs/arm64-v8a/`，再直接调 Gradle 的 `:app:compileArmDebugKotlin`（跳过 tauri CLI 的符号链接步骤）；检查完已删除该拷贝（免得留下陈旧 `.so` 被后续打包误用）。**未验**：安卓**运行时**行为（分享面板是否列出微信/QQ、保存后文件是否真落地）—— 静态编译查不出，需模拟器/真机实测。⑦**顺带澄清 `crypto.subtle`**（长期挂着"未实测"）：Tauri 页面 origin = `tauri.localhost`，W3C Secure Contexts 把 `localhost` 及子域列为可信来源 → 加密导出在安卓上**依据规范可用**（仍建议真机确认一次） |
| 2026-09/16（接管轮·第十八轮·文档轮） | **交接文档系统性完善（无代码改动，本轮）**。逐条核实并修正：①**§4.9 标题与导出描述过时** —— 标题仍写"导出把文件本身放进剪贴板"（那只是 Windows 的做法），已改为"导入选 .json；导出 = Windows 放进剪贴板 / 安卓分享或保存"；导出步骤补上 `export_config_file(text, fileName, mode?)` 的 `mode` 参数与平台分流指针。②**§4.9 旧格式条目状态不清**：原标"方案 A（旧记录 · 已删除）"，但其中"`Z1:`/紧凑/标准 JSON/`AQM1:` 仍可导入"是**现在仍然成立**的能力 → 改标"**旧格式（导入端仍支持，但不再产出）**"。③**§4.9 容量结论自相矛盾**：仍写着"超 10 站建议剪贴板、30+ 站建议文件导入导出"——导出早已是**文件**（不受字符数限制），该建议作废，已重写并注明只对仍禁用的二维码有意义。④**§6 命令区块的测试数过时**（125→**129** 项、Rust 5→**12** 项），并补上两道安卓编译门命令。⑤**新增 §7.6「安卓侧编译门」**：把本轮踩到并解决的"`tauri android build` 因 Windows 符号链接权限卡住"写成可照抄的替代流程（Rust 安卓目标 `cargo check` 的 NDK 环境变量 + Gradle `:app:compileArmDebugKotlin` 的 flavor 任务名 + 先拷 `.so` 绕开符号链接 + 检查后删拷贝 + **`CC` 别留着跑桌面构建**）。⑥**§8 去重**：「仓库体积与忽略规则」原有**三行**（含一行还是建库前的过期状态）→ 合一；并**新增两行待办**：「充值判据下界 `peak + recharged` 疑点」（登记非零充值后「今日已用」= 用户填的充值额、且后续小额充值不再提示；**已向用户提出、未决**，连候选改法一起记下）、「安卓导入/导出的设备端实测」（本轮只过编译，四条待验清单）。⑦**修正一条已过时的"能力限制"**：文档里原写"本 AI 模型不能看图"——现在模型**可以**读图（本轮已用 `read_image` 核对设置页像素），该过时表述已删除。⑧**§2 新增「安卓侧」行**（两个手写 Kotlin 插件的职责与桥接方式），**§10** 补 JDK/NDK 实测路径与"整包构建需开发者模式/管理员"+ §7.6 指针，**§0** 补两条接手须知（Rust 改动会让 dev 自动重启、安卓构建需要提权否则跑 §7.6）。⑨**§4.10 补全探针回传字段表**（新增 `cls`/`fontWeight`/`color` 三项及其用途与 `SVGAnimatedString` 坑）。⑩**§11 加"读表须知"**：本速查表只收有功能变化的轮次，纯文档轮只在明细表出现 → 因此轮号会跳号（第十一→十三、十七→十九），并说明新记录必须**追加到明细表末尾**。文档 757 → **820+ 行** |
| 2026-09/16（接管轮·第十九轮） | **「今日充值金额」面板改为独立窗口（挂件正上方 + 右对齐）；删除「缩放后模糊」条目**。**触发**：用户两条口径 —— ①「已经没有模糊问题，从交接文档里删除这个」；②「鲸鱼娘窗口缩小后，设置界面会显示不全，把他独立，移动到鲸鱼娘窗口上方，和鲸鱼娘窗口右对齐」。①**删条目（模糊问题不存在）**：§8「挂件「缩放后模糊」」整行、§4.6 的排查段（含逐尺寸跃迁宽度表）、§11 两处历史表述、§0 与 §4.10 两处拿它当例子的方法论句子（**改成通用表述，教训本身保留**）全部删除；同时把 `WidgetApp.tsx`/`widget.css` 里"常驻 `will-change` → 栅格不重新生成 → 文字与图片发虚"那段注释改成诚实表述（该开关对清晰度**未测出差异**，保留它只因"空闲时不占合成层"合理且无代价）——避免后人拿被证否的结论去动渲染路径。②**面板独立**：新增 `src-tauri/src/widget_menu.rs`（`MENU_LABEL="widget-menu"`；纯函数 `menu_origin(wx,wy,ww,wh,mw,mh,top_bound)` = 与挂件**右对齐 + 贴挂件正上方 6px**，上方放不下则**翻到挂件下方**；命令 `widget_menu_toggle` / `widget_menu_close` / `widget_menu_resize`；`sync_menu_pos` 由挂件那条 60Hz 命中检测轮询顺带调用，目标位置与当前差 ≥0.5px 才动窗口）+ `src/widget/WidgetMenuApp.tsx`（内容与保存逻辑自 `WidgetApp` 迁入，`useLayoutEffect` + `ResizeObserver` 量测根节点高度回报 Rust）+ `src/widget/widget-menu.css`（面板样式自 `widget.css` 迁出）；`main.tsx` 改三窗口分流；`capabilities/default.json` 的 windows 加 `widget-menu`。③**顺手清理**：`widget.rs` 右上角保护区由 230×176（原为罩住展开的面板）**缩到 40×72**（两键实际边界 `x≥W-36, y≤68` + 4px 余量），右上角其余空白恢复穿透；`widget_set_bounds` 里内联的 `SetWindowPos` 抽成 `pub(crate) fn set_bounds_atomic` 供面板复用（"一次 SetWindowPos 原子改位置+尺寸"的说明随函数走）。④**实测（Rust 日志 + 探针量测 + 真实鼠标 + 截图，四方交叉）**：`[menu] widget rect (80.0,488.8) 250.4x250.4` → `[menu] opened at (106.4,350.8) size 224x132`：**右缘 106.4+224 = 330.4 == 挂件右缘**、**底边 350.8+132 = 482.8 == 挂件顶边 488.8 − 6** ✓；内容高度回报触发两次 resize（118 → 151），两次都保持**右缘 330.4 / 底边 482.8 不动**（"向上生长"不变量成立）✓；面板窗口内探针量测 `.widget-menu-panel` 204×130.9、输入框与两按钮 `inViewport=true`、`scrollW == clientW` 无折行 ✓；截图肉眼核对：**挂件仅 250px 宽时面板完整显示**（正是用户报的"显示不全"场景）✓；**真实鼠标**点 ☰ 打开（`[menu] reused at (106.4,331.6)`）→ 再点关闭（`[menu] hidden`）✓ —— 这一步**必须用真实输入**（JS `el.click()` 绕过系统级命中判定，测不出"被穿透点不到"）；**真实鼠标拖动挂件** → 面板全程跟随（两张截图对照：右缘与 6px 间隙保持不变）✓。⑤**过程记录（含一条环境教训）**：驱动验证用的 `probe/trigger.json` 事后**逐字节恢复**（原文无尾换行），被拖动的挂件用探针 `{"posX":80,"posY":488.8}` **归位**并截图确认回到原位；⚠️ 验证中途发现**这台机器正在被使用**（前台有浏览器、挂件被移到左上角）→ 立即停止一切真实输入类验证，「关闭挂件时一并收起面板」这条**只做了代码走查、未再实点**。**教训：在别人的工作机上做真实鼠标/键盘验证前，先确认没有人在用**（真实输入会抢焦点、可能点进用户正在操作的窗口）。⑥**验证**：`tsc --noEmit` 0 错、`pnpm test` **129 全绿**、`cargo test --lib` **16 项全绿**（新增 4 项 `menu_origin` 单测：右对齐+贴上方 / 空间不足翻转 / 副屏上边界 / 高度变化时底边与右缘不动）、`cargo check --lib` 无警告 |
| 2026-09/16（接管轮·第二十轮） | **「今日已用」换模型（消费只由「下降」构成）+ 开发期白屏定位与修复**。**触发（两轮）**：①用户先问「看一下这个项目是怎么解决充值干扰今日用量问题的，只总结不动手」并给出开源仓库 `MeteorNOX/DeepSeek-Balance-Whale-Widget` → 读它的 `lib/accounting.mjs` 后总结出它的模型（**升降分开累计 + 显式校正 + 乐观锁 + 定点记账**：余额**下降**累加为消费、**上升**只记 `credit` 并触发「待核对」，充值与消费落在同一刷新间隔内时才需要用户填累计到账额来校正）；②用户随即定案：「我的项目也改为：消费只由『下降』构成」。**改动**：①**Rust 观测账本**（`config.rs`）：`UsageBaseline` 由 `{value,currency,day,recharged,peak,maxSeen,last}` 改为 `{opening(serde alias value),currency,day,last,lastAt,debit,credit}`；新增纯函数 `observe(&mut b, value, currency, day, at)`：与上次读数比较，**下降累加进 `debit`**、**上升累加进 `credit`**、`at <= lastAt` 的重复/迟到样本**原样丢弃**、跨天或**币种变化**重建区间；金额累加走定点（`MONEY_SCALE = 1e8`，整数相加后再落盘成可读小数）。`record_usage_baseline` 增加 `at: u64` 参数（观测时刻）。**删除**：`set_recharge_amount` / `apply_recharge` / `fresh_baseline` 旧语义 / 6 项旧单测 / `lib.rs` 的命令注册。②**前端**：`usage.ts` 重写（`todayUsed(baseline, currency)` 直接返回 `debit`；删 `needsRechargeInput` / `setRechargeAmount` / `rechargeLowerBound` / `usageUpperBound`；`recordUsageBaseline` 带上 `at`）；`WidgetApp.tsx` 删掉"需要登记充值 → 状态行改显提示"整条分支（**数字永不被顶掉**）与面板交互，保留 ☰ 与 `aria-expanded` 同步；`WidgetMenuApp.tsx` 改为**占位设置面板**（「设置 / 暂无设置项，这个面板留给后续功能。」+ 关闭按钮 —— 用户口径「只是去掉今日充值金额功能，保留面板和按钮，以便之后添加其他功能」）；`widget-menu.css` 清掉输入相关死样式；`widget.css` 删 `.widget-status.recharge`；`test/usage.test.ts` 重写为 12 项（含"★ 充值（credit）不进这个数"）。③**白屏定位（本轮最费时也最有价值）**：用户报「现在 dev 启动后白屏」。dev 进程与 Vite（1420）都正常，但日志里**一条前端活动都没有**（无 `icon-fetch`、无 `[ui] result`）→ 用探针打 `{"measure":[".app-shell",".site-card"]}`：**有 trace 回传但 count=0** → 判定"脚本在跑、React 没挂载"；再在 `index.html` 临时挂 `window.addEventListener('error')` 把报错 POST 到 vite 的 `/__aqm/trace`，**一次就拿到真凶**：`Uncaught SyntaxError: The requested module '/src/lib/usage.ts' does not provide an export named 'setRechargeAmount'`（来自旧的 `WidgetMenuApp.tsx`）—— 即**改 `usage.ts` 删导出时 dev 还开着**，WebView 缓存里留着旧版 `WidgetMenuApp`（仍 import 它）与新 `usage.ts` 混用 → 模块图语法错误 → `main.tsx` 整条链不执行 → 白屏。**修复**：停干净 dev → 删 `%LOCALAPPDATA%\com.apibalance.monitor.dev\EBWebView\Default\{Cache,Code Cache,GPUCache}`（实测 177MB + 152MB）→ 重启 dev；验证：`icon-fetch` 与 `[ui] result` 恢复、探针 `.app-shell count=1` / `.site-card count=6`、`run-999` 无新报错。**教训写进 §0（改前端导出/签名后必须重启 dev）与 §10（白屏三步排查 + 清缓存命令）**。④**实测验收（改配置构造，非肉眼）**：把各行 `last` 人为调低 10 元再触发卡片刷新 → **`debit` 原样不变、`credit` = 10.0** ✓（这就是"充值不再干扰消费"的硬证据）；真实消耗时 `debit` 精确累加（`48.18 → 47.77` = `0.41`）✓；挂件 `render-state` = `余额 ¥47.77 / 今日已用 ¥ 0.41` ✓；测完把 `config.json` 从备份还原、`probe/trigger.json` 逐字节还原。⑤**过程中自查纠正的两处**：一是 `observe` 的**方向写反**（用 `now - prev` 导致"充值被记成消费"）——被新单测 `only_decreases_count_as_usage` 当场抓出；二是**哨兵值缺陷**：原先用 `last_at > 0` 判"是否已有区间"，导致 `at = 0` 的样本永远建不起区间（单测 `accumulation_does_not_drift` 一直差 0.01 才暴露）→ 改为只靠 `day + 币种` 判定。⑥**验证**：`tsc --noEmit` 0 错、`pnpm test` **116 全绿**、`cargo test --lib` **14 项全绿**、`cargo check --lib` 无警告。⑦**同轮打包（用户要求「封装一个 exe，替换掉桌面上的，沿用独立规则，不影响项目」）**：按 §7.5 走 `打包.ps1`（只桌面、不带 `-Android`）——先停 dev（cargo target 锁，§7.3 规矩）→ 脚本生成 `src-tauri/runtime.zip`（37MB，已剔除 `web_state/` 登录态）→ `pnpm tauri build --no-bundle`（release 构建 1m14s + dist 生产构建）→ 桌面 `API余额通_260916.1740.exe`（62.4MB）→ 删除旧包 `API余额通_260831.0247.exe`（一次成功，未被 Defender 锁定）→ 重启 dev（本轮结束状态：dev 在跑）。**独立规则照旧**：release 用 identifier `com.apibalance.monitor`（与 dev 的 `.dev` 互不干扰）、免安装单文件内嵌运行时、时间戳命名、桌面只留最新一个。冒烟测试：新 exe 起进程后 `Responding=True`、窗口标题 `API余额通` ✓。⚠️ 但**冒烟测试意外动了真实 release 配置**（我以为把 `APPDATA` 重定向到临时目录就隔离了，实测 Tauri 的 `app_config_dir()` 不读该环境变量）——已核实站点/Key/邮箱密码/AccessKey/主题/壁纸全在，唯一变化是 `usageBaselines` 按新模型给今天建了 4 条（`debit=0`），属新版本首次启动本来就会做的迁移，**无数据丢失**；教训已写进 §7.5 |
| 2026-09/16（接管轮·第二十一轮） | **启动开关改成「启动时自动打开桌宠模式」下拉框三选一（关闭自动 / 上次站点 / 置顶站点）**。**触发**：用户口径「启动时自动打开鲸鱼娘窗口(上次选择)改为：启动时自动打开桌宠模式，右边按钮换成下拉框选择：关闭自动、上次站点、置顶站点」。**改动**：①**新增纯函数 `src/lib/autoWidget.ts`**：`autoWidgetMode()` 把 `autoOpenWidgetMode` / 老布尔 `autoOpenWidget` 归一到三态（老配置 `true → "last"`、`false → "off"`，非法值也回退到布尔语义，不静默当成开启）；`pickAutoOpenUid()` 决定启动时打开哪个站点 —— `off` → null、`last` → `lastWidgetUid`（被删/停用则回退第一个启用站点）、`pinned` → 列表第一张卡（首张被停用时跳过、回退同一链条）；配 **12 项单测**（`test/autoWidget.test.ts`，含"三个选项就是三态且顺序固定"）。②**配置**：`config.rs` 新增 `auto_open_widget_mode: String`（`"off"/"last"/"pinned"`），老字段 `auto_open_widget` 保留作兼容读取 + 同步回写（保存时 `mode != "off" ⇔ true`）；`store.ts` 加 `AutoWidgetMode` 类型与 `autoOpenWidgetMode` 字段。③**界面**：`SettingsDialog` 该行文案改「**启动时自动打开桌宠模式**」，右侧控件由 `.switch` 滑动开关换成项目自制的 `MenuSelect` 下拉（`app.css` 新增 `.auto-widget-select`）；`App.tsx` 的启动 effect 改用两个纯函数（诊断标记也带上模式：`invoking:${mode}:uid=…`），`handleAutoOpenWidget` → `handleAutoWidgetMode(...)`（同时回写老布尔字段）。④⚠️ **踩坑并修掉（用户报的）**：用户反馈「下拉框展开后和按钮右对齐，现在是左对齐」。先**量测**（探针读几何）而不是改 CSS 猜：下拉按钮 `x=286.8 w=90.4`（右缘 377.2）、面板 `x=286.8 w=106`（右缘 392.8）—— **两边左缘相同**。根因是我给下拉根元素加的 `min-width: 106px`：`.menu-select-pop` 是 `right:0` 定位、对齐的是**根元素**右缘而非按钮右缘，根元素比按钮宽 → 面板右缘凸出按钮 15.6px、左缘却与按钮齐平，看起来就是"左对齐"。**修法：去掉根元素定宽**（三个选项都是 4 个汉字，按钮宽度天然稳定）。复测：**按钮右缘 392.8 == 面板右缘 392.8（差 0.0px）** ✓。⑤⚠️ **又一次白屏（同轮内第二次，根因与上次不同、更值得记）**：改完这一版后主窗口又全白——探针显示 `clicked: .settings-btn=True` 但 `.app-shell count=0`，即"DOM 渲染过、点开设置就崩"。用临时 `window.onerror → /__aqm/trace` 抓到：`Uncaught ReferenceError: autoWidgetMode is not defined`（`at App (...):1031`）。**根因是我改代码的顺序**：先在活着的 dev 上写了新用法（`autoWidgetMode(...)`）、**最后才补 import**，中间那一版被 HMR 加载后就崩了，且崩溃状态一直留着（后续 HMR 不会重新挂载已卸载的根）。**处置**：停 dev → 清 WebView2 缓存 → 重启 → 复测通过（`.app-shell n=1`、设置页打开、下拉框 '关闭自动▾' 在 x=286.8/inViewport ✓）。**教训已并入 §0「改前端导入/导出/组件 props 后一律重启 dev」与 §10 白屏三步法**。⑥**验证**：`tsc --noEmit` 0 错、`pnpm test` **128 项全绿**（7 个文件，含新增 12 项）、`cargo test --lib` **14 项全绿**、`cargo check --lib` 无警告；`probe/trigger.json` 与 `index.html`（临时诊断脚本）均已还原至与 HEAD 一致 |
| 2026-09/16（接管轮·第二十二轮） | **安卓端更新 + 模拟器验证（自动提权）**。**触发**：用户要求「更新推到安卓端，使用模拟器验证，自动提权」。**流程与改动**：①**先摸环境、不靠假设**：`rustup target list` 显示 4 个安卓 Rust 目标都已装；`tauri_avd` 的 `config.ini` 是 `abi.type = x86_64`（镜像 `android-35 google_apis x86_64`）→ **项目惯例的 `--target aarch64` 最小包在这个模拟器上有装不上的风险**，故改用**不带 `--target` 的全 ABI universal 包**（实测 106.8MB）。②停 dev（cargo target 锁）→ `pnpm build` 刷 dist（安卓构建前必做）→ 写临时提权 bat（ASCII；设置 PATH / ANDROID_HOME / NDK_HOME / JAVA_HOME）→ `Start-Process -Verb RunAs` 自动提权跑 `pnpm tauri android build --apk`，日志 `D:\android-universal-build.log`，后台盯 `BUILD_EXIT=` → **BUILD_EXIT=0**。③`apksigner`（debug keystore）签名 → 桌面 **API余额通-安卓_260916.2048.apk**，`apksigner verify` 通过；按惯例清掉桌面旧 APK（8/31 那个）。④模拟器（`-avd tauri_avd -gpu host`，Android 15，`emulator-5554`）→ `adb install -r` **Success**（`-r` 保留原有应用数据，故未卸载）。⑤**验证（截图肉眼 + 配置实测，不是"编译过了"就算）**：`topResumedActivity` = `com.apibalance.monitor/.MainActivity`、日志无 FATAL；主界面 7 站点真实余额（DeepSeek ¥45.26 / Poke ¥2.87 / 绿洲 ¥4.99 / 阿里云 ¥8.64 / 基元律动 ¥0.01 / Max66 ¥0.00 / MiniMax 未刷新——启动时先弹的是应用**自己的内嵌 WebView 登录页**，点「取消」即回主界面）；`adb root` 读 `/data/data/com.apibalance.monitor/config.json` → **`usageBaselines` 已迁移到新模型**（7 站点，字段为 `opening/last/debit/credit`；`autoOpenWidgetMode` 为空 + `autoOpenWidget=false` → 归一为「关闭自动」）；设置页截图确认主题/背景壁纸/高级设置都在，且**桌面专用的「启动时自动打开桌宠模式」那一行正确地不出现**（`isAndroidPlatform()` 门控有效）。⑥⚠️ **顺带发现、但只修自己引入的问题**：整包构建暴露约 15 条安卓 `never used` 警告，其中 **6 条来自本轮新模块 `widget_menu.rs`**（桌面专用的 `MENU_*` 常量与 `menu_origin` 未门控）→ 补 `#[cfg(desktop)]`；**桌面 `cargo check` 看不见这类警告**（那边都被用到），只有安卓目标会暴露，故用 §7.6 的编译门复验：`cargo check --lib --target x86_64-linux-android` 通过、我引入的警告清零。**仍剩 6 条既有的**（`widget.rs` 的 `DEFAULT_W/H`、`MIN_W/H`；`network.rs` 的 `record_node_pid`/`clear_node_pid`；release 另有 `ensure_runtime`/`RUNTIME_ZIP`）已按"待清理"记入 §7.6，**未擅自改别人的代码**。⑦**验证门**：`tsc` 0 错、`pnpm test` **128 全绿**、`cargo test --lib` **14 项全绿**、桌面 `cargo check --lib` 无警告；临时 bat 与验证截图已清理；dev 收尾时重启回运行状态 |
| 2026-09/16（接管轮·第二十三轮） | **推送前审计与仓库清洁重建**。**触发**：用户「准备一下，准备推送到GitHub仓库」。**审计（这次真抓到了东西）**：①先做常规项 —— 跟踪文件里 `sk-…`/`LTAI…`/`access_key_secret` **一个都没有**；`git log --all --diff-filter=A --name-only` 显示历史**从未**入仓过 `config.json`/`web_state`/`browser_*.json`/`*.keystore`/`node.exe`/`runtime.zip`；提交身份是 `Zimu233L <zimu233l@users.noreply.github.com>`（GitHub 匿名邮箱）；入仓 210 文件 / 20.8MB，最大单文件 6.1MB。②**但**盘点"最大文件"时注意到被跟踪的 `minmax.7z`（3.11MB，基线提交 `374b524` 引入）—— 本机没装 7-Zip 时无法判断内容，用户提示"有 360压缩"，于是改用本机已有的 7z 命令行（`C:\Program Files (x86)\Game Cheats Manager\dependency\7z\7z.exe`）**列包内容**，一列就露馅：包内带 `minmax/web_state/minimax_web.json`（Playwright storageState，**16 条 cookie** 覆盖 `.minimaxi.com`/`platform.minimaxi.com`/`account.minimaxi.com`，含 `_sid`/`_token`/`_oauth_state`，**最长有效期 2027-10-03**），blob `de59bc6a…` 存在于**全部 32 个提交的树**里 → **推送即公开**（只删工作区文件没用，历史里还在）。③**按用户选择（方案 B）处置**：先 `Compress-Archive` 备份旧 `.git` 到 `D:\DeepSeek\api-quota-monitor-tauri-git-backup-260916.zip`（20.3MB；⚠️ 里面仍含那套 cookie，作废凭据后应删）→ 确认无任何代码/脚本引用 `icons.bak`/`minmax.7z` → 删这两项（`icons.bak` 35 文件 3.33MB，属旧图标备份，`icons/` 才在用）→ `Remove-Item .git -Recurse -Force` → `git init -b main` + 重设仓库级身份 → `git add -A` → 单笔初始提交（含今天四轮改动与本节文档）。④**复验**：`git log --all --oneline` 仅 1 笔；`git rev-list --objects --all` 里**搜不到** `de59bc6a`；`git ls-files` 无 `minmax.7z`/`icons.bak`；工作区干净；入仓 174 文件 / 14.4MB（最大单文件 6.1MB 字体）。⑤**遗留（只能用户做）**：去 MiniMax 后台**登出其它会话或改密码**（cookie 到 2027 年才过期，清 git ≠ 作废凭据）；确认不需要旧提交后删掉备份 zip。⑥**写进文档的教训**：`.gitignore` 只挡工作区文件、**挡不住打进压缩包的副本** → 任何要入仓的归档（`.7z`/`.zip`）必须**先列内容**（§9）；文档 §0 的仓库状态、§8 新增审计条目一并更新 |
