# 统一换行与二进制判定（2026-09/15 建库时新增）
# 目的：避免 Windows 检出时把源码换成 CRLF、把二进制当文本做行尾转换而损坏。
#
# 实测本仓现状（2026-09/15）：
#   - 打包.ps1 / probe/*.ps1 ：**LF 换行 + UTF-8 BOM**（不是 CRLF）→ 这里必须用 eol=lf，
#     否则 git 会在检出时把脚本改写成 CRLF，而这几个脚本改一个字节都属高风险（见交接文档 §7.1）
#   - minmax/启动.bat ：CRLF → eol=crlf 保持不变
# 规则：统一「仓库内 = 工作区 = LF」，除 .bat/.cmd 外不做任何行尾转换。

# 默认按文本归一化
* text=auto eol=lf

# 明确按文本处理
*.ts     text eol=lf
*.tsx    text eol=lf
*.js     text eol=lf
*.mjs    text eol=lf
*.json   text eol=lf
*.css    text eol=lf
*.html   text eol=lf
*.rs     text eol=lf
*.toml   text eol=lf
*.md     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.kt     text eol=lf
*.kts    text eol=lf
*.xml    text eol=lf
*.gradle text eol=lf

# ⚠️ PowerShell 脚本：保持现有 LF（BOM 由文件内容本身携带，git 不管 BOM）
*.ps1    text eol=lf
*.psm1   text eol=lf

# Windows 批处理保持 CRLF
*.bat    text eol=crlf
*.cmd    text eol=crlf

# ⚠️ 明确按二进制处理，禁止任何行尾/编码转换
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.icns   binary
*.woff2  binary
*.woff   binary
*.ttf    binary
*.exe    binary
*.dll    binary
*.so     binary
*.zip    binary
*.7z     binary
*.apk    binary
*.jar    binary
*.keystore binary
